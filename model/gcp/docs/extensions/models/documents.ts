// Auto-generated extension model for @swamp/gcp/docs/documents
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
} from "./_lib/gcp.ts";

const BASE_URL = "https://docs.googleapis.com/";

const GET_CONFIG = {
  "id": "docs.documents.get",
  "path": "v1/documents/{documentId}",
  "httpMethod": "GET",
  "parameterOrder": [
    "documentId",
  ],
  "parameters": {
    "documentId": {
      "location": "path",
      "required": true,
    },
    "includeTabsContent": {
      "location": "query",
    },
    "suggestionsViewMode": {
      "location": "query",
    },
  },
} as const;

const INSERT_CONFIG = {
  "id": "docs.documents.create",
  "path": "v1/documents",
  "httpMethod": "POST",
  "parameterOrder": [],
  "parameters": {},
} as const;

const GlobalArgsSchema = z.object({
  name: z.string().describe(
    "Instance name for this resource (used as the unique identifier in the factory pattern)",
  ),
  body: z.object({
    content: z.array(z.object({
      endIndex: z.number().int().describe(
        "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
      ).optional(),
      paragraph: z.object({
        bullet: z.object({
          listId: z.string().describe(
            "The ID of the list this paragraph belongs to.",
          ).optional(),
          nestingLevel: z.number().int().describe(
            "The nesting level of this paragraph in the list.",
          ).optional(),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            baselineOffset: z.enum([
              "BASELINE_OFFSET_UNSPECIFIED",
              "NONE",
              "SUPERSCRIPT",
              "SUBSCRIPT",
            ]).describe(
              "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
            ).optional(),
            bold: z.boolean().describe(
              "Whether or not the text is rendered as bold.",
            ).optional(),
            fontSize: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            italic: z.boolean().describe(
              "Whether or not the text is italicized.",
            ).optional(),
            link: z.object({
              bookmark: z.object({
                id: z.string().describe(
                  "The ID of a bookmark in this document.",
                ).optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this bookmark.",
                ).optional(),
              }).describe("A reference to a bookmark in this document.")
                .optional(),
              bookmarkId: z.string().describe(
                "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              heading: z.object({
                id: z.string().describe("The ID of a heading in this document.")
                  .optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this heading.",
                ).optional(),
              }).describe("A reference to a heading in this document.")
                .optional(),
              headingId: z.string().describe(
                "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              tabId: z.string().describe("The ID of a tab in this document.")
                .optional(),
              url: z.string().describe("An external URL.").optional(),
            }).describe(
              "A reference to another portion of a document or an external URL resource.",
            ).optional(),
            smallCaps: z.boolean().describe(
              "Whether or not the text is in small capital letters.",
            ).optional(),
            strikethrough: z.boolean().describe(
              "Whether or not the text is struck through.",
            ).optional(),
            underline: z.boolean().describe(
              "Whether or not the text is underlined.",
            ).optional(),
            weightedFontFamily: z.object({
              fontFamily: z.string().describe(
                "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
              ).optional(),
              weight: z.number().int().describe(
                "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
              ).optional(),
            }).describe("Represents a font family and weight of text.")
              .optional(),
          }).describe(
            "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
        }).describe("Describes the bullet of a paragraph.").optional(),
        elements: z.array(z.object({
          autoText: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this AutoText, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            type: z.enum(["TYPE_UNSPECIFIED", "PAGE_NUMBER", "PAGE_COUNT"])
              .describe("The type of this auto text.").optional(),
          }).describe(
            "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
          ).optional(),
          columnBreak: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
          ).optional(),
          dateElement: z.object({
            dateElementProperties: z.object({
              dateFormat: z.enum([
                "DATE_FORMAT_UNSPECIFIED",
                "DATE_FORMAT_CUSTOM",
                "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                "DATE_FORMAT_MONTH_DAY_FULL",
                "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                "DATE_FORMAT_ISO8601",
              ]).describe(
                "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
              ).optional(),
              displayText: z.string().describe(
                "Output only. Indicates how the DateElement is displayed in the document.",
              ).optional(),
              locale: z.string().describe(
                "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
              ).optional(),
              timeFormat: z.enum([
                "TIME_FORMAT_UNSPECIFIED",
                "TIME_FORMAT_DISABLED",
                "TIME_FORMAT_HOUR_MINUTE",
                "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
              ]).describe(
                "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
              ).optional(),
              timeZoneId: z.string().describe(
                "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
              ).optional(),
              timestamp: z.string().describe(
                "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
              ).optional(),
            }).describe("Properties of a DateElement.").optional(),
            dateId: z.string().describe(
              "Output only. The unique ID of this date.",
            ).optional(),
            suggestedDateElementPropertiesChanges: z.record(
              z.string(),
              z.object({
                dateElementProperties: z.object({
                  dateFormat: z.enum([
                    "DATE_FORMAT_UNSPECIFIED",
                    "DATE_FORMAT_CUSTOM",
                    "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                    "DATE_FORMAT_MONTH_DAY_FULL",
                    "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                    "DATE_FORMAT_ISO8601",
                  ]).describe(
                    "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                  ).optional(),
                  displayText: z.string().describe(
                    "Output only. Indicates how the DateElement is displayed in the document.",
                  ).optional(),
                  locale: z.string().describe(
                    "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                  ).optional(),
                  timeFormat: z.enum([
                    "TIME_FORMAT_UNSPECIFIED",
                    "TIME_FORMAT_DISABLED",
                    "TIME_FORMAT_HOUR_MINUTE",
                    "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                  ]).describe(
                    "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                  ).optional(),
                  timeZoneId: z.string().describe(
                    "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                  ).optional(),
                  timestamp: z.string().describe(
                    "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                  ).optional(),
                }).describe("Properties of a DateElement.").optional(),
                dateElementPropertiesSuggestionState: z.object({
                  dateFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to date_format.",
                  ).optional(),
                  localeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to locale.",
                  ).optional(),
                  timeFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to time_format.",
                  ).optional(),
                  timeZoneIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to time_zone_id.",
                  ).optional(),
                  timestampSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to timestamp.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested changes to the date element properties, keyed by suggestion ID.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this DateElement, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A date instance mentioned in a document.").optional(),
          endIndex: z.number().int().describe(
            "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
          ).optional(),
          equation: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe("A ParagraphElement representing an equation.")
            .optional(),
          footnoteReference: z.object({
            footnoteId: z.string().describe(
              "The ID of the footnote that contains the content of this footnote reference.",
            ).optional(),
            footnoteNumber: z.string().describe(
              "The rendered number of this footnote.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
          ).optional(),
          horizontalRule: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A ParagraphElement representing a horizontal line.")
            .optional(),
          inlineObjectElement: z.object({
            inlineObjectId: z.string().describe(
              "The ID of the InlineObject this element contains.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A ParagraphElement that contains an InlineObject.")
            .optional(),
          pageBreak: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
          ).optional(),
          person: z.object({
            personId: z.string().describe(
              "Output only. The unique ID of this link.",
            ).optional(),
            personProperties: z.object({
              email: z.string().describe(
                "The email address linked to this Person. This field is always present.",
              ).optional(),
              name: z.string().describe(
                "The name of the person if it's displayed in the link text instead of the person's email address.",
              ).optional(),
            }).describe("Properties specific to a linked Person.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this Person, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
          ).optional(),
          richLink: z.object({
            richLinkId: z.string().describe("Output only. The ID of this link.")
              .optional(),
            richLinkProperties: z.object({
              mimeType: z.string().describe(
                "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
              ).optional(),
              title: z.string().describe(
                "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
              ).optional(),
              uri: z.string().describe(
                "The URI to the RichLink. This is always present.",
              ).optional(),
            }).describe("Properties specific to a RichLink.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this RichLink, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this paragraph element, in UTF-16 code units.",
          ).optional(),
          textRun: z.object({
            content: z.string().describe(
              "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this run, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement that represents a run of text that all has the same styling.",
          ).optional(),
        })).describe(
          "The content of the paragraph, broken down into its component parts.",
        ).optional(),
        paragraphStyle: z.object({
          alignment: z.enum([
            "ALIGNMENT_UNSPECIFIED",
            "START",
            "CENTER",
            "END",
            "JUSTIFIED",
          ]).describe("The text alignment for this paragraph.").optional(),
          avoidWidowAndOrphan: z.boolean().describe(
            "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
          ).optional(),
          borderBetween: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderBottom: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderLeft: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderRight: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderTop: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          direction: z.enum([
            "CONTENT_DIRECTION_UNSPECIFIED",
            "LEFT_TO_RIGHT",
            "RIGHT_TO_LEFT",
          ]).describe(
            "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
          ).optional(),
          headingId: z.string().describe(
            "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
          ).optional(),
          indentEnd: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          indentFirstLine: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          indentStart: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          keepLinesTogether: z.boolean().describe(
            "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
          ).optional(),
          keepWithNext: z.boolean().describe(
            "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
          ).optional(),
          lineSpacing: z.number().describe(
            "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
          ).optional(),
          namedStyleType: z.enum([
            "NAMED_STYLE_TYPE_UNSPECIFIED",
            "NORMAL_TEXT",
            "TITLE",
            "SUBTITLE",
            "HEADING_1",
            "HEADING_2",
            "HEADING_3",
            "HEADING_4",
            "HEADING_5",
            "HEADING_6",
          ]).describe(
            "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
          ).optional(),
          pageBreakBefore: z.boolean().describe(
            "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
          ).optional(),
          shading: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
          }).describe("The shading of a paragraph.").optional(),
          spaceAbove: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          spaceBelow: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          spacingMode: z.enum([
            "SPACING_MODE_UNSPECIFIED",
            "NEVER_COLLAPSE",
            "COLLAPSE_LISTS",
          ]).describe("The spacing mode for the paragraph.").optional(),
          tabStops: z.array(z.object({
            alignment: z.enum([
              "TAB_STOP_ALIGNMENT_UNSPECIFIED",
              "START",
              "CENTER",
              "END",
            ]).describe(
              "The alignment of this tab stop. If unset, the value defaults to START.",
            ).optional(),
            offset: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          })).describe(
            "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
          ).optional(),
        }).describe(
          "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
        ).optional(),
        positionedObjectIds: z.array(z.string()).describe(
          "The IDs of the positioned objects tethered to this paragraph.",
        ).optional(),
        suggestedBulletChanges: z.record(
          z.string(),
          z.object({
            bullet: z.object({
              listId: z.string().describe(
                "The ID of the list this paragraph belongs to.",
              ).optional(),
              nestingLevel: z.number().int().describe(
                "The nesting level of this paragraph in the list.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            }).describe("Describes the bullet of a paragraph.").optional(),
            bulletSuggestionState: z.object({
              listIdSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to the list_id.",
              ).optional(),
              nestingLevelSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to the nesting_level.",
              ).optional(),
              textStyleSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to background_color.",
                ).optional(),
                baselineOffsetSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to baseline_offset.",
                ).optional(),
                boldSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to bold.",
                ).optional(),
                fontSizeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to font_size.",
                ).optional(),
                foregroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to foreground_color.",
                ).optional(),
                italicSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to italic.",
                ).optional(),
                linkSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to link.",
                ).optional(),
                smallCapsSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to small_caps.",
                ).optional(),
                strikethroughSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to strikethrough.",
                ).optional(),
                underlineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to underline.",
                ).optional(),
                weightedFontFamilySuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to weighted_font_family.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
            ).optional(),
          }),
        ).describe("The suggested changes to this paragraph's bullet.")
          .optional(),
        suggestedParagraphStyleChanges: z.record(
          z.string(),
          z.object({
            paragraphStyle: z.object({
              alignment: z.enum([
                "ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
                "JUSTIFIED",
              ]).describe("The text alignment for this paragraph.").optional(),
              avoidWidowAndOrphan: z.boolean().describe(
                "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
              ).optional(),
              borderBetween: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              direction: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
              ).optional(),
              headingId: z.string().describe(
                "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
              ).optional(),
              indentEnd: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              keepLinesTogether: z.boolean().describe(
                "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              keepWithNext: z.boolean().describe(
                "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              lineSpacing: z.number().describe(
                "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
              ).optional(),
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
              ).optional(),
              pageBreakBefore: z.boolean().describe(
                "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
              ).optional(),
              shading: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
              }).describe("The shading of a paragraph.").optional(),
              spaceAbove: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spaceBelow: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spacingMode: z.enum([
                "SPACING_MODE_UNSPECIFIED",
                "NEVER_COLLAPSE",
                "COLLAPSE_LISTS",
              ]).describe("The spacing mode for the paragraph.").optional(),
              tabStops: z.array(z.object({
                alignment: z.enum([
                  "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                ]).describe(
                  "The alignment of this tab stop. If unset, the value defaults to START.",
                ).optional(),
                offset: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
              ).optional(),
            }).describe(
              "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            paragraphStyleSuggestionState: z.object({
              alignmentSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to alignment.",
              ).optional(),
              avoidWidowAndOrphanSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to avoid_widow_and_orphan.",
              ).optional(),
              borderBetweenSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_between.",
              ).optional(),
              borderBottomSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_bottom.",
              ).optional(),
              borderLeftSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_left.",
              ).optional(),
              borderRightSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_right.",
              ).optional(),
              borderTopSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_top.",
              ).optional(),
              directionSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to direction.",
              ).optional(),
              headingIdSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to heading_id.",
              ).optional(),
              indentEndSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_end.",
              ).optional(),
              indentFirstLineSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_first_line.",
              ).optional(),
              indentStartSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_start.",
              ).optional(),
              keepLinesTogetherSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to keep_lines_together.",
              ).optional(),
              keepWithNextSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to keep_with_next.",
              ).optional(),
              lineSpacingSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to line_spacing.",
              ).optional(),
              namedStyleTypeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to named_style_type.",
              ).optional(),
              pageBreakBeforeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to page_break_before.",
              ).optional(),
              shadingSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to the Shading.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
              ).optional(),
              spaceAboveSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to space_above.",
              ).optional(),
              spaceBelowSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to space_below.",
              ).optional(),
              spacingModeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to spacing_mode.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
            ).optional(),
          }),
        ).describe(
          "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
        ).optional(),
        suggestedPositionedObjectIds: z.record(
          z.string(),
          z.object({
            objectIds: z.array(z.string()).describe("The object IDs.")
              .optional(),
          }),
        ).describe(
          "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
        ).optional(),
      }).describe(
        "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
      ).optional(),
      sectionBreak: z.object({
        sectionStyle: z.object({
          columnProperties: z.array(z.object({
            paddingEnd: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          })).describe(
            "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
          ).optional(),
          columnSeparatorStyle: z.enum([
            "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
            "NONE",
            "BETWEEN_EACH_COLUMN",
          ]).describe(
            "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          contentDirection: z.enum([
            "CONTENT_DIRECTION_UNSPECIFIED",
            "LEFT_TO_RIGHT",
            "RIGHT_TO_LEFT",
          ]).describe(
            "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          defaultFooterId: z.string().describe(
            "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          defaultHeaderId: z.string().describe(
            "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          evenPageFooterId: z.string().describe(
            "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          evenPageHeaderId: z.string().describe(
            "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          firstPageFooterId: z.string().describe(
            "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          firstPageHeaderId: z.string().describe(
            "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          flipPageOrientation: z.boolean().describe(
            "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          marginBottom: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginFooter: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginHeader: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginLeft: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginRight: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginTop: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          pageNumberStart: z.number().int().describe(
            "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          sectionType: z.enum([
            "SECTION_TYPE_UNSPECIFIED",
            "CONTINUOUS",
            "NEXT_PAGE",
          ]).describe("Output only. The type of section.").optional(),
          useFirstPageHeaderFooter: z.boolean().describe(
            "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
        }).describe("The styling that applies to a section.").optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
      }).describe(
        "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
      ).optional(),
      startIndex: z.number().int().describe(
        "The zero-based start index of this structural element, in UTF-16 code units.",
      ).optional(),
      table: z.object({
        columns: z.number().int().describe(
          "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
        ).optional(),
        rows: z.number().int().describe("Number of rows in the table.")
          .optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
        tableRows: z.array(z.object({
          endIndex: z.number().int().describe(
            "The zero-based end index of this row, exclusive, in UTF-16 code units.",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this row, in UTF-16 code units.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInsertionIds: z.array(z.string()).describe(
            "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedTableRowStyleChanges: z.record(
            z.string(),
            z.object({
              tableRowStyle: z.object({
                minRowHeight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                preventOverflow: z.boolean().describe(
                  "Whether the row cannot overflow across page or column boundaries.",
                ).optional(),
                tableHeader: z.boolean().describe(
                  "Whether the row is a table header.",
                ).optional(),
              }).describe("Styles that apply to a table row.").optional(),
              tableRowStyleSuggestionState: z.object({
                minRowHeightSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to min_row_height.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested style changes to this row, keyed by suggestion ID.",
          ).optional(),
          tableCells: z.array(z.object({
            content: z.array(z.string()).describe("The content of the cell.")
              .optional(),
            endIndex: z.number().int().describe(
              "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this cell, in UTF-16 code units.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTableCellStyleChanges: z.record(
              z.string(),
              z.object({
                tableCellStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  columnSpan: z.number().int().describe(
                    "The column span of the cell. This property is read-only.",
                  ).optional(),
                  contentAlignment: z.enum([
                    "CONTENT_ALIGNMENT_UNSPECIFIED",
                    "CONTENT_ALIGNMENT_UNSUPPORTED",
                    "TOP",
                    "MIDDLE",
                    "BOTTOM",
                  ]).describe(
                    "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                  ).optional(),
                  paddingBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  rowSpan: z.number().int().describe(
                    "The row span of the cell. This property is read-only.",
                  ).optional(),
                }).describe(
                  "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                ).optional(),
                tableCellStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  borderBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_bottom.",
                  ).optional(),
                  borderLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_left.",
                  ).optional(),
                  borderRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_right.",
                  ).optional(),
                  borderTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_top.",
                  ).optional(),
                  columnSpanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to column_span.",
                  ).optional(),
                  contentAlignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to content_alignment.",
                  ).optional(),
                  paddingBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_bottom.",
                  ).optional(),
                  paddingLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_left.",
                  ).optional(),
                  paddingRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_right.",
                  ).optional(),
                  paddingTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_top.",
                  ).optional(),
                  rowSpanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to row_span.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested changes to the table cell style, keyed by suggestion ID.",
            ).optional(),
            tableCellStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              columnSpan: z.number().int().describe(
                "The column span of the cell. This property is read-only.",
              ).optional(),
              contentAlignment: z.enum([
                "CONTENT_ALIGNMENT_UNSPECIFIED",
                "CONTENT_ALIGNMENT_UNSUPPORTED",
                "TOP",
                "MIDDLE",
                "BOTTOM",
              ]).describe(
                "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
              ).optional(),
              paddingBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              rowSpan: z.number().int().describe(
                "The row span of the cell. This property is read-only.",
              ).optional(),
            }).describe(
              "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
            ).optional(),
          })).describe(
            "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
          ).optional(),
          tableRowStyle: z.object({
            minRowHeight: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            preventOverflow: z.boolean().describe(
              "Whether the row cannot overflow across page or column boundaries.",
            ).optional(),
            tableHeader: z.boolean().describe(
              "Whether the row is a table header.",
            ).optional(),
          }).describe("Styles that apply to a table row.").optional(),
        })).describe("The contents and style of each row.").optional(),
        tableStyle: z.object({
          tableColumnProperties: z.array(z.object({
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            widthType: z.enum([
              "WIDTH_TYPE_UNSPECIFIED",
              "EVENLY_DISTRIBUTED",
              "FIXED_WIDTH",
            ]).describe("The width type of the column.").optional(),
          })).describe(
            "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
          ).optional(),
        }).describe("Styles that apply to a table.").optional(),
      }).describe("A StructuralElement representing a table.").optional(),
      tableOfContents: z.object({
        content: z.array(z.string()).describe(
          "The content of the table of contents.",
        ).optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
      }).describe("A StructuralElement representing a table of contents.")
        .optional(),
    })).describe(
      "The contents of the body. The indexes for the body's content begin at zero.",
    ).optional(),
  }).describe(
    "The document body. The body typically contains the full document contents except for headers, footers, and footnotes.",
  ).optional(),
  documentStyle: z.object({
    background: z.object({
      color: z.object({
        color: z.object({
          rgbColor: z.object({
            blue: z.number().describe(
              "The blue component of the color, from 0.0 to 1.0.",
            ).optional(),
            green: z.number().describe(
              "The green component of the color, from 0.0 to 1.0.",
            ).optional(),
            red: z.number().describe(
              "The red component of the color, from 0.0 to 1.0.",
            ).optional(),
          }).describe("An RGB color.").optional(),
        }).describe("A solid color.").optional(),
      }).describe(
        "A color that can either be fully opaque or fully transparent.",
      ).optional(),
    }).describe("Represents the background of a document.").optional(),
    defaultFooterId: z.string().describe(
      "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    defaultHeaderId: z.string().describe(
      "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    documentFormat: z.object({
      documentMode: z.enum(["DOCUMENT_MODE_UNSPECIFIED", "PAGES", "PAGELESS"])
        .describe("Whether the document has pages or is pageless.").optional(),
    }).describe("Represents document-level format settings.").optional(),
    evenPageFooterId: z.string().describe(
      "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    evenPageHeaderId: z.string().describe(
      "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    firstPageFooterId: z.string().describe(
      "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    firstPageHeaderId: z.string().describe(
      "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    flipPageOrientation: z.boolean().describe(
      "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    marginBottom: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginFooter: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginHeader: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginLeft: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginRight: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginTop: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    pageNumberStart: z.number().int().describe(
      "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    pageSize: z.object({
      height: z.object({
        magnitude: z.number().describe("The magnitude.").optional(),
        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
          "The units for magnitude.",
        ).optional(),
      }).describe("A magnitude in a single direction in the specified units.")
        .optional(),
      width: z.object({
        magnitude: z.number().describe("The magnitude.").optional(),
        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
          "The units for magnitude.",
        ).optional(),
      }).describe("A magnitude in a single direction in the specified units.")
        .optional(),
    }).describe("A width and height.").optional(),
    useCustomHeaderFooterMargins: z.boolean().describe(
      "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    useEvenPageHeaderFooter: z.boolean().describe(
      "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    useFirstPageHeaderFooter: z.boolean().describe(
      "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
  }).describe("The style of the document.").optional(),
  namedStyles: z.object({
    styles: z.array(z.object({
      namedStyleType: z.enum([
        "NAMED_STYLE_TYPE_UNSPECIFIED",
        "NORMAL_TEXT",
        "TITLE",
        "SUBTITLE",
        "HEADING_1",
        "HEADING_2",
        "HEADING_3",
        "HEADING_4",
        "HEADING_5",
        "HEADING_6",
      ]).describe("The type of this named style.").optional(),
      paragraphStyle: z.object({
        alignment: z.enum([
          "ALIGNMENT_UNSPECIFIED",
          "START",
          "CENTER",
          "END",
          "JUSTIFIED",
        ]).describe("The text alignment for this paragraph.").optional(),
        avoidWidowAndOrphan: z.boolean().describe(
          "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
        ).optional(),
        borderBetween: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderBottom: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderLeft: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderRight: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderTop: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        direction: z.enum([
          "CONTENT_DIRECTION_UNSPECIFIED",
          "LEFT_TO_RIGHT",
          "RIGHT_TO_LEFT",
        ]).describe(
          "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
        ).optional(),
        headingId: z.string().describe(
          "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
        ).optional(),
        indentEnd: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        indentFirstLine: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        indentStart: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        keepLinesTogether: z.boolean().describe(
          "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
        ).optional(),
        keepWithNext: z.boolean().describe(
          "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
        ).optional(),
        lineSpacing: z.number().describe(
          "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
        ).optional(),
        namedStyleType: z.enum([
          "NAMED_STYLE_TYPE_UNSPECIFIED",
          "NORMAL_TEXT",
          "TITLE",
          "SUBTITLE",
          "HEADING_1",
          "HEADING_2",
          "HEADING_3",
          "HEADING_4",
          "HEADING_5",
          "HEADING_6",
        ]).describe(
          "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
        ).optional(),
        pageBreakBefore: z.boolean().describe(
          "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
        ).optional(),
        shading: z.object({
          backgroundColor: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
        }).describe("The shading of a paragraph.").optional(),
        spaceAbove: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        spaceBelow: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        spacingMode: z.enum([
          "SPACING_MODE_UNSPECIFIED",
          "NEVER_COLLAPSE",
          "COLLAPSE_LISTS",
        ]).describe("The spacing mode for the paragraph.").optional(),
        tabStops: z.array(z.object({
          alignment: z.enum([
            "TAB_STOP_ALIGNMENT_UNSPECIFIED",
            "START",
            "CENTER",
            "END",
          ]).describe(
            "The alignment of this tab stop. If unset, the value defaults to START.",
          ).optional(),
          offset: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        })).describe(
          "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
        ).optional(),
      }).describe(
        "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
      ).optional(),
      textStyle: z.object({
        backgroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number().describe(
                "The blue component of the color, from 0.0 to 1.0.",
              ).optional(),
              green: z.number().describe(
                "The green component of the color, from 0.0 to 1.0.",
              ).optional(),
              red: z.number().describe(
                "The red component of the color, from 0.0 to 1.0.",
              ).optional(),
            }).describe("An RGB color.").optional(),
          }).describe("A solid color.").optional(),
        }).describe(
          "A color that can either be fully opaque or fully transparent.",
        ).optional(),
        baselineOffset: z.enum([
          "BASELINE_OFFSET_UNSPECIFIED",
          "NONE",
          "SUPERSCRIPT",
          "SUBSCRIPT",
        ]).describe(
          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
        ).optional(),
        bold: z.boolean().describe(
          "Whether or not the text is rendered as bold.",
        ).optional(),
        fontSize: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        foregroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number().describe(
                "The blue component of the color, from 0.0 to 1.0.",
              ).optional(),
              green: z.number().describe(
                "The green component of the color, from 0.0 to 1.0.",
              ).optional(),
              red: z.number().describe(
                "The red component of the color, from 0.0 to 1.0.",
              ).optional(),
            }).describe("An RGB color.").optional(),
          }).describe("A solid color.").optional(),
        }).describe(
          "A color that can either be fully opaque or fully transparent.",
        ).optional(),
        italic: z.boolean().describe("Whether or not the text is italicized.")
          .optional(),
        link: z.object({
          bookmark: z.object({
            id: z.string().describe("The ID of a bookmark in this document.")
              .optional(),
            tabId: z.string().describe(
              "The ID of the tab containing this bookmark.",
            ).optional(),
          }).describe("A reference to a bookmark in this document.").optional(),
          bookmarkId: z.string().describe(
            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
          ).optional(),
          heading: z.object({
            id: z.string().describe("The ID of a heading in this document.")
              .optional(),
            tabId: z.string().describe(
              "The ID of the tab containing this heading.",
            ).optional(),
          }).describe("A reference to a heading in this document.").optional(),
          headingId: z.string().describe(
            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
          ).optional(),
          tabId: z.string().describe("The ID of a tab in this document.")
            .optional(),
          url: z.string().describe("An external URL.").optional(),
        }).describe(
          "A reference to another portion of a document or an external URL resource.",
        ).optional(),
        smallCaps: z.boolean().describe(
          "Whether or not the text is in small capital letters.",
        ).optional(),
        strikethrough: z.boolean().describe(
          "Whether or not the text is struck through.",
        ).optional(),
        underline: z.boolean().describe(
          "Whether or not the text is underlined.",
        ).optional(),
        weightedFontFamily: z.object({
          fontFamily: z.string().describe(
            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
          ).optional(),
          weight: z.number().int().describe(
            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
          ).optional(),
        }).describe("Represents a font family and weight of text.").optional(),
      }).describe(
        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
      ).optional(),
    })).describe(
      "The named styles. There's an entry for each of the possible named style types.",
    ).optional(),
  }).describe(
    "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
  ).optional(),
  tabs: z.array(z.object({
    childTabs: z.array(z.string()).describe(
      "The child tabs nested within this tab.",
    ).optional(),
    documentTab: z.object({
      body: z.object({
        content: z.array(z.object({
          endIndex: z.number().int().describe(
            "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
          ).optional(),
          paragraph: z.object({
            bullet: z.object({
              listId: z.string().describe(
                "The ID of the list this paragraph belongs to.",
              ).optional(),
              nestingLevel: z.number().int().describe(
                "The nesting level of this paragraph in the list.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            }).describe("Describes the bullet of a paragraph.").optional(),
            elements: z.array(z.object({
              autoText: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                type: z.enum(["TYPE_UNSPECIFIED", "PAGE_NUMBER", "PAGE_COUNT"])
                  .describe("The type of this auto text.").optional(),
              }).describe(
                "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
              ).optional(),
              columnBreak: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
              ).optional(),
              dateElement: z.object({
                dateElementProperties: z.object({
                  dateFormat: z.enum([
                    "DATE_FORMAT_UNSPECIFIED",
                    "DATE_FORMAT_CUSTOM",
                    "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                    "DATE_FORMAT_MONTH_DAY_FULL",
                    "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                    "DATE_FORMAT_ISO8601",
                  ]).describe(
                    "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                  ).optional(),
                  displayText: z.string().describe(
                    "Output only. Indicates how the DateElement is displayed in the document.",
                  ).optional(),
                  locale: z.string().describe(
                    "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                  ).optional(),
                  timeFormat: z.enum([
                    "TIME_FORMAT_UNSPECIFIED",
                    "TIME_FORMAT_DISABLED",
                    "TIME_FORMAT_HOUR_MINUTE",
                    "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                  ]).describe(
                    "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                  ).optional(),
                  timeZoneId: z.string().describe(
                    "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                  ).optional(),
                  timestamp: z.string().describe(
                    "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                  ).optional(),
                }).describe("Properties of a DateElement.").optional(),
                dateId: z.string().describe(
                  "Output only. The unique ID of this date.",
                ).optional(),
                suggestedDateElementPropertiesChanges: z.record(
                  z.string(),
                  z.object({
                    dateElementProperties: z.object({
                      dateFormat: z.enum([
                        "DATE_FORMAT_UNSPECIFIED",
                        "DATE_FORMAT_CUSTOM",
                        "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                        "DATE_FORMAT_MONTH_DAY_FULL",
                        "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                        "DATE_FORMAT_ISO8601",
                      ]).describe(
                        "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                      ).optional(),
                      displayText: z.string().describe(
                        "Output only. Indicates how the DateElement is displayed in the document.",
                      ).optional(),
                      locale: z.string().describe(
                        "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                      ).optional(),
                      timeFormat: z.enum([
                        "TIME_FORMAT_UNSPECIFIED",
                        "TIME_FORMAT_DISABLED",
                        "TIME_FORMAT_HOUR_MINUTE",
                        "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                      ]).describe(
                        "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                      ).optional(),
                      timeZoneId: z.string().describe(
                        "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                      ).optional(),
                      timestamp: z.string().describe(
                        "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                      ).optional(),
                    }).describe("Properties of a DateElement.").optional(),
                    dateElementPropertiesSuggestionState: z.object({
                      dateFormatSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to date_format.",
                      ).optional(),
                      localeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to locale.",
                      ).optional(),
                      timeFormatSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to time_format.",
                      ).optional(),
                      timeZoneIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to time_zone_id.",
                      ).optional(),
                      timestampSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to timestamp.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested changes to the date element properties, keyed by suggestion ID.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A date instance mentioned in a document.")
                .optional(),
              endIndex: z.number().int().describe(
                "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
              ).optional(),
              equation: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
              }).describe("A ParagraphElement representing an equation.")
                .optional(),
              footnoteReference: z.object({
                footnoteId: z.string().describe(
                  "The ID of the footnote that contains the content of this footnote reference.",
                ).optional(),
                footnoteNumber: z.string().describe(
                  "The rendered number of this footnote.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
              ).optional(),
              horizontalRule: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A ParagraphElement representing a horizontal line.")
                .optional(),
              inlineObjectElement: z.object({
                inlineObjectId: z.string().describe(
                  "The ID of the InlineObject this element contains.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A ParagraphElement that contains an InlineObject.")
                .optional(),
              pageBreak: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
              ).optional(),
              person: z.object({
                personId: z.string().describe(
                  "Output only. The unique ID of this link.",
                ).optional(),
                personProperties: z.object({
                  email: z.string().describe(
                    "The email address linked to this Person. This field is always present.",
                  ).optional(),
                  name: z.string().describe(
                    "The name of the person if it's displayed in the link text instead of the person's email address.",
                  ).optional(),
                }).describe("Properties specific to a linked Person.")
                  .optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this Person, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
              ).optional(),
              richLink: z.object({
                richLinkId: z.string().describe(
                  "Output only. The ID of this link.",
                ).optional(),
                richLinkProperties: z.object({
                  mimeType: z.string().describe(
                    "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                  ).optional(),
                  title: z.string().describe(
                    "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                  ).optional(),
                  uri: z.string().describe(
                    "The URI to the RichLink. This is always present.",
                  ).optional(),
                }).describe("Properties specific to a RichLink.").optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this paragraph element, in UTF-16 code units.",
              ).optional(),
              textRun: z.object({
                content: z.string().describe(
                  "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this run, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement that represents a run of text that all has the same styling.",
              ).optional(),
            })).describe(
              "The content of the paragraph, broken down into its component parts.",
            ).optional(),
            paragraphStyle: z.object({
              alignment: z.enum([
                "ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
                "JUSTIFIED",
              ]).describe("The text alignment for this paragraph.").optional(),
              avoidWidowAndOrphan: z.boolean().describe(
                "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
              ).optional(),
              borderBetween: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              direction: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
              ).optional(),
              headingId: z.string().describe(
                "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
              ).optional(),
              indentEnd: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              keepLinesTogether: z.boolean().describe(
                "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              keepWithNext: z.boolean().describe(
                "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              lineSpacing: z.number().describe(
                "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
              ).optional(),
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
              ).optional(),
              pageBreakBefore: z.boolean().describe(
                "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
              ).optional(),
              shading: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
              }).describe("The shading of a paragraph.").optional(),
              spaceAbove: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spaceBelow: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spacingMode: z.enum([
                "SPACING_MODE_UNSPECIFIED",
                "NEVER_COLLAPSE",
                "COLLAPSE_LISTS",
              ]).describe("The spacing mode for the paragraph.").optional(),
              tabStops: z.array(z.object({
                alignment: z.enum([
                  "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                ]).describe(
                  "The alignment of this tab stop. If unset, the value defaults to START.",
                ).optional(),
                offset: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
              ).optional(),
            }).describe(
              "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            positionedObjectIds: z.array(z.string()).describe(
              "The IDs of the positioned objects tethered to this paragraph.",
            ).optional(),
            suggestedBulletChanges: z.record(
              z.string(),
              z.object({
                bullet: z.object({
                  listId: z.string().describe(
                    "The ID of the list this paragraph belongs to.",
                  ).optional(),
                  nestingLevel: z.number().int().describe(
                    "The nesting level of this paragraph in the list.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("Describes the bullet of a paragraph.").optional(),
                bulletSuggestionState: z.object({
                  listIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the list_id.",
                  ).optional(),
                  nestingLevelSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the nesting_level.",
                  ).optional(),
                  textStyleSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to background_color.",
                    ).optional(),
                    baselineOffsetSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to baseline_offset.",
                    ).optional(),
                    boldSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to bold.",
                    ).optional(),
                    fontSizeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to font_size.",
                    ).optional(),
                    foregroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to foreground_color.",
                    ).optional(),
                    italicSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to italic.",
                    ).optional(),
                    linkSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to link.",
                    ).optional(),
                    smallCapsSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to small_caps.",
                    ).optional(),
                    strikethroughSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to strikethrough.",
                    ).optional(),
                    underlineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to underline.",
                    ).optional(),
                    weightedFontFamilySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to weighted_font_family.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe("The suggested changes to this paragraph's bullet.")
              .optional(),
            suggestedParagraphStyleChanges: z.record(
              z.string(),
              z.object({
                paragraphStyle: z.object({
                  alignment: z.enum([
                    "ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                    "JUSTIFIED",
                  ]).describe("The text alignment for this paragraph.")
                    .optional(),
                  avoidWidowAndOrphan: z.boolean().describe(
                    "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                  ).optional(),
                  borderBetween: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  direction: z.enum([
                    "CONTENT_DIRECTION_UNSPECIFIED",
                    "LEFT_TO_RIGHT",
                    "RIGHT_TO_LEFT",
                  ]).describe(
                    "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                  ).optional(),
                  headingId: z.string().describe(
                    "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                  ).optional(),
                  indentEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentFirstLine: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentStart: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  keepLinesTogether: z.boolean().describe(
                    "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                  ).optional(),
                  keepWithNext: z.boolean().describe(
                    "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                  ).optional(),
                  lineSpacing: z.number().describe(
                    "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                  ).optional(),
                  namedStyleType: z.enum([
                    "NAMED_STYLE_TYPE_UNSPECIFIED",
                    "NORMAL_TEXT",
                    "TITLE",
                    "SUBTITLE",
                    "HEADING_1",
                    "HEADING_2",
                    "HEADING_3",
                    "HEADING_4",
                    "HEADING_5",
                    "HEADING_6",
                  ]).describe(
                    "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                  ).optional(),
                  pageBreakBefore: z.boolean().describe(
                    "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                  ).optional(),
                  shading: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                  }).describe("The shading of a paragraph.").optional(),
                  spaceAbove: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  spaceBelow: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  spacingMode: z.enum([
                    "SPACING_MODE_UNSPECIFIED",
                    "NEVER_COLLAPSE",
                    "COLLAPSE_LISTS",
                  ]).describe("The spacing mode for the paragraph.").optional(),
                  tabStops: z.array(z.object({
                    alignment: z.enum([
                      "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                    ]).describe(
                      "The alignment of this tab stop. If unset, the value defaults to START.",
                    ).optional(),
                    offset: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  })).describe(
                    "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                  ).optional(),
                }).describe(
                  "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                paragraphStyleSuggestionState: z.object({
                  alignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to alignment.",
                  ).optional(),
                  avoidWidowAndOrphanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                  ).optional(),
                  borderBetweenSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_between.",
                  ).optional(),
                  borderBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_bottom.",
                  ).optional(),
                  borderLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_left.",
                  ).optional(),
                  borderRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_right.",
                  ).optional(),
                  borderTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_top.",
                  ).optional(),
                  directionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to direction.",
                  ).optional(),
                  headingIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to heading_id.",
                  ).optional(),
                  indentEndSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_end.",
                  ).optional(),
                  indentFirstLineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_first_line.",
                  ).optional(),
                  indentStartSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_start.",
                  ).optional(),
                  keepLinesTogetherSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to keep_lines_together.",
                  ).optional(),
                  keepWithNextSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to keep_with_next.",
                  ).optional(),
                  lineSpacingSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to line_spacing.",
                  ).optional(),
                  namedStyleTypeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to named_style_type.",
                  ).optional(),
                  pageBreakBeforeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to page_break_before.",
                  ).optional(),
                  shadingSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the Shading.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  spaceAboveSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to space_above.",
                  ).optional(),
                  spaceBelowSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to space_below.",
                  ).optional(),
                  spacingModeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to spacing_mode.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
            ).optional(),
            suggestedPositionedObjectIds: z.record(
              z.string(),
              z.object({
                objectIds: z.array(z.string()).describe("The object IDs.")
                  .optional(),
              }),
            ).describe(
              "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
            ).optional(),
          }).describe(
            "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
          ).optional(),
          sectionBreak: z.object({
            sectionStyle: z.object({
              columnProperties: z.array(z.object({
                paddingEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
              ).optional(),
              columnSeparatorStyle: z.enum([
                "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                "NONE",
                "BETWEEN_EACH_COLUMN",
              ]).describe(
                "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              contentDirection: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              defaultFooterId: z.string().describe(
                "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              defaultHeaderId: z.string().describe(
                "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              evenPageFooterId: z.string().describe(
                "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              evenPageHeaderId: z.string().describe(
                "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              firstPageFooterId: z.string().describe(
                "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              firstPageHeaderId: z.string().describe(
                "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              flipPageOrientation: z.boolean().describe(
                "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginFooter: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginHeader: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              pageNumberStart: z.number().int().describe(
                "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              sectionType: z.enum([
                "SECTION_TYPE_UNSPECIFIED",
                "CONTINUOUS",
                "NEXT_PAGE",
              ]).describe("Output only. The type of section.").optional(),
              useFirstPageHeaderFooter: z.boolean().describe(
                "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
            }).describe("The styling that applies to a section.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe(
            "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this structural element, in UTF-16 code units.",
          ).optional(),
          table: z.object({
            columns: z.number().int().describe(
              "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
            ).optional(),
            rows: z.number().int().describe("Number of rows in the table.")
              .optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            tableRows: z.array(z.object({
              endIndex: z.number().int().describe(
                "The zero-based end index of this row, exclusive, in UTF-16 code units.",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this row, in UTF-16 code units.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              suggestedTableRowStyleChanges: z.record(
                z.string(),
                z.object({
                  tableRowStyle: z.object({
                    minRowHeight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    preventOverflow: z.boolean().describe(
                      "Whether the row cannot overflow across page or column boundaries.",
                    ).optional(),
                    tableHeader: z.boolean().describe(
                      "Whether the row is a table header.",
                    ).optional(),
                  }).describe("Styles that apply to a table row.").optional(),
                  tableRowStyleSuggestionState: z.object({
                    minRowHeightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to min_row_height.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested style changes to this row, keyed by suggestion ID.",
              ).optional(),
              tableCells: z.array(z.object({
                content: z.array(z.string()).describe(
                  "The content of the cell.",
                ).optional(),
                endIndex: z.number().int().describe(
                  "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this cell, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableCellStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableCellStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      borderBottom: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderLeft: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderRight: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderTop: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      columnSpan: z.number().int().describe(
                        "The column span of the cell. This property is read-only.",
                      ).optional(),
                      contentAlignment: z.enum([
                        "CONTENT_ALIGNMENT_UNSPECIFIED",
                        "CONTENT_ALIGNMENT_UNSUPPORTED",
                        "TOP",
                        "MIDDLE",
                        "BOTTOM",
                      ]).describe(
                        "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                      ).optional(),
                      paddingBottom: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingLeft: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingRight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingTop: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      rowSpan: z.number().int().describe(
                        "The row span of the cell. This property is read-only.",
                      ).optional(),
                    }).describe(
                      "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                    ).optional(),
                    tableCellStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      borderBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_bottom.",
                      ).optional(),
                      borderLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_left.",
                      ).optional(),
                      borderRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_right.",
                      ).optional(),
                      borderTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_top.",
                      ).optional(),
                      columnSpanSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to column_span.",
                      ).optional(),
                      contentAlignmentSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to content_alignment.",
                      ).optional(),
                      paddingBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_bottom.",
                      ).optional(),
                      paddingLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_left.",
                      ).optional(),
                      paddingRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_right.",
                      ).optional(),
                      paddingTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_top.",
                      ).optional(),
                      rowSpanSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to row_span.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested changes to the table cell style, keyed by suggestion ID.",
                ).optional(),
                tableCellStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  columnSpan: z.number().int().describe(
                    "The column span of the cell. This property is read-only.",
                  ).optional(),
                  contentAlignment: z.enum([
                    "CONTENT_ALIGNMENT_UNSPECIFIED",
                    "CONTENT_ALIGNMENT_UNSUPPORTED",
                    "TOP",
                    "MIDDLE",
                    "BOTTOM",
                  ]).describe(
                    "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                  ).optional(),
                  paddingBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  rowSpan: z.number().int().describe(
                    "The row span of the cell. This property is read-only.",
                  ).optional(),
                }).describe(
                  "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                ).optional(),
              })).describe(
                "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
              ).optional(),
              tableRowStyle: z.object({
                minRowHeight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                preventOverflow: z.boolean().describe(
                  "Whether the row cannot overflow across page or column boundaries.",
                ).optional(),
                tableHeader: z.boolean().describe(
                  "Whether the row is a table header.",
                ).optional(),
              }).describe("Styles that apply to a table row.").optional(),
            })).describe("The contents and style of each row.").optional(),
            tableStyle: z.object({
              tableColumnProperties: z.array(z.object({
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                widthType: z.enum([
                  "WIDTH_TYPE_UNSPECIFIED",
                  "EVENLY_DISTRIBUTED",
                  "FIXED_WIDTH",
                ]).describe("The width type of the column.").optional(),
              })).describe(
                "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
              ).optional(),
            }).describe("Styles that apply to a table.").optional(),
          }).describe("A StructuralElement representing a table.").optional(),
          tableOfContents: z.object({
            content: z.array(z.string()).describe(
              "The content of the table of contents.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe("A StructuralElement representing a table of contents.")
            .optional(),
        })).describe(
          "The contents of the body. The indexes for the body's content begin at zero.",
        ).optional(),
      }).describe(
        "The document body. The body typically contains the full document contents except for headers, footers, and footnotes.",
      ).optional(),
      documentStyle: z.object({
        background: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
        }).describe("Represents the background of a document.").optional(),
        defaultFooterId: z.string().describe(
          "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        defaultHeaderId: z.string().describe(
          "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        documentFormat: z.object({
          documentMode: z.enum([
            "DOCUMENT_MODE_UNSPECIFIED",
            "PAGES",
            "PAGELESS",
          ]).describe("Whether the document has pages or is pageless.")
            .optional(),
        }).describe("Represents document-level format settings.").optional(),
        evenPageFooterId: z.string().describe(
          "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        evenPageHeaderId: z.string().describe(
          "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        firstPageFooterId: z.string().describe(
          "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        firstPageHeaderId: z.string().describe(
          "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        flipPageOrientation: z.boolean().describe(
          "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        marginBottom: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginFooter: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginHeader: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginLeft: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginRight: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginTop: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        pageNumberStart: z.number().int().describe(
          "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        pageSize: z.object({
          height: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A width and height.").optional(),
        useCustomHeaderFooterMargins: z.boolean().describe(
          "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        useEvenPageHeaderFooter: z.boolean().describe(
          "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        useFirstPageHeaderFooter: z.boolean().describe(
          "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
      }).describe("The style of the document.").optional(),
      footers: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the footer. The indexes for a footer's content begin at zero.",
          ).optional(),
          footerId: z.string().describe("The ID of the footer.").optional(),
        }),
      ).describe("The footers in the document tab, keyed by footer ID.")
        .optional(),
      footnotes: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the footnote. The indexes for a footnote's content begin at zero.",
          ).optional(),
          footnoteId: z.string().describe("The ID of the footnote.").optional(),
        }),
      ).describe("The footnotes in the document tab, keyed by footnote ID.")
        .optional(),
      headers: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the header. The indexes for a header's content begin at zero.",
          ).optional(),
          headerId: z.string().describe("The ID of the header.").optional(),
        }),
      ).describe("The headers in the document tab, keyed by header ID.")
        .optional(),
      inlineObjects: z.record(
        z.string(),
        z.object({
          inlineObjectProperties: z.object({
            embeddedObject: z.object({
              description: z.string().describe(
                "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
              embeddedDrawingProperties: z.object({}).describe(
                "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
              ).optional(),
              embeddedObjectBorder: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                propertyState: z.enum(["RENDERED", "NOT_RENDERED"]).describe(
                  "The property state of the border property.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around an EmbeddedObject.").optional(),
              imageProperties: z.object({
                angle: z.number().describe(
                  "The clockwise rotation angle of the image, in radians.",
                ).optional(),
                brightness: z.number().describe(
                  "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                contentUri: z.string().describe(
                  "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                ).optional(),
                contrast: z.number().describe(
                  "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                cropProperties: z.object({
                  angle: z.number().describe(
                    "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                  ).optional(),
                  offsetBottom: z.number().describe(
                    "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                  offsetLeft: z.number().describe(
                    "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetRight: z.number().describe(
                    "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetTop: z.number().describe(
                    "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                }).describe(
                  "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                ).optional(),
                sourceUri: z.string().describe(
                  "The source URI is the URI used to insert the image. The source URI can be empty.",
                ).optional(),
                transparency: z.number().describe(
                  "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                ).optional(),
              }).describe("The properties of an image.").optional(),
              linkedContentReference: z.object({
                sheetsChartReference: z.object({
                  chartId: z.number().int().describe(
                    "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                  ).optional(),
                  spreadsheetId: z.string().describe(
                    "The ID of the Google Sheets spreadsheet that contains the source chart.",
                  ).optional(),
                }).describe(
                  "A reference to a linked chart embedded from Google Sheets.",
                ).optional(),
              }).describe("A reference to the external linked source content.")
                .optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              size: z.object({
                height: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A width and height.").optional(),
              title: z.string().describe(
                "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
            }).describe("An embedded object in the document.").optional(),
          }).describe("Properties of an InlineObject.").optional(),
          objectId: z.string().describe(
            "The ID of this inline object. Can be used to update an object’s properties.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInlineObjectPropertiesChanges: z.record(
            z.string(),
            z.object({
              inlineObjectProperties: z.object({
                embeddedObject: z.object({
                  description: z.string().describe(
                    "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                  embeddedDrawingProperties: z.object({}).describe(
                    "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
                  ).optional(),
                  embeddedObjectBorder: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    propertyState: z.enum(["RENDERED", "NOT_RENDERED"])
                      .describe("The property state of the border property.")
                      .optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around an EmbeddedObject.").optional(),
                  imageProperties: z.object({
                    angle: z.number().describe(
                      "The clockwise rotation angle of the image, in radians.",
                    ).optional(),
                    brightness: z.number().describe(
                      "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    contentUri: z.string().describe(
                      "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                    ).optional(),
                    contrast: z.number().describe(
                      "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    cropProperties: z.object({
                      angle: z.number().describe(
                        "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                      ).optional(),
                      offsetBottom: z.number().describe(
                        "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                      offsetLeft: z.number().describe(
                        "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetRight: z.number().describe(
                        "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetTop: z.number().describe(
                        "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                    }).describe(
                      "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                    ).optional(),
                    sourceUri: z.string().describe(
                      "The source URI is the URI used to insert the image. The source URI can be empty.",
                    ).optional(),
                    transparency: z.number().describe(
                      "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                    ).optional(),
                  }).describe("The properties of an image.").optional(),
                  linkedContentReference: z.object({
                    sheetsChartReference: z.object({
                      chartId: z.number().int().describe(
                        "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                      ).optional(),
                      spreadsheetId: z.string().describe(
                        "The ID of the Google Sheets spreadsheet that contains the source chart.",
                      ).optional(),
                    }).describe(
                      "A reference to a linked chart embedded from Google Sheets.",
                    ).optional(),
                  }).describe(
                    "A reference to the external linked source content.",
                  ).optional(),
                  marginBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  size: z.object({
                    height: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A width and height.").optional(),
                  title: z.string().describe(
                    "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                }).describe("An embedded object in the document.").optional(),
              }).describe("Properties of an InlineObject.").optional(),
              inlineObjectPropertiesSuggestionState: z.object({
                embeddedObjectSuggestionState: z.object({
                  descriptionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to description.",
                  ).optional(),
                  embeddedDrawingPropertiesSuggestionState: z.object({})
                    .describe(
                      "A mask that indicates which of the fields on the base EmbeddedDrawingProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  embeddedObjectBorderSuggestionState: z.object({
                    colorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to color.",
                    ).optional(),
                    dashStyleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to dash_style.",
                    ).optional(),
                    propertyStateSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to property_state.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base EmbeddedObjectBorder have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  imagePropertiesSuggestionState: z.object({
                    angleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to angle.",
                    ).optional(),
                    brightnessSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to brightness.",
                    ).optional(),
                    contentUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to content_uri.",
                    ).optional(),
                    contrastSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to contrast.",
                    ).optional(),
                    cropPropertiesSuggestionState: z.object({
                      angleSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to angle.",
                      ).optional(),
                      offsetBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_bottom.",
                      ).optional(),
                      offsetLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_left.",
                      ).optional(),
                      offsetRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_right.",
                      ).optional(),
                      offsetTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_top.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base CropProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    sourceUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to source_uri.",
                    ).optional(),
                    transparencySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to transparency.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ImageProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  linkedContentReferenceSuggestionState: z.object({
                    sheetsChartReferenceSuggestionState: z.object({
                      chartIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to chart_id.",
                      ).optional(),
                      spreadsheetIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to spreadsheet_id.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base SheetsChartReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base LinkedContentReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  marginBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_bottom.",
                  ).optional(),
                  marginLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_left.",
                  ).optional(),
                  marginRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_right.",
                  ).optional(),
                  marginTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_top.",
                  ).optional(),
                  sizeSuggestionState: z.object({
                    heightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to height.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
                  ).optional(),
                  titleSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to title.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base EmbeddedObject have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base InlineObjectProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the inline object properties, keyed by suggestion ID.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
        }),
      ).describe("The inline objects in the document tab, keyed by object ID.")
        .optional(),
      lists: z.record(
        z.string(),
        z.object({
          listProperties: z.object({
            nestingLevels: z.array(z.object({
              bulletAlignment: z.enum([
                "BULLET_ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
              ]).describe(
                "The alignment of the bullet within the space allotted for rendering the bullet.",
              ).optional(),
              glyphFormat: z.string().describe(
                "The format string used by bullets at this level of nesting. The glyph format contains one or more placeholders, and these placeholders are replaced with the appropriate values depending on the glyph_type or glyph_symbol. The placeholders follow the pattern `%[nesting_level]`. Furthermore, placeholders can have prefixes and suffixes. Thus, the glyph format follows the pattern `%[nesting_level]`. Note that the prefix and suffix are optional and can be arbitrary strings. For example, the glyph format `%0.` indicates that the rendered glyph will replace the placeholder with the corresponding glyph for nesting level 0 followed by a period as the suffix. So a list with a glyph type of UPPER_ALPHA and glyph format `%0.` at nesting level 0 will result in a list with rendered glyphs `A.` `B.` `C.` The glyph format can contain placeholders for the current nesting level as well as placeholders for parent nesting levels. For example, a list can have a glyph format of `%0.` at nesting level 0 and a glyph format of `%0.%1.` at nesting level 1. Assuming both nesting levels have DECIMAL glyph types, this would result in a list with rendered glyphs `1.` `2.` ` 2.1.` ` 2.2.` `3.` For nesting levels that are ordered, the string that replaces a placeholder in the glyph format for a particular paragraph depends on the paragraph's order within the list.",
              ).optional(),
              glyphSymbol: z.string().describe(
                "A custom glyph symbol used by bullets when paragraphs at this level of nesting is unordered. The glyph symbol replaces placeholders within the glyph_format. For example, if the glyph_symbol is the solid circle corresponding to Unicode U+25cf code point and the glyph_format is `%0`, the rendered glyph would be the solid circle.",
              ).optional(),
              glyphType: z.enum([
                "GLYPH_TYPE_UNSPECIFIED",
                "NONE",
                "DECIMAL",
                "ZERO_DECIMAL",
                "UPPER_ALPHA",
                "ALPHA",
                "UPPER_ROMAN",
                "ROMAN",
              ]).describe(
                "The type of glyph used by bullets when paragraphs at this level of nesting is ordered. The glyph type determines the type of glyph used to replace placeholders within the glyph_format when paragraphs at this level of nesting are ordered. For example, if the nesting level is 0, the glyph_format is `%0.` and the glyph type is DECIMAL, then the rendered glyph would replace the placeholder `%0` in the glyph format with a number corresponding to the list item's order within the list.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              startNumber: z.number().int().describe(
                "The number of the first list item at this nesting level. A value of 0 is treated as a value of 1 for lettered lists and Roman numeral lists. For values of both 0 and 1, lettered and Roman numeral lists will begin at `a` and `i` respectively. This value is ignored for nesting levels with unordered glyphs.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            })).describe(
              "Describes the properties of the bullets at the associated level. A list has at most 9 levels of nesting with nesting level 0 corresponding to the top-most level and nesting level 8 corresponding to the most nested level. The nesting levels are returned in ascending order with the least nested returned first.",
            ).optional(),
          }).describe(
            "The properties of a list that describe the look and feel of bullets belonging to paragraphs associated with a list.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this list.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedListPropertiesChanges: z.record(
            z.string(),
            z.object({
              listProperties: z.object({
                nestingLevels: z.array(z.object({
                  bulletAlignment: z.enum([
                    "BULLET_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of the bullet within the space allotted for rendering the bullet.",
                  ).optional(),
                  glyphFormat: z.string().describe(
                    "The format string used by bullets at this level of nesting. The glyph format contains one or more placeholders, and these placeholders are replaced with the appropriate values depending on the glyph_type or glyph_symbol. The placeholders follow the pattern `%[nesting_level]`. Furthermore, placeholders can have prefixes and suffixes. Thus, the glyph format follows the pattern `%[nesting_level]`. Note that the prefix and suffix are optional and can be arbitrary strings. For example, the glyph format `%0.` indicates that the rendered glyph will replace the placeholder with the corresponding glyph for nesting level 0 followed by a period as the suffix. So a list with a glyph type of UPPER_ALPHA and glyph format `%0.` at nesting level 0 will result in a list with rendered glyphs `A.` `B.` `C.` The glyph format can contain placeholders for the current nesting level as well as placeholders for parent nesting levels. For example, a list can have a glyph format of `%0.` at nesting level 0 and a glyph format of `%0.%1.` at nesting level 1. Assuming both nesting levels have DECIMAL glyph types, this would result in a list with rendered glyphs `1.` `2.` ` 2.1.` ` 2.2.` `3.` For nesting levels that are ordered, the string that replaces a placeholder in the glyph format for a particular paragraph depends on the paragraph's order within the list.",
                  ).optional(),
                  glyphSymbol: z.string().describe(
                    "A custom glyph symbol used by bullets when paragraphs at this level of nesting is unordered. The glyph symbol replaces placeholders within the glyph_format. For example, if the glyph_symbol is the solid circle corresponding to Unicode U+25cf code point and the glyph_format is `%0`, the rendered glyph would be the solid circle.",
                  ).optional(),
                  glyphType: z.enum([
                    "GLYPH_TYPE_UNSPECIFIED",
                    "NONE",
                    "DECIMAL",
                    "ZERO_DECIMAL",
                    "UPPER_ALPHA",
                    "ALPHA",
                    "UPPER_ROMAN",
                    "ROMAN",
                  ]).describe(
                    "The type of glyph used by bullets when paragraphs at this level of nesting is ordered. The glyph type determines the type of glyph used to replace placeholders within the glyph_format when paragraphs at this level of nesting are ordered. For example, if the nesting level is 0, the glyph_format is `%0.` and the glyph type is DECIMAL, then the rendered glyph would replace the placeholder `%0` in the glyph format with a number corresponding to the list item's order within the list.",
                  ).optional(),
                  indentFirstLine: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentStart: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  startNumber: z.number().int().describe(
                    "The number of the first list item at this nesting level. A value of 0 is treated as a value of 1 for lettered lists and Roman numeral lists. For values of both 0 and 1, lettered and Roman numeral lists will begin at `a` and `i` respectively. This value is ignored for nesting levels with unordered glyphs.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                })).describe(
                  "Describes the properties of the bullets at the associated level. A list has at most 9 levels of nesting with nesting level 0 corresponding to the top-most level and nesting level 8 corresponding to the most nested level. The nesting levels are returned in ascending order with the least nested returned first.",
                ).optional(),
              }).describe(
                "The properties of a list that describe the look and feel of bullets belonging to paragraphs associated with a list.",
              ).optional(),
              listPropertiesSuggestionState: z.object({
                nestingLevelsSuggestionStates: z.array(z.object({
                  bulletAlignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bullet_alignment.",
                  ).optional(),
                  glyphFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_format.",
                  ).optional(),
                  glyphSymbolSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_symbol.",
                  ).optional(),
                  glyphTypeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_type.",
                  ).optional(),
                  indentFirstLineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_first_line.",
                  ).optional(),
                  indentStartSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_start.",
                  ).optional(),
                  startNumberSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to start_number.",
                  ).optional(),
                  textStyleSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to background_color.",
                    ).optional(),
                    baselineOffsetSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to baseline_offset.",
                    ).optional(),
                    boldSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to bold.",
                    ).optional(),
                    fontSizeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to font_size.",
                    ).optional(),
                    foregroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to foreground_color.",
                    ).optional(),
                    italicSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to italic.",
                    ).optional(),
                    linkSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to link.",
                    ).optional(),
                    smallCapsSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to small_caps.",
                    ).optional(),
                    strikethroughSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to strikethrough.",
                    ).optional(),
                    underlineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to underline.",
                    ).optional(),
                    weightedFontFamilySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to weighted_font_family.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                })).describe(
                  "A mask that indicates which of the fields on the corresponding NestingLevel in nesting_levels have been changed in this suggestion. The nesting level suggestion states are returned in ascending order of the nesting level with the least nested returned first.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base ListProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the list properties, keyed by suggestion ID.",
          ).optional(),
        }),
      ).describe("The lists in the document tab, keyed by list ID.").optional(),
      namedRanges: z.record(
        z.string(),
        z.object({
          name: z.string().describe("The name that all the named ranges share.")
            .optional(),
          namedRanges: z.array(z.object({
            name: z.string().describe("The name of the named range.")
              .optional(),
            namedRangeId: z.string().describe("The ID of the named range.")
              .optional(),
            ranges: z.array(z.object({
              endIndex: z.number().int().describe(
                "The zero-based end index of this range, exclusive, in UTF-16 code units. In all current uses, an end index must be provided. This field is an Int32Value in order to accommodate future use cases with open-ended ranges.",
              ).optional(),
              segmentId: z.string().describe(
                "The ID of the header, footer, or footnote that this range is contained in. An empty segment ID signifies the document's body.",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this range, in UTF-16 code units. In all current uses, a start index must be provided. This field is an Int32Value in order to accommodate future use cases with open-ended ranges.",
              ).optional(),
              tabId: z.string().describe(
                "The tab that contains this range. When omitted, the request applies to the first tab. In a document containing a single tab: - If provided, must match the singular tab's ID. - If omitted, the request applies to the singular tab. In a document containing multiple tabs: - If provided, the request applies to the specified tab. - If omitted, the request applies to the first tab in the document.",
              ).optional(),
            })).describe("The ranges that belong to this named range.")
              .optional(),
          })).describe("The NamedRanges that share the same name.").optional(),
        }),
      ).describe("The named ranges in the document tab, keyed by name.")
        .optional(),
      namedStyles: z.object({
        styles: z.array(z.object({
          namedStyleType: z.enum([
            "NAMED_STYLE_TYPE_UNSPECIFIED",
            "NORMAL_TEXT",
            "TITLE",
            "SUBTITLE",
            "HEADING_1",
            "HEADING_2",
            "HEADING_3",
            "HEADING_4",
            "HEADING_5",
            "HEADING_6",
          ]).describe("The type of this named style.").optional(),
          paragraphStyle: z.object({
            alignment: z.enum([
              "ALIGNMENT_UNSPECIFIED",
              "START",
              "CENTER",
              "END",
              "JUSTIFIED",
            ]).describe("The text alignment for this paragraph.").optional(),
            avoidWidowAndOrphan: z.boolean().describe(
              "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
            ).optional(),
            borderBetween: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderBottom: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderLeft: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderRight: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderTop: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            direction: z.enum([
              "CONTENT_DIRECTION_UNSPECIFIED",
              "LEFT_TO_RIGHT",
              "RIGHT_TO_LEFT",
            ]).describe(
              "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
            ).optional(),
            headingId: z.string().describe(
              "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
            ).optional(),
            indentEnd: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            indentFirstLine: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            indentStart: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            keepLinesTogether: z.boolean().describe(
              "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
            ).optional(),
            keepWithNext: z.boolean().describe(
              "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
            ).optional(),
            lineSpacing: z.number().describe(
              "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
            ).optional(),
            namedStyleType: z.enum([
              "NAMED_STYLE_TYPE_UNSPECIFIED",
              "NORMAL_TEXT",
              "TITLE",
              "SUBTITLE",
              "HEADING_1",
              "HEADING_2",
              "HEADING_3",
              "HEADING_4",
              "HEADING_5",
              "HEADING_6",
            ]).describe(
              "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
            ).optional(),
            pageBreakBefore: z.boolean().describe(
              "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
            ).optional(),
            shading: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
            }).describe("The shading of a paragraph.").optional(),
            spaceAbove: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            spaceBelow: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            spacingMode: z.enum([
              "SPACING_MODE_UNSPECIFIED",
              "NEVER_COLLAPSE",
              "COLLAPSE_LISTS",
            ]).describe("The spacing mode for the paragraph.").optional(),
            tabStops: z.array(z.object({
              alignment: z.enum([
                "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
              ]).describe(
                "The alignment of this tab stop. If unset, the value defaults to START.",
              ).optional(),
              offset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            })).describe(
              "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
            ).optional(),
          }).describe(
            "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            baselineOffset: z.enum([
              "BASELINE_OFFSET_UNSPECIFIED",
              "NONE",
              "SUPERSCRIPT",
              "SUBSCRIPT",
            ]).describe(
              "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
            ).optional(),
            bold: z.boolean().describe(
              "Whether or not the text is rendered as bold.",
            ).optional(),
            fontSize: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            italic: z.boolean().describe(
              "Whether or not the text is italicized.",
            ).optional(),
            link: z.object({
              bookmark: z.object({
                id: z.string().describe(
                  "The ID of a bookmark in this document.",
                ).optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this bookmark.",
                ).optional(),
              }).describe("A reference to a bookmark in this document.")
                .optional(),
              bookmarkId: z.string().describe(
                "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              heading: z.object({
                id: z.string().describe("The ID of a heading in this document.")
                  .optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this heading.",
                ).optional(),
              }).describe("A reference to a heading in this document.")
                .optional(),
              headingId: z.string().describe(
                "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              tabId: z.string().describe("The ID of a tab in this document.")
                .optional(),
              url: z.string().describe("An external URL.").optional(),
            }).describe(
              "A reference to another portion of a document or an external URL resource.",
            ).optional(),
            smallCaps: z.boolean().describe(
              "Whether or not the text is in small capital letters.",
            ).optional(),
            strikethrough: z.boolean().describe(
              "Whether or not the text is struck through.",
            ).optional(),
            underline: z.boolean().describe(
              "Whether or not the text is underlined.",
            ).optional(),
            weightedFontFamily: z.object({
              fontFamily: z.string().describe(
                "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
              ).optional(),
              weight: z.number().int().describe(
                "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
              ).optional(),
            }).describe("Represents a font family and weight of text.")
              .optional(),
          }).describe(
            "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
        })).describe(
          "The named styles. There's an entry for each of the possible named style types.",
        ).optional(),
      }).describe(
        "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
      ).optional(),
      positionedObjects: z.record(
        z.string(),
        z.object({
          objectId: z.string().describe("The ID of this positioned object.")
            .optional(),
          positionedObjectProperties: z.object({
            embeddedObject: z.object({
              description: z.string().describe(
                "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
              embeddedDrawingProperties: z.object({}).describe(
                "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
              ).optional(),
              embeddedObjectBorder: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                propertyState: z.enum(["RENDERED", "NOT_RENDERED"]).describe(
                  "The property state of the border property.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around an EmbeddedObject.").optional(),
              imageProperties: z.object({
                angle: z.number().describe(
                  "The clockwise rotation angle of the image, in radians.",
                ).optional(),
                brightness: z.number().describe(
                  "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                contentUri: z.string().describe(
                  "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                ).optional(),
                contrast: z.number().describe(
                  "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                cropProperties: z.object({
                  angle: z.number().describe(
                    "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                  ).optional(),
                  offsetBottom: z.number().describe(
                    "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                  offsetLeft: z.number().describe(
                    "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetRight: z.number().describe(
                    "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetTop: z.number().describe(
                    "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                }).describe(
                  "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                ).optional(),
                sourceUri: z.string().describe(
                  "The source URI is the URI used to insert the image. The source URI can be empty.",
                ).optional(),
                transparency: z.number().describe(
                  "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                ).optional(),
              }).describe("The properties of an image.").optional(),
              linkedContentReference: z.object({
                sheetsChartReference: z.object({
                  chartId: z.number().int().describe(
                    "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                  ).optional(),
                  spreadsheetId: z.string().describe(
                    "The ID of the Google Sheets spreadsheet that contains the source chart.",
                  ).optional(),
                }).describe(
                  "A reference to a linked chart embedded from Google Sheets.",
                ).optional(),
              }).describe("A reference to the external linked source content.")
                .optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              size: z.object({
                height: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A width and height.").optional(),
              title: z.string().describe(
                "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
            }).describe("An embedded object in the document.").optional(),
            positioning: z.object({
              layout: z.enum([
                "POSITIONED_OBJECT_LAYOUT_UNSPECIFIED",
                "WRAP_TEXT",
                "BREAK_LEFT",
                "BREAK_RIGHT",
                "BREAK_LEFT_RIGHT",
                "IN_FRONT_OF_TEXT",
                "BEHIND_TEXT",
              ]).describe("The layout of this positioned object.").optional(),
              leftOffset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              topOffset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe(
              "The positioning of a PositionedObject. The positioned object is positioned relative to the beginning of the Paragraph it's tethered to.",
            ).optional(),
          }).describe("Properties of a PositionedObject.").optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedPositionedObjectPropertiesChanges: z.record(
            z.string(),
            z.object({
              positionedObjectProperties: z.object({
                embeddedObject: z.object({
                  description: z.string().describe(
                    "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                  embeddedDrawingProperties: z.object({}).describe(
                    "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
                  ).optional(),
                  embeddedObjectBorder: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    propertyState: z.enum(["RENDERED", "NOT_RENDERED"])
                      .describe("The property state of the border property.")
                      .optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around an EmbeddedObject.").optional(),
                  imageProperties: z.object({
                    angle: z.number().describe(
                      "The clockwise rotation angle of the image, in radians.",
                    ).optional(),
                    brightness: z.number().describe(
                      "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    contentUri: z.string().describe(
                      "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                    ).optional(),
                    contrast: z.number().describe(
                      "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    cropProperties: z.object({
                      angle: z.number().describe(
                        "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                      ).optional(),
                      offsetBottom: z.number().describe(
                        "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                      offsetLeft: z.number().describe(
                        "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetRight: z.number().describe(
                        "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetTop: z.number().describe(
                        "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                    }).describe(
                      "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                    ).optional(),
                    sourceUri: z.string().describe(
                      "The source URI is the URI used to insert the image. The source URI can be empty.",
                    ).optional(),
                    transparency: z.number().describe(
                      "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                    ).optional(),
                  }).describe("The properties of an image.").optional(),
                  linkedContentReference: z.object({
                    sheetsChartReference: z.object({
                      chartId: z.number().int().describe(
                        "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                      ).optional(),
                      spreadsheetId: z.string().describe(
                        "The ID of the Google Sheets spreadsheet that contains the source chart.",
                      ).optional(),
                    }).describe(
                      "A reference to a linked chart embedded from Google Sheets.",
                    ).optional(),
                  }).describe(
                    "A reference to the external linked source content.",
                  ).optional(),
                  marginBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  size: z.object({
                    height: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A width and height.").optional(),
                  title: z.string().describe(
                    "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                }).describe("An embedded object in the document.").optional(),
                positioning: z.object({
                  layout: z.enum([
                    "POSITIONED_OBJECT_LAYOUT_UNSPECIFIED",
                    "WRAP_TEXT",
                    "BREAK_LEFT",
                    "BREAK_RIGHT",
                    "BREAK_LEFT_RIGHT",
                    "IN_FRONT_OF_TEXT",
                    "BEHIND_TEXT",
                  ]).describe("The layout of this positioned object.")
                    .optional(),
                  leftOffset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  topOffset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe(
                  "The positioning of a PositionedObject. The positioned object is positioned relative to the beginning of the Paragraph it's tethered to.",
                ).optional(),
              }).describe("Properties of a PositionedObject.").optional(),
              positionedObjectPropertiesSuggestionState: z.object({
                embeddedObjectSuggestionState: z.object({
                  descriptionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to description.",
                  ).optional(),
                  embeddedDrawingPropertiesSuggestionState: z.object({})
                    .describe(
                      "A mask that indicates which of the fields on the base EmbeddedDrawingProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  embeddedObjectBorderSuggestionState: z.object({
                    colorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to color.",
                    ).optional(),
                    dashStyleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to dash_style.",
                    ).optional(),
                    propertyStateSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to property_state.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base EmbeddedObjectBorder have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  imagePropertiesSuggestionState: z.object({
                    angleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to angle.",
                    ).optional(),
                    brightnessSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to brightness.",
                    ).optional(),
                    contentUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to content_uri.",
                    ).optional(),
                    contrastSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to contrast.",
                    ).optional(),
                    cropPropertiesSuggestionState: z.object({
                      angleSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to angle.",
                      ).optional(),
                      offsetBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_bottom.",
                      ).optional(),
                      offsetLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_left.",
                      ).optional(),
                      offsetRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_right.",
                      ).optional(),
                      offsetTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_top.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base CropProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    sourceUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to source_uri.",
                    ).optional(),
                    transparencySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to transparency.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ImageProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  linkedContentReferenceSuggestionState: z.object({
                    sheetsChartReferenceSuggestionState: z.object({
                      chartIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to chart_id.",
                      ).optional(),
                      spreadsheetIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to spreadsheet_id.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base SheetsChartReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base LinkedContentReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  marginBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_bottom.",
                  ).optional(),
                  marginLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_left.",
                  ).optional(),
                  marginRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_right.",
                  ).optional(),
                  marginTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_top.",
                  ).optional(),
                  sizeSuggestionState: z.object({
                    heightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to height.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
                  ).optional(),
                  titleSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to title.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base EmbeddedObject have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
                positioningSuggestionState: z.object({
                  layoutSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to layout.",
                  ).optional(),
                  leftOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to left_offset.",
                  ).optional(),
                  topOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to top_offset.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base PositionedObjectPositioning have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base PositionedObjectProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the positioned object properties, keyed by suggestion ID.",
          ).optional(),
        }),
      ).describe(
        "The positioned objects in the document tab, keyed by object ID.",
      ).optional(),
      suggestedDocumentStyleChanges: z.record(
        z.string(),
        z.object({
          documentStyle: z.object({
            background: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
            }).describe("Represents the background of a document.").optional(),
            defaultFooterId: z.string().describe(
              "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            defaultHeaderId: z.string().describe(
              "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            documentFormat: z.object({
              documentMode: z.enum([
                "DOCUMENT_MODE_UNSPECIFIED",
                "PAGES",
                "PAGELESS",
              ]).describe("Whether the document has pages or is pageless.")
                .optional(),
            }).describe("Represents document-level format settings.")
              .optional(),
            evenPageFooterId: z.string().describe(
              "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            evenPageHeaderId: z.string().describe(
              "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            firstPageFooterId: z.string().describe(
              "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            firstPageHeaderId: z.string().describe(
              "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            flipPageOrientation: z.boolean().describe(
              "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            marginBottom: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginFooter: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginHeader: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginLeft: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginRight: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginTop: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            pageNumberStart: z.number().int().describe(
              "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            pageSize: z.object({
              height: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A width and height.").optional(),
            useCustomHeaderFooterMargins: z.boolean().describe(
              "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            useEvenPageHeaderFooter: z.boolean().describe(
              "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            useFirstPageHeaderFooter: z.boolean().describe(
              "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
          }).describe("The style of the document.").optional(),
          documentStyleSuggestionState: z.object({
            backgroundSuggestionState: z.object({
              backgroundColorSuggested: z.boolean().describe(
                "Indicates whether the current background color has been modified in this suggestion.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Background have been changed in this suggestion. For any field set to true, the Backgound has a new suggested value.",
            ).optional(),
            defaultFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to default_footer_id.",
            ).optional(),
            defaultHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to default_header_id.",
            ).optional(),
            evenPageFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to even_page_footer_id.",
            ).optional(),
            evenPageHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to even_page_header_id.",
            ).optional(),
            firstPageFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to first_page_footer_id.",
            ).optional(),
            firstPageHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to first_page_header_id.",
            ).optional(),
            flipPageOrientationSuggested: z.boolean().describe(
              "Optional. Indicates if there was a suggested change to flip_page_orientation.",
            ).optional(),
            marginBottomSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_bottom.",
            ).optional(),
            marginFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_footer.",
            ).optional(),
            marginHeaderSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_header.",
            ).optional(),
            marginLeftSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_left.",
            ).optional(),
            marginRightSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_right.",
            ).optional(),
            marginTopSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_top.",
            ).optional(),
            pageNumberStartSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to page_number_start.",
            ).optional(),
            pageSizeSuggestionState: z.object({
              heightSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to height.",
              ).optional(),
              widthSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to width.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
            ).optional(),
            useCustomHeaderFooterMarginsSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_custom_header_footer_margins.",
            ).optional(),
            useEvenPageHeaderFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_even_page_header_footer.",
            ).optional(),
            useFirstPageHeaderFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_first_page_header_footer.",
            ).optional(),
          }).describe(
            "A mask that indicates which of the fields on the base DocumentStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
          ).optional(),
        }),
      ).describe(
        "The suggested changes to the style of the document tab, keyed by suggestion ID.",
      ).optional(),
      suggestedNamedStylesChanges: z.record(
        z.string(),
        z.object({
          namedStyles: z.object({
            styles: z.array(z.object({
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe("The type of this named style.").optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            })).describe(
              "The named styles. There's an entry for each of the possible named style types.",
            ).optional(),
          }).describe(
            "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
          ).optional(),
          namedStylesSuggestionState: z.object({
            stylesSuggestionStates: z.array(z.object({
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type that this suggestion state corresponds to. This field is provided as a convenience for matching the NamedStyleSuggestionState with its corresponding NamedStyle.",
              ).optional(),
              paragraphStyleSuggestionState: z.object({
                alignmentSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to alignment.",
                ).optional(),
                avoidWidowAndOrphanSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                ).optional(),
                borderBetweenSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_between.",
                ).optional(),
                borderBottomSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_bottom.",
                ).optional(),
                borderLeftSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_left.",
                ).optional(),
                borderRightSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_right.",
                ).optional(),
                borderTopSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_top.",
                ).optional(),
                directionSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to direction.",
                ).optional(),
                headingIdSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to heading_id.",
                ).optional(),
                indentEndSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_end.",
                ).optional(),
                indentFirstLineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_first_line.",
                ).optional(),
                indentStartSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_start.",
                ).optional(),
                keepLinesTogetherSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to keep_lines_together.",
                ).optional(),
                keepWithNextSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to keep_with_next.",
                ).optional(),
                lineSpacingSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to line_spacing.",
                ).optional(),
                namedStyleTypeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to named_style_type.",
                ).optional(),
                pageBreakBeforeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to page_break_before.",
                ).optional(),
                shadingSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the Shading.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                ).optional(),
                spaceAboveSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to space_above.",
                ).optional(),
                spaceBelowSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to space_below.",
                ).optional(),
                spacingModeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to spacing_mode.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
              textStyleSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to background_color.",
                ).optional(),
                baselineOffsetSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to baseline_offset.",
                ).optional(),
                boldSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to bold.",
                ).optional(),
                fontSizeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to font_size.",
                ).optional(),
                foregroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to foreground_color.",
                ).optional(),
                italicSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to italic.",
                ).optional(),
                linkSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to link.",
                ).optional(),
                smallCapsSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to small_caps.",
                ).optional(),
                strikethroughSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to strikethrough.",
                ).optional(),
                underlineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to underline.",
                ).optional(),
                weightedFontFamilySuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to weighted_font_family.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            })).describe(
              "A mask that indicates which of the fields on the corresponding NamedStyle in styles have been changed in this suggestion. The order of these named style suggestion states matches the order of the corresponding named style within the named styles suggestion.",
            ).optional(),
          }).describe("The suggestion state of a NamedStyles message.")
            .optional(),
        }),
      ).describe(
        "The suggested changes to the named styles of the document tab, keyed by suggestion ID.",
      ).optional(),
    }).describe("A tab with document contents.").optional(),
    tabProperties: z.object({
      iconEmoji: z.string().describe(
        "Optional. The emoji icon displayed with the tab. A valid emoji icon is represented by a non-empty Unicode string. Any set of characters that don't represent a single emoji is invalid. If an emoji is invalid, a 400 bad request error is returned. If this value is unset or empty, the tab will display the default tab icon.",
      ).optional(),
      index: z.number().int().describe(
        "The zero-based index of the tab within the parent.",
      ).optional(),
      nestingLevel: z.number().int().describe(
        "Output only. The depth of the tab within the document. Root-level tabs start at 0.",
      ).optional(),
      parentTabId: z.string().describe(
        "Optional. The ID of the parent tab. Empty when the current tab is a root-level tab, which means it doesn't have any parents.",
      ).optional(),
      tabId: z.string().describe("The immutable ID of the tab.").optional(),
      title: z.string().describe("The user-visible name of the tab.")
        .optional(),
    }).describe("Properties of a tab.").optional(),
  })).describe(
    "Tabs that are part of a document. Tabs can contain child tabs, a tab nested within another tab. Child tabs are represented by the Tab.childTabs field.",
  ).optional(),
  title: z.string().describe("The title of the document.").optional(),
});

const StateSchema = z.object({
  body: z.object({
    content: z.array(z.object({
      endIndex: z.number(),
      paragraph: z.object({
        bullet: z.object({
          listId: z.string(),
          nestingLevel: z.number(),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            baselineOffset: z.string(),
            bold: z.boolean(),
            fontSize: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            italic: z.boolean(),
            link: z.object({
              bookmark: z.object({
                id: z.string(),
                tabId: z.string(),
              }),
              bookmarkId: z.string(),
              heading: z.object({
                id: z.string(),
                tabId: z.string(),
              }),
              headingId: z.string(),
              tabId: z.string(),
              url: z.string(),
            }),
            smallCaps: z.boolean(),
            strikethrough: z.boolean(),
            underline: z.boolean(),
            weightedFontFamily: z.object({
              fontFamily: z.string(),
              weight: z.number(),
            }),
          }),
        }),
        elements: z.array(z.object({
          autoText: z.object({
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
            type: z.string(),
          }),
          columnBreak: z.object({
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          dateElement: z.object({
            dateElementProperties: z.object({
              dateFormat: z.string(),
              displayText: z.string(),
              locale: z.string(),
              timeFormat: z.string(),
              timeZoneId: z.string(),
              timestamp: z.string(),
            }),
            dateId: z.string(),
            suggestedDateElementPropertiesChanges: z.record(
              z.string(),
              z.unknown(),
            ),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          endIndex: z.number(),
          equation: z.object({
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
          }),
          footnoteReference: z.object({
            footnoteId: z.string(),
            footnoteNumber: z.string(),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          horizontalRule: z.object({
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          inlineObjectElement: z.object({
            inlineObjectId: z.string(),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          pageBreak: z.object({
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          person: z.object({
            personId: z.string(),
            personProperties: z.object({
              email: z.string(),
              name: z.string(),
            }),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          richLink: z.object({
            richLinkId: z.string(),
            richLinkProperties: z.object({
              mimeType: z.string(),
              title: z.string(),
              uri: z.string(),
            }),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
          startIndex: z.number(),
          textRun: z.object({
            content: z.string(),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              baselineOffset: z.string(),
              bold: z.boolean(),
              fontSize: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              italic: z.boolean(),
              link: z.object({
                bookmark: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                bookmarkId: z.string(),
                heading: z.object({
                  id: z.string(),
                  tabId: z.string(),
                }),
                headingId: z.string(),
                tabId: z.string(),
                url: z.string(),
              }),
              smallCaps: z.boolean(),
              strikethrough: z.boolean(),
              underline: z.boolean(),
              weightedFontFamily: z.object({
                fontFamily: z.string(),
                weight: z.number(),
              }),
            }),
          }),
        })),
        paragraphStyle: z.object({
          alignment: z.string(),
          avoidWidowAndOrphan: z.boolean(),
          borderBetween: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            dashStyle: z.string(),
            padding: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          }),
          borderBottom: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            dashStyle: z.string(),
            padding: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          }),
          borderLeft: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            dashStyle: z.string(),
            padding: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          }),
          borderRight: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            dashStyle: z.string(),
            padding: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          }),
          borderTop: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            dashStyle: z.string(),
            padding: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          }),
          direction: z.string(),
          headingId: z.string(),
          indentEnd: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          indentFirstLine: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          indentStart: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          keepLinesTogether: z.boolean(),
          keepWithNext: z.boolean(),
          lineSpacing: z.number(),
          namedStyleType: z.string(),
          pageBreakBefore: z.boolean(),
          shading: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
          }),
          spaceAbove: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          spaceBelow: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          spacingMode: z.string(),
          tabStops: z.array(z.object({
            alignment: z.string(),
            offset: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          })),
        }),
        positionedObjectIds: z.array(z.string()),
        suggestedBulletChanges: z.record(z.string(), z.unknown()),
        suggestedParagraphStyleChanges: z.record(z.string(), z.unknown()),
        suggestedPositionedObjectIds: z.record(z.string(), z.unknown()),
      }),
      sectionBreak: z.object({
        sectionStyle: z.object({
          columnProperties: z.array(z.object({
            paddingEnd: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
          })),
          columnSeparatorStyle: z.string(),
          contentDirection: z.string(),
          defaultFooterId: z.string(),
          defaultHeaderId: z.string(),
          evenPageFooterId: z.string(),
          evenPageHeaderId: z.string(),
          firstPageFooterId: z.string(),
          firstPageHeaderId: z.string(),
          flipPageOrientation: z.boolean(),
          marginBottom: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          marginFooter: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          marginHeader: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          marginLeft: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          marginRight: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          marginTop: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          pageNumberStart: z.number(),
          sectionType: z.string(),
          useFirstPageHeaderFooter: z.boolean(),
        }),
        suggestedDeletionIds: z.array(z.string()),
        suggestedInsertionIds: z.array(z.string()),
      }),
      startIndex: z.number(),
      table: z.object({
        columns: z.number(),
        rows: z.number(),
        suggestedDeletionIds: z.array(z.string()),
        suggestedInsertionIds: z.array(z.string()),
        tableRows: z.array(z.object({
          endIndex: z.number(),
          startIndex: z.number(),
          suggestedDeletionIds: z.array(z.string()),
          suggestedInsertionIds: z.array(z.string()),
          suggestedTableRowStyleChanges: z.record(z.string(), z.unknown()),
          tableCells: z.array(z.object({
            content: z.array(z.string()),
            endIndex: z.number(),
            startIndex: z.number(),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            suggestedTableCellStyleChanges: z.record(z.string(), z.unknown()),
            tableCellStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              columnSpan: z.number(),
              contentAlignment: z.string(),
              paddingBottom: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              paddingLeft: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              paddingRight: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              paddingTop: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              rowSpan: z.number(),
            }),
          })),
          tableRowStyle: z.object({
            minRowHeight: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            preventOverflow: z.boolean(),
            tableHeader: z.boolean(),
          }),
        })),
        tableStyle: z.object({
          tableColumnProperties: z.array(z.object({
            width: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            widthType: z.string(),
          })),
        }),
      }),
      tableOfContents: z.object({
        content: z.array(z.string()),
        suggestedDeletionIds: z.array(z.string()),
        suggestedInsertionIds: z.array(z.string()),
      }),
    })),
  }).optional(),
  documentId: z.string().optional(),
  documentStyle: z.object({
    background: z.object({
      color: z.object({
        color: z.object({
          rgbColor: z.object({
            blue: z.number(),
            green: z.number(),
            red: z.number(),
          }),
        }),
      }),
    }),
    defaultFooterId: z.string(),
    defaultHeaderId: z.string(),
    documentFormat: z.object({
      documentMode: z.string(),
    }),
    evenPageFooterId: z.string(),
    evenPageHeaderId: z.string(),
    firstPageFooterId: z.string(),
    firstPageHeaderId: z.string(),
    flipPageOrientation: z.boolean(),
    marginBottom: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    marginFooter: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    marginHeader: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    marginLeft: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    marginRight: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    marginTop: z.object({
      magnitude: z.number(),
      unit: z.string(),
    }),
    pageNumberStart: z.number(),
    pageSize: z.object({
      height: z.object({
        magnitude: z.number(),
        unit: z.string(),
      }),
      width: z.object({
        magnitude: z.number(),
        unit: z.string(),
      }),
    }),
    useCustomHeaderFooterMargins: z.boolean(),
    useEvenPageHeaderFooter: z.boolean(),
    useFirstPageHeaderFooter: z.boolean(),
  }).optional(),
  footers: z.record(z.string(), z.unknown()).optional(),
  footnotes: z.record(z.string(), z.unknown()).optional(),
  headers: z.record(z.string(), z.unknown()).optional(),
  inlineObjects: z.record(z.string(), z.unknown()).optional(),
  lists: z.record(z.string(), z.unknown()).optional(),
  namedRanges: z.record(z.string(), z.unknown()).optional(),
  namedStyles: z.object({
    styles: z.array(z.object({
      namedStyleType: z.string(),
      paragraphStyle: z.object({
        alignment: z.string(),
        avoidWidowAndOrphan: z.boolean(),
        borderBetween: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
          dashStyle: z.string(),
          padding: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        borderBottom: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
          dashStyle: z.string(),
          padding: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        borderLeft: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
          dashStyle: z.string(),
          padding: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        borderRight: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
          dashStyle: z.string(),
          padding: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        borderTop: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
          dashStyle: z.string(),
          padding: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        direction: z.string(),
        headingId: z.string(),
        indentEnd: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        indentFirstLine: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        indentStart: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        keepLinesTogether: z.boolean(),
        keepWithNext: z.boolean(),
        lineSpacing: z.number(),
        namedStyleType: z.string(),
        pageBreakBefore: z.boolean(),
        shading: z.object({
          backgroundColor: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
        }),
        spaceAbove: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        spaceBelow: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        spacingMode: z.string(),
        tabStops: z.array(z.object({
          alignment: z.string(),
          offset: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        })),
      }),
      textStyle: z.object({
        backgroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number(),
              green: z.number(),
              red: z.number(),
            }),
          }),
        }),
        baselineOffset: z.string(),
        bold: z.boolean(),
        fontSize: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        foregroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number(),
              green: z.number(),
              red: z.number(),
            }),
          }),
        }),
        italic: z.boolean(),
        link: z.object({
          bookmark: z.object({
            id: z.string(),
            tabId: z.string(),
          }),
          bookmarkId: z.string(),
          heading: z.object({
            id: z.string(),
            tabId: z.string(),
          }),
          headingId: z.string(),
          tabId: z.string(),
          url: z.string(),
        }),
        smallCaps: z.boolean(),
        strikethrough: z.boolean(),
        underline: z.boolean(),
        weightedFontFamily: z.object({
          fontFamily: z.string(),
          weight: z.number(),
        }),
      }),
    })),
  }).optional(),
  positionedObjects: z.record(z.string(), z.unknown()).optional(),
  revisionId: z.string().optional(),
  suggestedDocumentStyleChanges: z.record(z.string(), z.unknown()).optional(),
  suggestedNamedStylesChanges: z.record(z.string(), z.unknown()).optional(),
  suggestionsViewMode: z.string().optional(),
  tabs: z.array(z.object({
    childTabs: z.array(z.string()),
    documentTab: z.object({
      body: z.object({
        content: z.array(z.object({
          endIndex: z.number(),
          paragraph: z.object({
            bullet: z.object({
              listId: z.string(),
              nestingLevel: z.number(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                baselineOffset: z.string(),
                bold: z.boolean(),
                fontSize: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                italic: z.boolean(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string(),
                    tabId: z.string(),
                  }),
                  bookmarkId: z.string(),
                  heading: z.object({
                    id: z.string(),
                    tabId: z.string(),
                  }),
                  headingId: z.string(),
                  tabId: z.string(),
                  url: z.string(),
                }),
                smallCaps: z.boolean(),
                strikethrough: z.boolean(),
                underline: z.boolean(),
                weightedFontFamily: z.object({
                  fontFamily: z.string(),
                  weight: z.number(),
                }),
              }),
            }),
            elements: z.array(z.object({
              autoText: z.object({
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
                type: z.string(),
              }),
              columnBreak: z.object({
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              dateElement: z.object({
                dateElementProperties: z.object({
                  dateFormat: z.string(),
                  displayText: z.string(),
                  locale: z.string(),
                  timeFormat: z.string(),
                  timeZoneId: z.string(),
                  timestamp: z.string(),
                }),
                dateId: z.string(),
                suggestedDateElementPropertiesChanges: z.record(
                  z.string(),
                  z.unknown(),
                ),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              endIndex: z.number(),
              equation: z.object({
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
              }),
              footnoteReference: z.object({
                footnoteId: z.string(),
                footnoteNumber: z.string(),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              horizontalRule: z.object({
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              inlineObjectElement: z.object({
                inlineObjectId: z.string(),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              pageBreak: z.object({
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              person: z.object({
                personId: z.string(),
                personProperties: z.object({
                  email: z.string(),
                  name: z.string(),
                }),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              richLink: z.object({
                richLinkId: z.string(),
                richLinkProperties: z.object({
                  mimeType: z.string(),
                  title: z.string(),
                  uri: z.string(),
                }),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
              startIndex: z.number(),
              textRun: z.object({
                content: z.string(),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTextStyleChanges: z.record(z.string(), z.unknown()),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  baselineOffset: z.string(),
                  bold: z.boolean(),
                  fontSize: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  italic: z.boolean(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    bookmarkId: z.string(),
                    heading: z.object({
                      id: z.string(),
                      tabId: z.string(),
                    }),
                    headingId: z.string(),
                    tabId: z.string(),
                    url: z.string(),
                  }),
                  smallCaps: z.boolean(),
                  strikethrough: z.boolean(),
                  underline: z.boolean(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string(),
                    weight: z.number(),
                  }),
                }),
              }),
            })),
            paragraphStyle: z.object({
              alignment: z.string(),
              avoidWidowAndOrphan: z.boolean(),
              borderBetween: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                padding: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                padding: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                padding: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                padding: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
                dashStyle: z.string(),
                padding: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              }),
              direction: z.string(),
              headingId: z.string(),
              indentEnd: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              indentFirstLine: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              indentStart: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              keepLinesTogether: z.boolean(),
              keepWithNext: z.boolean(),
              lineSpacing: z.number(),
              namedStyleType: z.string(),
              pageBreakBefore: z.boolean(),
              shading: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number(),
                      green: z.number(),
                      red: z.number(),
                    }),
                  }),
                }),
              }),
              spaceAbove: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              spaceBelow: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              spacingMode: z.string(),
              tabStops: z.array(z.object({
                alignment: z.string(),
                offset: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              })),
            }),
            positionedObjectIds: z.array(z.string()),
            suggestedBulletChanges: z.record(z.string(), z.unknown()),
            suggestedParagraphStyleChanges: z.record(z.string(), z.unknown()),
            suggestedPositionedObjectIds: z.record(z.string(), z.unknown()),
          }),
          sectionBreak: z.object({
            sectionStyle: z.object({
              columnProperties: z.array(z.object({
                paddingEnd: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
              })),
              columnSeparatorStyle: z.string(),
              contentDirection: z.string(),
              defaultFooterId: z.string(),
              defaultHeaderId: z.string(),
              evenPageFooterId: z.string(),
              evenPageHeaderId: z.string(),
              firstPageFooterId: z.string(),
              firstPageHeaderId: z.string(),
              flipPageOrientation: z.boolean(),
              marginBottom: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              marginFooter: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              marginHeader: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              marginLeft: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              marginRight: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              marginTop: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              pageNumberStart: z.number(),
              sectionType: z.string(),
              useFirstPageHeaderFooter: z.boolean(),
            }),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
          }),
          startIndex: z.number(),
          table: z.object({
            columns: z.number(),
            rows: z.number(),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
            tableRows: z.array(z.object({
              endIndex: z.number(),
              startIndex: z.number(),
              suggestedDeletionIds: z.array(z.string()),
              suggestedInsertionIds: z.array(z.string()),
              suggestedTableRowStyleChanges: z.record(z.string(), z.unknown()),
              tableCells: z.array(z.object({
                content: z.array(z.string()),
                endIndex: z.number(),
                startIndex: z.number(),
                suggestedDeletionIds: z.array(z.string()),
                suggestedInsertionIds: z.array(z.string()),
                suggestedTableCellStyleChanges: z.record(
                  z.string(),
                  z.unknown(),
                ),
                tableCellStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number(),
                        green: z.number(),
                        red: z.number(),
                      }),
                    }),
                  }),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number(),
                          green: z.number(),
                          red: z.number(),
                        }),
                      }),
                    }),
                    dashStyle: z.string(),
                    width: z.object({
                      magnitude: z.number(),
                      unit: z.string(),
                    }),
                  }),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number(),
                          green: z.number(),
                          red: z.number(),
                        }),
                      }),
                    }),
                    dashStyle: z.string(),
                    width: z.object({
                      magnitude: z.number(),
                      unit: z.string(),
                    }),
                  }),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number(),
                          green: z.number(),
                          red: z.number(),
                        }),
                      }),
                    }),
                    dashStyle: z.string(),
                    width: z.object({
                      magnitude: z.number(),
                      unit: z.string(),
                    }),
                  }),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number(),
                          green: z.number(),
                          red: z.number(),
                        }),
                      }),
                    }),
                    dashStyle: z.string(),
                    width: z.object({
                      magnitude: z.number(),
                      unit: z.string(),
                    }),
                  }),
                  columnSpan: z.number(),
                  contentAlignment: z.string(),
                  paddingBottom: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  paddingLeft: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  paddingRight: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  paddingTop: z.object({
                    magnitude: z.number(),
                    unit: z.string(),
                  }),
                  rowSpan: z.number(),
                }),
              })),
              tableRowStyle: z.object({
                minRowHeight: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                preventOverflow: z.boolean(),
                tableHeader: z.boolean(),
              }),
            })),
            tableStyle: z.object({
              tableColumnProperties: z.array(z.object({
                width: z.object({
                  magnitude: z.number(),
                  unit: z.string(),
                }),
                widthType: z.string(),
              })),
            }),
          }),
          tableOfContents: z.object({
            content: z.array(z.string()),
            suggestedDeletionIds: z.array(z.string()),
            suggestedInsertionIds: z.array(z.string()),
          }),
        })),
      }),
      documentStyle: z.object({
        background: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number(),
                green: z.number(),
                red: z.number(),
              }),
            }),
          }),
        }),
        defaultFooterId: z.string(),
        defaultHeaderId: z.string(),
        documentFormat: z.object({
          documentMode: z.string(),
        }),
        evenPageFooterId: z.string(),
        evenPageHeaderId: z.string(),
        firstPageFooterId: z.string(),
        firstPageHeaderId: z.string(),
        flipPageOrientation: z.boolean(),
        marginBottom: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        marginFooter: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        marginHeader: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        marginLeft: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        marginRight: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        marginTop: z.object({
          magnitude: z.number(),
          unit: z.string(),
        }),
        pageNumberStart: z.number(),
        pageSize: z.object({
          height: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
          width: z.object({
            magnitude: z.number(),
            unit: z.string(),
          }),
        }),
        useCustomHeaderFooterMargins: z.boolean(),
        useEvenPageHeaderFooter: z.boolean(),
        useFirstPageHeaderFooter: z.boolean(),
      }),
      footers: z.record(z.string(), z.unknown()),
      footnotes: z.record(z.string(), z.unknown()),
      headers: z.record(z.string(), z.unknown()),
      inlineObjects: z.record(z.string(), z.unknown()),
      lists: z.record(z.string(), z.unknown()),
      namedRanges: z.record(z.string(), z.unknown()),
      namedStyles: z.object({
        styles: z.array(z.object({
          namedStyleType: z.string(),
          paragraphStyle: z.object({
            alignment: z.string(),
            avoidWidowAndOrphan: z.boolean(),
            borderBetween: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              dashStyle: z.string(),
              padding: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              width: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            }),
            borderBottom: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              dashStyle: z.string(),
              padding: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              width: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            }),
            borderLeft: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              dashStyle: z.string(),
              padding: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              width: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            }),
            borderRight: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              dashStyle: z.string(),
              padding: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              width: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            }),
            borderTop: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
              dashStyle: z.string(),
              padding: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
              width: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            }),
            direction: z.string(),
            headingId: z.string(),
            indentEnd: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            indentFirstLine: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            indentStart: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            keepLinesTogether: z.boolean(),
            keepWithNext: z.boolean(),
            lineSpacing: z.number(),
            namedStyleType: z.string(),
            pageBreakBefore: z.boolean(),
            shading: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number(),
                    green: z.number(),
                    red: z.number(),
                  }),
                }),
              }),
            }),
            spaceAbove: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            spaceBelow: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            spacingMode: z.string(),
            tabStops: z.array(z.object({
              alignment: z.string(),
              offset: z.object({
                magnitude: z.number(),
                unit: z.string(),
              }),
            })),
          }),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            baselineOffset: z.string(),
            bold: z.boolean(),
            fontSize: z.object({
              magnitude: z.number(),
              unit: z.string(),
            }),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number(),
                  green: z.number(),
                  red: z.number(),
                }),
              }),
            }),
            italic: z.boolean(),
            link: z.object({
              bookmark: z.object({
                id: z.string(),
                tabId: z.string(),
              }),
              bookmarkId: z.string(),
              heading: z.object({
                id: z.string(),
                tabId: z.string(),
              }),
              headingId: z.string(),
              tabId: z.string(),
              url: z.string(),
            }),
            smallCaps: z.boolean(),
            strikethrough: z.boolean(),
            underline: z.boolean(),
            weightedFontFamily: z.object({
              fontFamily: z.string(),
              weight: z.number(),
            }),
          }),
        })),
      }),
      positionedObjects: z.record(z.string(), z.unknown()),
      suggestedDocumentStyleChanges: z.record(z.string(), z.unknown()),
      suggestedNamedStylesChanges: z.record(z.string(), z.unknown()),
    }),
    tabProperties: z.object({
      iconEmoji: z.string(),
      index: z.number(),
      nestingLevel: z.number(),
      parentTabId: z.string(),
      tabId: z.string(),
      title: z.string(),
    }),
  })).optional(),
  title: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  name: z.string().optional(),
  body: z.object({
    content: z.array(z.object({
      endIndex: z.number().int().describe(
        "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
      ).optional(),
      paragraph: z.object({
        bullet: z.object({
          listId: z.string().describe(
            "The ID of the list this paragraph belongs to.",
          ).optional(),
          nestingLevel: z.number().int().describe(
            "The nesting level of this paragraph in the list.",
          ).optional(),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            baselineOffset: z.enum([
              "BASELINE_OFFSET_UNSPECIFIED",
              "NONE",
              "SUPERSCRIPT",
              "SUBSCRIPT",
            ]).describe(
              "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
            ).optional(),
            bold: z.boolean().describe(
              "Whether or not the text is rendered as bold.",
            ).optional(),
            fontSize: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            italic: z.boolean().describe(
              "Whether or not the text is italicized.",
            ).optional(),
            link: z.object({
              bookmark: z.object({
                id: z.string().describe(
                  "The ID of a bookmark in this document.",
                ).optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this bookmark.",
                ).optional(),
              }).describe("A reference to a bookmark in this document.")
                .optional(),
              bookmarkId: z.string().describe(
                "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              heading: z.object({
                id: z.string().describe("The ID of a heading in this document.")
                  .optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this heading.",
                ).optional(),
              }).describe("A reference to a heading in this document.")
                .optional(),
              headingId: z.string().describe(
                "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              tabId: z.string().describe("The ID of a tab in this document.")
                .optional(),
              url: z.string().describe("An external URL.").optional(),
            }).describe(
              "A reference to another portion of a document or an external URL resource.",
            ).optional(),
            smallCaps: z.boolean().describe(
              "Whether or not the text is in small capital letters.",
            ).optional(),
            strikethrough: z.boolean().describe(
              "Whether or not the text is struck through.",
            ).optional(),
            underline: z.boolean().describe(
              "Whether or not the text is underlined.",
            ).optional(),
            weightedFontFamily: z.object({
              fontFamily: z.string().describe(
                "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
              ).optional(),
              weight: z.number().int().describe(
                "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
              ).optional(),
            }).describe("Represents a font family and weight of text.")
              .optional(),
          }).describe(
            "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
        }).describe("Describes the bullet of a paragraph.").optional(),
        elements: z.array(z.object({
          autoText: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this AutoText, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            type: z.enum(["TYPE_UNSPECIFIED", "PAGE_NUMBER", "PAGE_COUNT"])
              .describe("The type of this auto text.").optional(),
          }).describe(
            "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
          ).optional(),
          columnBreak: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
          ).optional(),
          dateElement: z.object({
            dateElementProperties: z.object({
              dateFormat: z.enum([
                "DATE_FORMAT_UNSPECIFIED",
                "DATE_FORMAT_CUSTOM",
                "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                "DATE_FORMAT_MONTH_DAY_FULL",
                "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                "DATE_FORMAT_ISO8601",
              ]).describe(
                "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
              ).optional(),
              displayText: z.string().describe(
                "Output only. Indicates how the DateElement is displayed in the document.",
              ).optional(),
              locale: z.string().describe(
                "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
              ).optional(),
              timeFormat: z.enum([
                "TIME_FORMAT_UNSPECIFIED",
                "TIME_FORMAT_DISABLED",
                "TIME_FORMAT_HOUR_MINUTE",
                "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
              ]).describe(
                "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
              ).optional(),
              timeZoneId: z.string().describe(
                "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
              ).optional(),
              timestamp: z.string().describe(
                "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
              ).optional(),
            }).describe("Properties of a DateElement.").optional(),
            dateId: z.string().describe(
              "Output only. The unique ID of this date.",
            ).optional(),
            suggestedDateElementPropertiesChanges: z.record(
              z.string(),
              z.object({
                dateElementProperties: z.object({
                  dateFormat: z.enum([
                    "DATE_FORMAT_UNSPECIFIED",
                    "DATE_FORMAT_CUSTOM",
                    "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                    "DATE_FORMAT_MONTH_DAY_FULL",
                    "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                    "DATE_FORMAT_ISO8601",
                  ]).describe(
                    "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                  ).optional(),
                  displayText: z.string().describe(
                    "Output only. Indicates how the DateElement is displayed in the document.",
                  ).optional(),
                  locale: z.string().describe(
                    "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                  ).optional(),
                  timeFormat: z.enum([
                    "TIME_FORMAT_UNSPECIFIED",
                    "TIME_FORMAT_DISABLED",
                    "TIME_FORMAT_HOUR_MINUTE",
                    "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                  ]).describe(
                    "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                  ).optional(),
                  timeZoneId: z.string().describe(
                    "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                  ).optional(),
                  timestamp: z.string().describe(
                    "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                  ).optional(),
                }).describe("Properties of a DateElement.").optional(),
                dateElementPropertiesSuggestionState: z.object({
                  dateFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to date_format.",
                  ).optional(),
                  localeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to locale.",
                  ).optional(),
                  timeFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to time_format.",
                  ).optional(),
                  timeZoneIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to time_zone_id.",
                  ).optional(),
                  timestampSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to timestamp.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested changes to the date element properties, keyed by suggestion ID.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this DateElement, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A date instance mentioned in a document.").optional(),
          endIndex: z.number().int().describe(
            "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
          ).optional(),
          equation: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe("A ParagraphElement representing an equation.")
            .optional(),
          footnoteReference: z.object({
            footnoteId: z.string().describe(
              "The ID of the footnote that contains the content of this footnote reference.",
            ).optional(),
            footnoteNumber: z.string().describe(
              "The rendered number of this footnote.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
          ).optional(),
          horizontalRule: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A ParagraphElement representing a horizontal line.")
            .optional(),
          inlineObjectElement: z.object({
            inlineObjectId: z.string().describe(
              "The ID of the InlineObject this element contains.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe("A ParagraphElement that contains an InlineObject.")
            .optional(),
          pageBreak: z.object({
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
          ).optional(),
          person: z.object({
            personId: z.string().describe(
              "Output only. The unique ID of this link.",
            ).optional(),
            personProperties: z.object({
              email: z.string().describe(
                "The email address linked to this Person. This field is always present.",
              ).optional(),
              name: z.string().describe(
                "The name of the person if it's displayed in the link text instead of the person's email address.",
              ).optional(),
            }).describe("Properties specific to a linked Person.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this Person, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
          ).optional(),
          richLink: z.object({
            richLinkId: z.string().describe("Output only. The ID of this link.")
              .optional(),
            richLinkProperties: z.object({
              mimeType: z.string().describe(
                "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
              ).optional(),
              title: z.string().describe(
                "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
              ).optional(),
              uri: z.string().describe(
                "The URI to the RichLink. This is always present.",
              ).optional(),
            }).describe("Properties specific to a RichLink.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this RichLink, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this paragraph element, in UTF-16 code units.",
          ).optional(),
          textRun: z.object({
            content: z.string().describe(
              "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTextStyleChanges: z.record(
              z.string(),
              z.object({
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                textStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  baselineOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to baseline_offset.",
                  ).optional(),
                  boldSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bold.",
                  ).optional(),
                  fontSizeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to font_size.",
                  ).optional(),
                  foregroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to foreground_color.",
                  ).optional(),
                  italicSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to italic.",
                  ).optional(),
                  linkSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to link.",
                  ).optional(),
                  smallCapsSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to small_caps.",
                  ).optional(),
                  strikethroughSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to strikethrough.",
                  ).optional(),
                  underlineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to underline.",
                  ).optional(),
                  weightedFontFamilySuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to weighted_font_family.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested text style changes to this run, keyed by suggestion ID.",
            ).optional(),
            textStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              baselineOffset: z.enum([
                "BASELINE_OFFSET_UNSPECIFIED",
                "NONE",
                "SUPERSCRIPT",
                "SUBSCRIPT",
              ]).describe(
                "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
              ).optional(),
              bold: z.boolean().describe(
                "Whether or not the text is rendered as bold.",
              ).optional(),
              fontSize: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              foregroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              italic: z.boolean().describe(
                "Whether or not the text is italicized.",
              ).optional(),
              link: z.object({
                bookmark: z.object({
                  id: z.string().describe(
                    "The ID of a bookmark in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this bookmark.",
                  ).optional(),
                }).describe("A reference to a bookmark in this document.")
                  .optional(),
                bookmarkId: z.string().describe(
                  "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                heading: z.object({
                  id: z.string().describe(
                    "The ID of a heading in this document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of the tab containing this heading.",
                  ).optional(),
                }).describe("A reference to a heading in this document.")
                  .optional(),
                headingId: z.string().describe(
                  "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                ).optional(),
                tabId: z.string().describe("The ID of a tab in this document.")
                  .optional(),
                url: z.string().describe("An external URL.").optional(),
              }).describe(
                "A reference to another portion of a document or an external URL resource.",
              ).optional(),
              smallCaps: z.boolean().describe(
                "Whether or not the text is in small capital letters.",
              ).optional(),
              strikethrough: z.boolean().describe(
                "Whether or not the text is struck through.",
              ).optional(),
              underline: z.boolean().describe(
                "Whether or not the text is underlined.",
              ).optional(),
              weightedFontFamily: z.object({
                fontFamily: z.string().describe(
                  "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                ).optional(),
                weight: z.number().int().describe(
                  "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                ).optional(),
              }).describe("Represents a font family and weight of text.")
                .optional(),
            }).describe(
              "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
          }).describe(
            "A ParagraphElement that represents a run of text that all has the same styling.",
          ).optional(),
        })).describe(
          "The content of the paragraph, broken down into its component parts.",
        ).optional(),
        paragraphStyle: z.object({
          alignment: z.enum([
            "ALIGNMENT_UNSPECIFIED",
            "START",
            "CENTER",
            "END",
            "JUSTIFIED",
          ]).describe("The text alignment for this paragraph.").optional(),
          avoidWidowAndOrphan: z.boolean().describe(
            "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
          ).optional(),
          borderBetween: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderBottom: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderLeft: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderRight: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          borderTop: z.object({
            color: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            dashStyle: z.enum([
              "DASH_STYLE_UNSPECIFIED",
              "SOLID",
              "DOT",
              "DASH",
            ]).describe("The dash style of the border.").optional(),
            padding: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          }).describe("A border around a paragraph.").optional(),
          direction: z.enum([
            "CONTENT_DIRECTION_UNSPECIFIED",
            "LEFT_TO_RIGHT",
            "RIGHT_TO_LEFT",
          ]).describe(
            "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
          ).optional(),
          headingId: z.string().describe(
            "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
          ).optional(),
          indentEnd: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          indentFirstLine: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          indentStart: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          keepLinesTogether: z.boolean().describe(
            "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
          ).optional(),
          keepWithNext: z.boolean().describe(
            "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
          ).optional(),
          lineSpacing: z.number().describe(
            "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
          ).optional(),
          namedStyleType: z.enum([
            "NAMED_STYLE_TYPE_UNSPECIFIED",
            "NORMAL_TEXT",
            "TITLE",
            "SUBTITLE",
            "HEADING_1",
            "HEADING_2",
            "HEADING_3",
            "HEADING_4",
            "HEADING_5",
            "HEADING_6",
          ]).describe(
            "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
          ).optional(),
          pageBreakBefore: z.boolean().describe(
            "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
          ).optional(),
          shading: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
          }).describe("The shading of a paragraph.").optional(),
          spaceAbove: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          spaceBelow: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          spacingMode: z.enum([
            "SPACING_MODE_UNSPECIFIED",
            "NEVER_COLLAPSE",
            "COLLAPSE_LISTS",
          ]).describe("The spacing mode for the paragraph.").optional(),
          tabStops: z.array(z.object({
            alignment: z.enum([
              "TAB_STOP_ALIGNMENT_UNSPECIFIED",
              "START",
              "CENTER",
              "END",
            ]).describe(
              "The alignment of this tab stop. If unset, the value defaults to START.",
            ).optional(),
            offset: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          })).describe(
            "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
          ).optional(),
        }).describe(
          "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
        ).optional(),
        positionedObjectIds: z.array(z.string()).describe(
          "The IDs of the positioned objects tethered to this paragraph.",
        ).optional(),
        suggestedBulletChanges: z.record(
          z.string(),
          z.object({
            bullet: z.object({
              listId: z.string().describe(
                "The ID of the list this paragraph belongs to.",
              ).optional(),
              nestingLevel: z.number().int().describe(
                "The nesting level of this paragraph in the list.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            }).describe("Describes the bullet of a paragraph.").optional(),
            bulletSuggestionState: z.object({
              listIdSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to the list_id.",
              ).optional(),
              nestingLevelSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to the nesting_level.",
              ).optional(),
              textStyleSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to background_color.",
                ).optional(),
                baselineOffsetSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to baseline_offset.",
                ).optional(),
                boldSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to bold.",
                ).optional(),
                fontSizeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to font_size.",
                ).optional(),
                foregroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to foreground_color.",
                ).optional(),
                italicSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to italic.",
                ).optional(),
                linkSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to link.",
                ).optional(),
                smallCapsSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to small_caps.",
                ).optional(),
                strikethroughSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to strikethrough.",
                ).optional(),
                underlineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to underline.",
                ).optional(),
                weightedFontFamilySuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to weighted_font_family.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
            ).optional(),
          }),
        ).describe("The suggested changes to this paragraph's bullet.")
          .optional(),
        suggestedParagraphStyleChanges: z.record(
          z.string(),
          z.object({
            paragraphStyle: z.object({
              alignment: z.enum([
                "ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
                "JUSTIFIED",
              ]).describe("The text alignment for this paragraph.").optional(),
              avoidWidowAndOrphan: z.boolean().describe(
                "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
              ).optional(),
              borderBetween: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              direction: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
              ).optional(),
              headingId: z.string().describe(
                "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
              ).optional(),
              indentEnd: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              keepLinesTogether: z.boolean().describe(
                "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              keepWithNext: z.boolean().describe(
                "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              lineSpacing: z.number().describe(
                "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
              ).optional(),
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
              ).optional(),
              pageBreakBefore: z.boolean().describe(
                "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
              ).optional(),
              shading: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
              }).describe("The shading of a paragraph.").optional(),
              spaceAbove: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spaceBelow: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spacingMode: z.enum([
                "SPACING_MODE_UNSPECIFIED",
                "NEVER_COLLAPSE",
                "COLLAPSE_LISTS",
              ]).describe("The spacing mode for the paragraph.").optional(),
              tabStops: z.array(z.object({
                alignment: z.enum([
                  "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                ]).describe(
                  "The alignment of this tab stop. If unset, the value defaults to START.",
                ).optional(),
                offset: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
              ).optional(),
            }).describe(
              "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            paragraphStyleSuggestionState: z.object({
              alignmentSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to alignment.",
              ).optional(),
              avoidWidowAndOrphanSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to avoid_widow_and_orphan.",
              ).optional(),
              borderBetweenSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_between.",
              ).optional(),
              borderBottomSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_bottom.",
              ).optional(),
              borderLeftSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_left.",
              ).optional(),
              borderRightSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_right.",
              ).optional(),
              borderTopSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to border_top.",
              ).optional(),
              directionSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to direction.",
              ).optional(),
              headingIdSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to heading_id.",
              ).optional(),
              indentEndSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_end.",
              ).optional(),
              indentFirstLineSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_first_line.",
              ).optional(),
              indentStartSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to indent_start.",
              ).optional(),
              keepLinesTogetherSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to keep_lines_together.",
              ).optional(),
              keepWithNextSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to keep_with_next.",
              ).optional(),
              lineSpacingSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to line_spacing.",
              ).optional(),
              namedStyleTypeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to named_style_type.",
              ).optional(),
              pageBreakBeforeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to page_break_before.",
              ).optional(),
              shadingSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to the Shading.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
              ).optional(),
              spaceAboveSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to space_above.",
              ).optional(),
              spaceBelowSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to space_below.",
              ).optional(),
              spacingModeSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to spacing_mode.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
            ).optional(),
          }),
        ).describe(
          "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
        ).optional(),
        suggestedPositionedObjectIds: z.record(
          z.string(),
          z.object({
            objectIds: z.array(z.string()).describe("The object IDs.")
              .optional(),
          }),
        ).describe(
          "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
        ).optional(),
      }).describe(
        "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
      ).optional(),
      sectionBreak: z.object({
        sectionStyle: z.object({
          columnProperties: z.array(z.object({
            paddingEnd: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
          })).describe(
            "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
          ).optional(),
          columnSeparatorStyle: z.enum([
            "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
            "NONE",
            "BETWEEN_EACH_COLUMN",
          ]).describe(
            "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          contentDirection: z.enum([
            "CONTENT_DIRECTION_UNSPECIFIED",
            "LEFT_TO_RIGHT",
            "RIGHT_TO_LEFT",
          ]).describe(
            "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          defaultFooterId: z.string().describe(
            "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          defaultHeaderId: z.string().describe(
            "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          evenPageFooterId: z.string().describe(
            "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          evenPageHeaderId: z.string().describe(
            "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          firstPageFooterId: z.string().describe(
            "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          firstPageHeaderId: z.string().describe(
            "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
          ).optional(),
          flipPageOrientation: z.boolean().describe(
            "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          marginBottom: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginFooter: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginHeader: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginLeft: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginRight: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          marginTop: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          pageNumberStart: z.number().int().describe(
            "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
          sectionType: z.enum([
            "SECTION_TYPE_UNSPECIFIED",
            "CONTINUOUS",
            "NEXT_PAGE",
          ]).describe("Output only. The type of section.").optional(),
          useFirstPageHeaderFooter: z.boolean().describe(
            "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
          ).optional(),
        }).describe("The styling that applies to a section.").optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
      }).describe(
        "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
      ).optional(),
      startIndex: z.number().int().describe(
        "The zero-based start index of this structural element, in UTF-16 code units.",
      ).optional(),
      table: z.object({
        columns: z.number().int().describe(
          "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
        ).optional(),
        rows: z.number().int().describe("Number of rows in the table.")
          .optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
        tableRows: z.array(z.object({
          endIndex: z.number().int().describe(
            "The zero-based end index of this row, exclusive, in UTF-16 code units.",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this row, in UTF-16 code units.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInsertionIds: z.array(z.string()).describe(
            "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedTableRowStyleChanges: z.record(
            z.string(),
            z.object({
              tableRowStyle: z.object({
                minRowHeight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                preventOverflow: z.boolean().describe(
                  "Whether the row cannot overflow across page or column boundaries.",
                ).optional(),
                tableHeader: z.boolean().describe(
                  "Whether the row is a table header.",
                ).optional(),
              }).describe("Styles that apply to a table row.").optional(),
              tableRowStyleSuggestionState: z.object({
                minRowHeightSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to min_row_height.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested style changes to this row, keyed by suggestion ID.",
          ).optional(),
          tableCells: z.array(z.object({
            content: z.array(z.string()).describe("The content of the cell.")
              .optional(),
            endIndex: z.number().int().describe(
              "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this cell, in UTF-16 code units.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            suggestedTableCellStyleChanges: z.record(
              z.string(),
              z.object({
                tableCellStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  columnSpan: z.number().int().describe(
                    "The column span of the cell. This property is read-only.",
                  ).optional(),
                  contentAlignment: z.enum([
                    "CONTENT_ALIGNMENT_UNSPECIFIED",
                    "CONTENT_ALIGNMENT_UNSUPPORTED",
                    "TOP",
                    "MIDDLE",
                    "BOTTOM",
                  ]).describe(
                    "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                  ).optional(),
                  paddingBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  rowSpan: z.number().int().describe(
                    "The row span of the cell. This property is read-only.",
                  ).optional(),
                }).describe(
                  "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                ).optional(),
                tableCellStyleSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to background_color.",
                  ).optional(),
                  borderBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_bottom.",
                  ).optional(),
                  borderLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_left.",
                  ).optional(),
                  borderRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_right.",
                  ).optional(),
                  borderTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_top.",
                  ).optional(),
                  columnSpanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to column_span.",
                  ).optional(),
                  contentAlignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to content_alignment.",
                  ).optional(),
                  paddingBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_bottom.",
                  ).optional(),
                  paddingLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_left.",
                  ).optional(),
                  paddingRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_right.",
                  ).optional(),
                  paddingTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to padding_top.",
                  ).optional(),
                  rowSpanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to row_span.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested changes to the table cell style, keyed by suggestion ID.",
            ).optional(),
            tableCellStyle: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe(
                "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
              ).optional(),
              columnSpan: z.number().int().describe(
                "The column span of the cell. This property is read-only.",
              ).optional(),
              contentAlignment: z.enum([
                "CONTENT_ALIGNMENT_UNSPECIFIED",
                "CONTENT_ALIGNMENT_UNSUPPORTED",
                "TOP",
                "MIDDLE",
                "BOTTOM",
              ]).describe(
                "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
              ).optional(),
              paddingBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              paddingTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              rowSpan: z.number().int().describe(
                "The row span of the cell. This property is read-only.",
              ).optional(),
            }).describe(
              "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
            ).optional(),
          })).describe(
            "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
          ).optional(),
          tableRowStyle: z.object({
            minRowHeight: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            preventOverflow: z.boolean().describe(
              "Whether the row cannot overflow across page or column boundaries.",
            ).optional(),
            tableHeader: z.boolean().describe(
              "Whether the row is a table header.",
            ).optional(),
          }).describe("Styles that apply to a table row.").optional(),
        })).describe("The contents and style of each row.").optional(),
        tableStyle: z.object({
          tableColumnProperties: z.array(z.object({
            width: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            widthType: z.enum([
              "WIDTH_TYPE_UNSPECIFIED",
              "EVENLY_DISTRIBUTED",
              "FIXED_WIDTH",
            ]).describe("The width type of the column.").optional(),
          })).describe(
            "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
          ).optional(),
        }).describe("Styles that apply to a table.").optional(),
      }).describe("A StructuralElement representing a table.").optional(),
      tableOfContents: z.object({
        content: z.array(z.string()).describe(
          "The content of the table of contents.",
        ).optional(),
        suggestedDeletionIds: z.array(z.string()).describe(
          "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
        ).optional(),
        suggestedInsertionIds: z.array(z.string()).describe(
          "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
        ).optional(),
      }).describe("A StructuralElement representing a table of contents.")
        .optional(),
    })).describe(
      "The contents of the body. The indexes for the body's content begin at zero.",
    ).optional(),
  }).describe(
    "The document body. The body typically contains the full document contents except for headers, footers, and footnotes.",
  ).optional(),
  documentStyle: z.object({
    background: z.object({
      color: z.object({
        color: z.object({
          rgbColor: z.object({
            blue: z.number().describe(
              "The blue component of the color, from 0.0 to 1.0.",
            ).optional(),
            green: z.number().describe(
              "The green component of the color, from 0.0 to 1.0.",
            ).optional(),
            red: z.number().describe(
              "The red component of the color, from 0.0 to 1.0.",
            ).optional(),
          }).describe("An RGB color.").optional(),
        }).describe("A solid color.").optional(),
      }).describe(
        "A color that can either be fully opaque or fully transparent.",
      ).optional(),
    }).describe("Represents the background of a document.").optional(),
    defaultFooterId: z.string().describe(
      "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    defaultHeaderId: z.string().describe(
      "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    documentFormat: z.object({
      documentMode: z.enum(["DOCUMENT_MODE_UNSPECIFIED", "PAGES", "PAGELESS"])
        .describe("Whether the document has pages or is pageless.").optional(),
    }).describe("Represents document-level format settings.").optional(),
    evenPageFooterId: z.string().describe(
      "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    evenPageHeaderId: z.string().describe(
      "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    firstPageFooterId: z.string().describe(
      "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    firstPageHeaderId: z.string().describe(
      "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    flipPageOrientation: z.boolean().describe(
      "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    marginBottom: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginFooter: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginHeader: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginLeft: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginRight: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    marginTop: z.object({
      magnitude: z.number().describe("The magnitude.").optional(),
      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
        "The units for magnitude.",
      ).optional(),
    }).describe("A magnitude in a single direction in the specified units.")
      .optional(),
    pageNumberStart: z.number().int().describe(
      "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    pageSize: z.object({
      height: z.object({
        magnitude: z.number().describe("The magnitude.").optional(),
        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
          "The units for magnitude.",
        ).optional(),
      }).describe("A magnitude in a single direction in the specified units.")
        .optional(),
      width: z.object({
        magnitude: z.number().describe("The magnitude.").optional(),
        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
          "The units for magnitude.",
        ).optional(),
      }).describe("A magnitude in a single direction in the specified units.")
        .optional(),
    }).describe("A width and height.").optional(),
    useCustomHeaderFooterMargins: z.boolean().describe(
      "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
    ).optional(),
    useEvenPageHeaderFooter: z.boolean().describe(
      "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
    useFirstPageHeaderFooter: z.boolean().describe(
      "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
    ).optional(),
  }).describe("The style of the document.").optional(),
  namedStyles: z.object({
    styles: z.array(z.object({
      namedStyleType: z.enum([
        "NAMED_STYLE_TYPE_UNSPECIFIED",
        "NORMAL_TEXT",
        "TITLE",
        "SUBTITLE",
        "HEADING_1",
        "HEADING_2",
        "HEADING_3",
        "HEADING_4",
        "HEADING_5",
        "HEADING_6",
      ]).describe("The type of this named style.").optional(),
      paragraphStyle: z.object({
        alignment: z.enum([
          "ALIGNMENT_UNSPECIFIED",
          "START",
          "CENTER",
          "END",
          "JUSTIFIED",
        ]).describe("The text alignment for this paragraph.").optional(),
        avoidWidowAndOrphan: z.boolean().describe(
          "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
        ).optional(),
        borderBetween: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderBottom: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderLeft: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderRight: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        borderTop: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
          dashStyle: z.enum(["DASH_STYLE_UNSPECIFIED", "SOLID", "DOT", "DASH"])
            .describe("The dash style of the border.").optional(),
          padding: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A border around a paragraph.").optional(),
        direction: z.enum([
          "CONTENT_DIRECTION_UNSPECIFIED",
          "LEFT_TO_RIGHT",
          "RIGHT_TO_LEFT",
        ]).describe(
          "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
        ).optional(),
        headingId: z.string().describe(
          "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
        ).optional(),
        indentEnd: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        indentFirstLine: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        indentStart: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        keepLinesTogether: z.boolean().describe(
          "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
        ).optional(),
        keepWithNext: z.boolean().describe(
          "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
        ).optional(),
        lineSpacing: z.number().describe(
          "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
        ).optional(),
        namedStyleType: z.enum([
          "NAMED_STYLE_TYPE_UNSPECIFIED",
          "NORMAL_TEXT",
          "TITLE",
          "SUBTITLE",
          "HEADING_1",
          "HEADING_2",
          "HEADING_3",
          "HEADING_4",
          "HEADING_5",
          "HEADING_6",
        ]).describe(
          "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
        ).optional(),
        pageBreakBefore: z.boolean().describe(
          "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
        ).optional(),
        shading: z.object({
          backgroundColor: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
        }).describe("The shading of a paragraph.").optional(),
        spaceAbove: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        spaceBelow: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        spacingMode: z.enum([
          "SPACING_MODE_UNSPECIFIED",
          "NEVER_COLLAPSE",
          "COLLAPSE_LISTS",
        ]).describe("The spacing mode for the paragraph.").optional(),
        tabStops: z.array(z.object({
          alignment: z.enum([
            "TAB_STOP_ALIGNMENT_UNSPECIFIED",
            "START",
            "CENTER",
            "END",
          ]).describe(
            "The alignment of this tab stop. If unset, the value defaults to START.",
          ).optional(),
          offset: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        })).describe(
          "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
        ).optional(),
      }).describe(
        "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
      ).optional(),
      textStyle: z.object({
        backgroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number().describe(
                "The blue component of the color, from 0.0 to 1.0.",
              ).optional(),
              green: z.number().describe(
                "The green component of the color, from 0.0 to 1.0.",
              ).optional(),
              red: z.number().describe(
                "The red component of the color, from 0.0 to 1.0.",
              ).optional(),
            }).describe("An RGB color.").optional(),
          }).describe("A solid color.").optional(),
        }).describe(
          "A color that can either be fully opaque or fully transparent.",
        ).optional(),
        baselineOffset: z.enum([
          "BASELINE_OFFSET_UNSPECIFIED",
          "NONE",
          "SUPERSCRIPT",
          "SUBSCRIPT",
        ]).describe(
          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
        ).optional(),
        bold: z.boolean().describe(
          "Whether or not the text is rendered as bold.",
        ).optional(),
        fontSize: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        foregroundColor: z.object({
          color: z.object({
            rgbColor: z.object({
              blue: z.number().describe(
                "The blue component of the color, from 0.0 to 1.0.",
              ).optional(),
              green: z.number().describe(
                "The green component of the color, from 0.0 to 1.0.",
              ).optional(),
              red: z.number().describe(
                "The red component of the color, from 0.0 to 1.0.",
              ).optional(),
            }).describe("An RGB color.").optional(),
          }).describe("A solid color.").optional(),
        }).describe(
          "A color that can either be fully opaque or fully transparent.",
        ).optional(),
        italic: z.boolean().describe("Whether or not the text is italicized.")
          .optional(),
        link: z.object({
          bookmark: z.object({
            id: z.string().describe("The ID of a bookmark in this document.")
              .optional(),
            tabId: z.string().describe(
              "The ID of the tab containing this bookmark.",
            ).optional(),
          }).describe("A reference to a bookmark in this document.").optional(),
          bookmarkId: z.string().describe(
            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
          ).optional(),
          heading: z.object({
            id: z.string().describe("The ID of a heading in this document.")
              .optional(),
            tabId: z.string().describe(
              "The ID of the tab containing this heading.",
            ).optional(),
          }).describe("A reference to a heading in this document.").optional(),
          headingId: z.string().describe(
            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
          ).optional(),
          tabId: z.string().describe("The ID of a tab in this document.")
            .optional(),
          url: z.string().describe("An external URL.").optional(),
        }).describe(
          "A reference to another portion of a document or an external URL resource.",
        ).optional(),
        smallCaps: z.boolean().describe(
          "Whether or not the text is in small capital letters.",
        ).optional(),
        strikethrough: z.boolean().describe(
          "Whether or not the text is struck through.",
        ).optional(),
        underline: z.boolean().describe(
          "Whether or not the text is underlined.",
        ).optional(),
        weightedFontFamily: z.object({
          fontFamily: z.string().describe(
            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
          ).optional(),
          weight: z.number().int().describe(
            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
          ).optional(),
        }).describe("Represents a font family and weight of text.").optional(),
      }).describe(
        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
      ).optional(),
    })).describe(
      "The named styles. There's an entry for each of the possible named style types.",
    ).optional(),
  }).describe(
    "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
  ).optional(),
  tabs: z.array(z.object({
    childTabs: z.array(z.string()).describe(
      "The child tabs nested within this tab.",
    ).optional(),
    documentTab: z.object({
      body: z.object({
        content: z.array(z.object({
          endIndex: z.number().int().describe(
            "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
          ).optional(),
          paragraph: z.object({
            bullet: z.object({
              listId: z.string().describe(
                "The ID of the list this paragraph belongs to.",
              ).optional(),
              nestingLevel: z.number().int().describe(
                "The nesting level of this paragraph in the list.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            }).describe("Describes the bullet of a paragraph.").optional(),
            elements: z.array(z.object({
              autoText: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                type: z.enum(["TYPE_UNSPECIFIED", "PAGE_NUMBER", "PAGE_COUNT"])
                  .describe("The type of this auto text.").optional(),
              }).describe(
                "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
              ).optional(),
              columnBreak: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
              ).optional(),
              dateElement: z.object({
                dateElementProperties: z.object({
                  dateFormat: z.enum([
                    "DATE_FORMAT_UNSPECIFIED",
                    "DATE_FORMAT_CUSTOM",
                    "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                    "DATE_FORMAT_MONTH_DAY_FULL",
                    "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                    "DATE_FORMAT_ISO8601",
                  ]).describe(
                    "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                  ).optional(),
                  displayText: z.string().describe(
                    "Output only. Indicates how the DateElement is displayed in the document.",
                  ).optional(),
                  locale: z.string().describe(
                    "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                  ).optional(),
                  timeFormat: z.enum([
                    "TIME_FORMAT_UNSPECIFIED",
                    "TIME_FORMAT_DISABLED",
                    "TIME_FORMAT_HOUR_MINUTE",
                    "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                  ]).describe(
                    "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                  ).optional(),
                  timeZoneId: z.string().describe(
                    "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                  ).optional(),
                  timestamp: z.string().describe(
                    "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                  ).optional(),
                }).describe("Properties of a DateElement.").optional(),
                dateId: z.string().describe(
                  "Output only. The unique ID of this date.",
                ).optional(),
                suggestedDateElementPropertiesChanges: z.record(
                  z.string(),
                  z.object({
                    dateElementProperties: z.object({
                      dateFormat: z.enum([
                        "DATE_FORMAT_UNSPECIFIED",
                        "DATE_FORMAT_CUSTOM",
                        "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                        "DATE_FORMAT_MONTH_DAY_FULL",
                        "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                        "DATE_FORMAT_ISO8601",
                      ]).describe(
                        "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                      ).optional(),
                      displayText: z.string().describe(
                        "Output only. Indicates how the DateElement is displayed in the document.",
                      ).optional(),
                      locale: z.string().describe(
                        "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                      ).optional(),
                      timeFormat: z.enum([
                        "TIME_FORMAT_UNSPECIFIED",
                        "TIME_FORMAT_DISABLED",
                        "TIME_FORMAT_HOUR_MINUTE",
                        "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                      ]).describe(
                        "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                      ).optional(),
                      timeZoneId: z.string().describe(
                        "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                      ).optional(),
                      timestamp: z.string().describe(
                        "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                      ).optional(),
                    }).describe("Properties of a DateElement.").optional(),
                    dateElementPropertiesSuggestionState: z.object({
                      dateFormatSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to date_format.",
                      ).optional(),
                      localeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to locale.",
                      ).optional(),
                      timeFormatSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to time_format.",
                      ).optional(),
                      timeZoneIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to time_zone_id.",
                      ).optional(),
                      timestampSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to timestamp.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested changes to the date element properties, keyed by suggestion ID.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A date instance mentioned in a document.")
                .optional(),
              endIndex: z.number().int().describe(
                "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
              ).optional(),
              equation: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
              }).describe("A ParagraphElement representing an equation.")
                .optional(),
              footnoteReference: z.object({
                footnoteId: z.string().describe(
                  "The ID of the footnote that contains the content of this footnote reference.",
                ).optional(),
                footnoteNumber: z.string().describe(
                  "The rendered number of this footnote.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
              ).optional(),
              horizontalRule: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A ParagraphElement representing a horizontal line.")
                .optional(),
              inlineObjectElement: z.object({
                inlineObjectId: z.string().describe(
                  "The ID of the InlineObject this element contains.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("A ParagraphElement that contains an InlineObject.")
                .optional(),
              pageBreak: z.object({
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
              ).optional(),
              person: z.object({
                personId: z.string().describe(
                  "Output only. The unique ID of this link.",
                ).optional(),
                personProperties: z.object({
                  email: z.string().describe(
                    "The email address linked to this Person. This field is always present.",
                  ).optional(),
                  name: z.string().describe(
                    "The name of the person if it's displayed in the link text instead of the person's email address.",
                  ).optional(),
                }).describe("Properties specific to a linked Person.")
                  .optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this Person, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
              ).optional(),
              richLink: z.object({
                richLinkId: z.string().describe(
                  "Output only. The ID of this link.",
                ).optional(),
                richLinkProperties: z.object({
                  mimeType: z.string().describe(
                    "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                  ).optional(),
                  title: z.string().describe(
                    "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                  ).optional(),
                  uri: z.string().describe(
                    "The URI to the RichLink. This is always present.",
                  ).optional(),
                }).describe("Properties specific to a RichLink.").optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this paragraph element, in UTF-16 code units.",
              ).optional(),
              textRun: z.object({
                content: z.string().describe(
                  "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTextStyleChanges: z.record(
                  z.string(),
                  z.object({
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested text style changes to this run, keyed by suggestion ID.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe(
                "A ParagraphElement that represents a run of text that all has the same styling.",
              ).optional(),
            })).describe(
              "The content of the paragraph, broken down into its component parts.",
            ).optional(),
            paragraphStyle: z.object({
              alignment: z.enum([
                "ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
                "JUSTIFIED",
              ]).describe("The text alignment for this paragraph.").optional(),
              avoidWidowAndOrphan: z.boolean().describe(
                "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
              ).optional(),
              borderBetween: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderBottom: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderLeft: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderRight: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              borderTop: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                padding: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around a paragraph.").optional(),
              direction: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
              ).optional(),
              headingId: z.string().describe(
                "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
              ).optional(),
              indentEnd: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              keepLinesTogether: z.boolean().describe(
                "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              keepWithNext: z.boolean().describe(
                "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
              ).optional(),
              lineSpacing: z.number().describe(
                "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
              ).optional(),
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
              ).optional(),
              pageBreakBefore: z.boolean().describe(
                "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
              ).optional(),
              shading: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
              }).describe("The shading of a paragraph.").optional(),
              spaceAbove: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spaceBelow: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              spacingMode: z.enum([
                "SPACING_MODE_UNSPECIFIED",
                "NEVER_COLLAPSE",
                "COLLAPSE_LISTS",
              ]).describe("The spacing mode for the paragraph.").optional(),
              tabStops: z.array(z.object({
                alignment: z.enum([
                  "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                ]).describe(
                  "The alignment of this tab stop. If unset, the value defaults to START.",
                ).optional(),
                offset: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
              ).optional(),
            }).describe(
              "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
            ).optional(),
            positionedObjectIds: z.array(z.string()).describe(
              "The IDs of the positioned objects tethered to this paragraph.",
            ).optional(),
            suggestedBulletChanges: z.record(
              z.string(),
              z.object({
                bullet: z.object({
                  listId: z.string().describe(
                    "The ID of the list this paragraph belongs to.",
                  ).optional(),
                  nestingLevel: z.number().int().describe(
                    "The nesting level of this paragraph in the list.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("Describes the bullet of a paragraph.").optional(),
                bulletSuggestionState: z.object({
                  listIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the list_id.",
                  ).optional(),
                  nestingLevelSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the nesting_level.",
                  ).optional(),
                  textStyleSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to background_color.",
                    ).optional(),
                    baselineOffsetSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to baseline_offset.",
                    ).optional(),
                    boldSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to bold.",
                    ).optional(),
                    fontSizeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to font_size.",
                    ).optional(),
                    foregroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to foreground_color.",
                    ).optional(),
                    italicSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to italic.",
                    ).optional(),
                    linkSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to link.",
                    ).optional(),
                    smallCapsSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to small_caps.",
                    ).optional(),
                    strikethroughSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to strikethrough.",
                    ).optional(),
                    underlineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to underline.",
                    ).optional(),
                    weightedFontFamilySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to weighted_font_family.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe("The suggested changes to this paragraph's bullet.")
              .optional(),
            suggestedParagraphStyleChanges: z.record(
              z.string(),
              z.object({
                paragraphStyle: z.object({
                  alignment: z.enum([
                    "ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                    "JUSTIFIED",
                  ]).describe("The text alignment for this paragraph.")
                    .optional(),
                  avoidWidowAndOrphan: z.boolean().describe(
                    "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                  ).optional(),
                  borderBetween: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    padding: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around a paragraph.").optional(),
                  direction: z.enum([
                    "CONTENT_DIRECTION_UNSPECIFIED",
                    "LEFT_TO_RIGHT",
                    "RIGHT_TO_LEFT",
                  ]).describe(
                    "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                  ).optional(),
                  headingId: z.string().describe(
                    "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                  ).optional(),
                  indentEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentFirstLine: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentStart: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  keepLinesTogether: z.boolean().describe(
                    "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                  ).optional(),
                  keepWithNext: z.boolean().describe(
                    "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                  ).optional(),
                  lineSpacing: z.number().describe(
                    "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                  ).optional(),
                  namedStyleType: z.enum([
                    "NAMED_STYLE_TYPE_UNSPECIFIED",
                    "NORMAL_TEXT",
                    "TITLE",
                    "SUBTITLE",
                    "HEADING_1",
                    "HEADING_2",
                    "HEADING_3",
                    "HEADING_4",
                    "HEADING_5",
                    "HEADING_6",
                  ]).describe(
                    "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                  ).optional(),
                  pageBreakBefore: z.boolean().describe(
                    "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                  ).optional(),
                  shading: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                  }).describe("The shading of a paragraph.").optional(),
                  spaceAbove: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  spaceBelow: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  spacingMode: z.enum([
                    "SPACING_MODE_UNSPECIFIED",
                    "NEVER_COLLAPSE",
                    "COLLAPSE_LISTS",
                  ]).describe("The spacing mode for the paragraph.").optional(),
                  tabStops: z.array(z.object({
                    alignment: z.enum([
                      "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                    ]).describe(
                      "The alignment of this tab stop. If unset, the value defaults to START.",
                    ).optional(),
                    offset: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  })).describe(
                    "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                  ).optional(),
                }).describe(
                  "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
                paragraphStyleSuggestionState: z.object({
                  alignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to alignment.",
                  ).optional(),
                  avoidWidowAndOrphanSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                  ).optional(),
                  borderBetweenSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_between.",
                  ).optional(),
                  borderBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_bottom.",
                  ).optional(),
                  borderLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_left.",
                  ).optional(),
                  borderRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_right.",
                  ).optional(),
                  borderTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to border_top.",
                  ).optional(),
                  directionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to direction.",
                  ).optional(),
                  headingIdSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to heading_id.",
                  ).optional(),
                  indentEndSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_end.",
                  ).optional(),
                  indentFirstLineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_first_line.",
                  ).optional(),
                  indentStartSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_start.",
                  ).optional(),
                  keepLinesTogetherSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to keep_lines_together.",
                  ).optional(),
                  keepWithNextSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to keep_with_next.",
                  ).optional(),
                  lineSpacingSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to line_spacing.",
                  ).optional(),
                  namedStyleTypeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to named_style_type.",
                  ).optional(),
                  pageBreakBeforeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to page_break_before.",
                  ).optional(),
                  shadingSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the Shading.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  spaceAboveSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to space_above.",
                  ).optional(),
                  spaceBelowSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to space_below.",
                  ).optional(),
                  spacingModeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to spacing_mode.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }),
            ).describe(
              "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
            ).optional(),
            suggestedPositionedObjectIds: z.record(
              z.string(),
              z.object({
                objectIds: z.array(z.string()).describe("The object IDs.")
                  .optional(),
              }),
            ).describe(
              "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
            ).optional(),
          }).describe(
            "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
          ).optional(),
          sectionBreak: z.object({
            sectionStyle: z.object({
              columnProperties: z.array(z.object({
                paddingEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              })).describe(
                "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
              ).optional(),
              columnSeparatorStyle: z.enum([
                "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                "NONE",
                "BETWEEN_EACH_COLUMN",
              ]).describe(
                "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              contentDirection: z.enum([
                "CONTENT_DIRECTION_UNSPECIFIED",
                "LEFT_TO_RIGHT",
                "RIGHT_TO_LEFT",
              ]).describe(
                "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              defaultFooterId: z.string().describe(
                "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              defaultHeaderId: z.string().describe(
                "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              evenPageFooterId: z.string().describe(
                "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              evenPageHeaderId: z.string().describe(
                "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              firstPageFooterId: z.string().describe(
                "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              firstPageHeaderId: z.string().describe(
                "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
              ).optional(),
              flipPageOrientation: z.boolean().describe(
                "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginFooter: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginHeader: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              pageNumberStart: z.number().int().describe(
                "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
              sectionType: z.enum([
                "SECTION_TYPE_UNSPECIFIED",
                "CONTINUOUS",
                "NEXT_PAGE",
              ]).describe("Output only. The type of section.").optional(),
              useFirstPageHeaderFooter: z.boolean().describe(
                "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
              ).optional(),
            }).describe("The styling that applies to a section.").optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe(
            "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
          ).optional(),
          startIndex: z.number().int().describe(
            "The zero-based start index of this structural element, in UTF-16 code units.",
          ).optional(),
          table: z.object({
            columns: z.number().int().describe(
              "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
            ).optional(),
            rows: z.number().int().describe("Number of rows in the table.")
              .optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
            tableRows: z.array(z.object({
              endIndex: z.number().int().describe(
                "The zero-based end index of this row, exclusive, in UTF-16 code units.",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this row, in UTF-16 code units.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              suggestedTableRowStyleChanges: z.record(
                z.string(),
                z.object({
                  tableRowStyle: z.object({
                    minRowHeight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    preventOverflow: z.boolean().describe(
                      "Whether the row cannot overflow across page or column boundaries.",
                    ).optional(),
                    tableHeader: z.boolean().describe(
                      "Whether the row is a table header.",
                    ).optional(),
                  }).describe("Styles that apply to a table row.").optional(),
                  tableRowStyleSuggestionState: z.object({
                    minRowHeightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to min_row_height.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested style changes to this row, keyed by suggestion ID.",
              ).optional(),
              tableCells: z.array(z.object({
                content: z.array(z.string()).describe(
                  "The content of the cell.",
                ).optional(),
                endIndex: z.number().int().describe(
                  "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this cell, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableCellStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableCellStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      borderBottom: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderLeft: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderRight: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      borderTop: z.object({
                        color: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        dashStyle: z.enum([
                          "DASH_STYLE_UNSPECIFIED",
                          "SOLID",
                          "DOT",
                          "DASH",
                        ]).describe("The dash style of the border.").optional(),
                        width: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                      }).describe(
                        "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                      ).optional(),
                      columnSpan: z.number().int().describe(
                        "The column span of the cell. This property is read-only.",
                      ).optional(),
                      contentAlignment: z.enum([
                        "CONTENT_ALIGNMENT_UNSPECIFIED",
                        "CONTENT_ALIGNMENT_UNSUPPORTED",
                        "TOP",
                        "MIDDLE",
                        "BOTTOM",
                      ]).describe(
                        "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                      ).optional(),
                      paddingBottom: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingLeft: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingRight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      paddingTop: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      rowSpan: z.number().int().describe(
                        "The row span of the cell. This property is read-only.",
                      ).optional(),
                    }).describe(
                      "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                    ).optional(),
                    tableCellStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      borderBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_bottom.",
                      ).optional(),
                      borderLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_left.",
                      ).optional(),
                      borderRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_right.",
                      ).optional(),
                      borderTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to border_top.",
                      ).optional(),
                      columnSpanSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to column_span.",
                      ).optional(),
                      contentAlignmentSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to content_alignment.",
                      ).optional(),
                      paddingBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_bottom.",
                      ).optional(),
                      paddingLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_left.",
                      ).optional(),
                      paddingRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_right.",
                      ).optional(),
                      paddingTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to padding_top.",
                      ).optional(),
                      rowSpanSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to row_span.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested changes to the table cell style, keyed by suggestion ID.",
                ).optional(),
                tableCellStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  borderBottom: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderLeft: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderRight: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  borderTop: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe(
                    "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                  ).optional(),
                  columnSpan: z.number().int().describe(
                    "The column span of the cell. This property is read-only.",
                  ).optional(),
                  contentAlignment: z.enum([
                    "CONTENT_ALIGNMENT_UNSPECIFIED",
                    "CONTENT_ALIGNMENT_UNSUPPORTED",
                    "TOP",
                    "MIDDLE",
                    "BOTTOM",
                  ]).describe(
                    "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                  ).optional(),
                  paddingBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  paddingTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  rowSpan: z.number().int().describe(
                    "The row span of the cell. This property is read-only.",
                  ).optional(),
                }).describe(
                  "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                ).optional(),
              })).describe(
                "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
              ).optional(),
              tableRowStyle: z.object({
                minRowHeight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                preventOverflow: z.boolean().describe(
                  "Whether the row cannot overflow across page or column boundaries.",
                ).optional(),
                tableHeader: z.boolean().describe(
                  "Whether the row is a table header.",
                ).optional(),
              }).describe("Styles that apply to a table row.").optional(),
            })).describe("The contents and style of each row.").optional(),
            tableStyle: z.object({
              tableColumnProperties: z.array(z.object({
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                widthType: z.enum([
                  "WIDTH_TYPE_UNSPECIFIED",
                  "EVENLY_DISTRIBUTED",
                  "FIXED_WIDTH",
                ]).describe("The width type of the column.").optional(),
              })).describe(
                "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
              ).optional(),
            }).describe("Styles that apply to a table.").optional(),
          }).describe("A StructuralElement representing a table.").optional(),
          tableOfContents: z.object({
            content: z.array(z.string()).describe(
              "The content of the table of contents.",
            ).optional(),
            suggestedDeletionIds: z.array(z.string()).describe(
              "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
            ).optional(),
            suggestedInsertionIds: z.array(z.string()).describe(
              "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
            ).optional(),
          }).describe("A StructuralElement representing a table of contents.")
            .optional(),
        })).describe(
          "The contents of the body. The indexes for the body's content begin at zero.",
        ).optional(),
      }).describe(
        "The document body. The body typically contains the full document contents except for headers, footers, and footnotes.",
      ).optional(),
      documentStyle: z.object({
        background: z.object({
          color: z.object({
            color: z.object({
              rgbColor: z.object({
                blue: z.number().describe(
                  "The blue component of the color, from 0.0 to 1.0.",
                ).optional(),
                green: z.number().describe(
                  "The green component of the color, from 0.0 to 1.0.",
                ).optional(),
                red: z.number().describe(
                  "The red component of the color, from 0.0 to 1.0.",
                ).optional(),
              }).describe("An RGB color.").optional(),
            }).describe("A solid color.").optional(),
          }).describe(
            "A color that can either be fully opaque or fully transparent.",
          ).optional(),
        }).describe("Represents the background of a document.").optional(),
        defaultFooterId: z.string().describe(
          "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        defaultHeaderId: z.string().describe(
          "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        documentFormat: z.object({
          documentMode: z.enum([
            "DOCUMENT_MODE_UNSPECIFIED",
            "PAGES",
            "PAGELESS",
          ]).describe("Whether the document has pages or is pageless.")
            .optional(),
        }).describe("Represents document-level format settings.").optional(),
        evenPageFooterId: z.string().describe(
          "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        evenPageHeaderId: z.string().describe(
          "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        firstPageFooterId: z.string().describe(
          "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        firstPageHeaderId: z.string().describe(
          "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        flipPageOrientation: z.boolean().describe(
          "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        marginBottom: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginFooter: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginHeader: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginLeft: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginRight: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        marginTop: z.object({
          magnitude: z.number().describe("The magnitude.").optional(),
          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
            "The units for magnitude.",
          ).optional(),
        }).describe("A magnitude in a single direction in the specified units.")
          .optional(),
        pageNumberStart: z.number().int().describe(
          "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        pageSize: z.object({
          height: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
          width: z.object({
            magnitude: z.number().describe("The magnitude.").optional(),
            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
              "The units for magnitude.",
            ).optional(),
          }).describe(
            "A magnitude in a single direction in the specified units.",
          ).optional(),
        }).describe("A width and height.").optional(),
        useCustomHeaderFooterMargins: z.boolean().describe(
          "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
        ).optional(),
        useEvenPageHeaderFooter: z.boolean().describe(
          "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
        useFirstPageHeaderFooter: z.boolean().describe(
          "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
        ).optional(),
      }).describe("The style of the document.").optional(),
      footers: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the footer. The indexes for a footer's content begin at zero.",
          ).optional(),
          footerId: z.string().describe("The ID of the footer.").optional(),
        }),
      ).describe("The footers in the document tab, keyed by footer ID.")
        .optional(),
      footnotes: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the footnote. The indexes for a footnote's content begin at zero.",
          ).optional(),
          footnoteId: z.string().describe("The ID of the footnote.").optional(),
        }),
      ).describe("The footnotes in the document tab, keyed by footnote ID.")
        .optional(),
      headers: z.record(
        z.string(),
        z.object({
          content: z.array(z.object({
            endIndex: z.number().int().describe(
              "The zero-based end index of this structural element, exclusive, in UTF-16 code units.",
            ).optional(),
            paragraph: z.object({
              bullet: z.object({
                listId: z.string().describe(
                  "The ID of the list this paragraph belongs to.",
                ).optional(),
                nestingLevel: z.number().int().describe(
                  "The nesting level of this paragraph in the list.",
                ).optional(),
                textStyle: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  baselineOffset: z.enum([
                    "BASELINE_OFFSET_UNSPECIFIED",
                    "NONE",
                    "SUPERSCRIPT",
                    "SUBSCRIPT",
                  ]).describe(
                    "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                  ).optional(),
                  bold: z.boolean().describe(
                    "Whether or not the text is rendered as bold.",
                  ).optional(),
                  fontSize: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  foregroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  italic: z.boolean().describe(
                    "Whether or not the text is italicized.",
                  ).optional(),
                  link: z.object({
                    bookmark: z.object({
                      id: z.string().describe(
                        "The ID of a bookmark in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this bookmark.",
                      ).optional(),
                    }).describe("A reference to a bookmark in this document.")
                      .optional(),
                    bookmarkId: z.string().describe(
                      "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    heading: z.object({
                      id: z.string().describe(
                        "The ID of a heading in this document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of the tab containing this heading.",
                      ).optional(),
                    }).describe("A reference to a heading in this document.")
                      .optional(),
                    headingId: z.string().describe(
                      "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of a tab in this document.",
                    ).optional(),
                    url: z.string().describe("An external URL.").optional(),
                  }).describe(
                    "A reference to another portion of a document or an external URL resource.",
                  ).optional(),
                  smallCaps: z.boolean().describe(
                    "Whether or not the text is in small capital letters.",
                  ).optional(),
                  strikethrough: z.boolean().describe(
                    "Whether or not the text is struck through.",
                  ).optional(),
                  underline: z.boolean().describe(
                    "Whether or not the text is underlined.",
                  ).optional(),
                  weightedFontFamily: z.object({
                    fontFamily: z.string().describe(
                      "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                    ).optional(),
                    weight: z.number().int().describe(
                      "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                    ).optional(),
                  }).describe("Represents a font family and weight of text.")
                    .optional(),
                }).describe(
                  "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                ).optional(),
              }).describe("Describes the bullet of a paragraph.").optional(),
              elements: z.array(z.object({
                autoText: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this AutoText, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  type: z.enum([
                    "TYPE_UNSPECIFIED",
                    "PAGE_NUMBER",
                    "PAGE_COUNT",
                  ]).describe("The type of this auto text.").optional(),
                }).describe(
                  "A ParagraphElement representing a spot in the text that's dynamically replaced with content that can change over time, like a page number.",
                ).optional(),
                columnBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A ColumnBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this ColumnBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a column break. A column break makes the subsequent text start at the top of the next column.",
                ).optional(),
                dateElement: z.object({
                  dateElementProperties: z.object({
                    dateFormat: z.enum([
                      "DATE_FORMAT_UNSPECIFIED",
                      "DATE_FORMAT_CUSTOM",
                      "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                      "DATE_FORMAT_MONTH_DAY_FULL",
                      "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                      "DATE_FORMAT_ISO8601",
                    ]).describe(
                      "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                    ).optional(),
                    displayText: z.string().describe(
                      "Output only. Indicates how the DateElement is displayed in the document.",
                    ).optional(),
                    locale: z.string().describe(
                      "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                    ).optional(),
                    timeFormat: z.enum([
                      "TIME_FORMAT_UNSPECIFIED",
                      "TIME_FORMAT_DISABLED",
                      "TIME_FORMAT_HOUR_MINUTE",
                      "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                    ]).describe(
                      "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                    ).optional(),
                    timeZoneId: z.string().describe(
                      "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                    ).optional(),
                    timestamp: z.string().describe(
                      "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                    ).optional(),
                  }).describe("Properties of a DateElement.").optional(),
                  dateId: z.string().describe(
                    "Output only. The unique ID of this date.",
                  ).optional(),
                  suggestedDateElementPropertiesChanges: z.record(
                    z.string(),
                    z.object({
                      dateElementProperties: z.object({
                        dateFormat: z.enum([
                          "DATE_FORMAT_UNSPECIFIED",
                          "DATE_FORMAT_CUSTOM",
                          "DATE_FORMAT_MONTH_DAY_ABBREVIATED",
                          "DATE_FORMAT_MONTH_DAY_FULL",
                          "DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED",
                          "DATE_FORMAT_ISO8601",
                        ]).describe(
                          "Determines how the date part of the DateElement will be displayed in the document. If unset, the default value is DATE_FORMAT_MONTH_DAY_YEAR_ABBREVIATED, indicating the DateElement will be formatted as `MMM d, y` in `en`, or locale specific equivalent.",
                        ).optional(),
                        displayText: z.string().describe(
                          "Output only. Indicates how the DateElement is displayed in the document.",
                        ).optional(),
                        locale: z.string().describe(
                          "The language code of the DateElement. For example, `en`. If unset, the default locale is `en`. Limited to the following locales: `af`, `am`, `ar`, `as`, `az`, `be`, `bg`, `bn`, `ca`, `cs`, `da`, `de`, `el`, `en`, `en-CA`, `en-GB`, `es`, `es-419`, `et`, `eu`, `fa`, `fi`, `fil`, `fr`, `fr-CA`, `gl`, `gu`, `hi`, `hr`, `hu`, `hy`, `id`, `is`, `it`, `iw`, `ja`, `ka`, `kk`, `km`, `kn`, `ko`, `lo`, `lt`, `lv`, `mk`, `ml`, `mn`, `mr`, `ms`, `ne`, `nl`, `no`, `or`, `pa`, `pl`, `pt-BR`, `pt-PT`, `ro`, `ru`, `si`, `sk`, `sl`, `sq`, `sr`, `sv`, `sw`, `ta`, `te`, `th`, `tr`, `uk`, `ur`, `uz`, `vi`, `zh-CN`, `zh-HK`, `zh-TW`, `zu`, `cy`, `my`.",
                        ).optional(),
                        timeFormat: z.enum([
                          "TIME_FORMAT_UNSPECIFIED",
                          "TIME_FORMAT_DISABLED",
                          "TIME_FORMAT_HOUR_MINUTE",
                          "TIME_FORMAT_HOUR_MINUTE_TIMEZONE",
                        ]).describe(
                          "Determines how the time part of the DateElement will be displayed in the document. If unset, the default value is TIME_FORMAT_DISABLED, indicating no time should be shown.",
                        ).optional(),
                        timeZoneId: z.string().describe(
                          "The time zone of the DateElement, as defined by the Unicode Common Locale Data Repository (CLDR) project. For example, `America/New York`. If unset, the default time zone is `etc/UTC`.",
                        ).optional(),
                        timestamp: z.string().describe(
                          "The point in time to represent, in seconds and nanoseconds since Unix epoch: January 1, 1970 at midnight UTC. Timestamp is expected to be in UTC. If time_zone_id is set, the timestamp is adjusted according to the time zone. For example, a timestamp of `18000` with a date format of `DATE_FORMAT_ISO8601` and time format of `TIME_FORMAT_HOUR_MINUTE` would be displayed as `1970-01-01 5:00 AM`. A timestamp of `18000` with date format of `DATE_FORMAT_8SO8601`, time format of `TIME_FORMAT_HOUR_MINUTE`, and time zone set to `America/New_York` will instead be `1970-01-01 12:00 AM`.",
                        ).optional(),
                      }).describe("Properties of a DateElement.").optional(),
                      dateElementPropertiesSuggestionState: z.object({
                        dateFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to date_format.",
                        ).optional(),
                        localeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to locale.",
                        ).optional(),
                        timeFormatSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_format.",
                        ).optional(),
                        timeZoneIdSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to time_zone_id.",
                        ).optional(),
                        timestampSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to timestamp.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base DateElementProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the date element properties, keyed by suggestion ID.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this date from the document. A DateElement might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this date isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this date into the document. A DateElement might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this date isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this DateElement, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A date instance mentioned in a document.")
                  .optional(),
                endIndex: z.number().int().describe(
                  "The zero-base end index of this paragraph element, exclusive, in UTF-16 code units.",
                ).optional(),
                equation: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An Equation may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                }).describe("A ParagraphElement representing an equation.")
                  .optional(),
                footnoteReference: z.object({
                  footnoteId: z.string().describe(
                    "The ID of the footnote that contains the content of this footnote reference.",
                  ).optional(),
                  footnoteNumber: z.string().describe(
                    "The rendered number of this footnote.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A FootnoteReference may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this FootnoteReference, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a footnote reference. A footnote reference is the inline content rendered with a number and is used to identify the footnote.",
                ).optional(),
                horizontalRule: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A HorizontalRule may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this HorizontalRule, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a horizontal line.",
                ).optional(),
                inlineObjectElement: z.object({
                  inlineObjectId: z.string().describe(
                    "The ID of the InlineObject this element contains.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. An InlineObjectElement may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this InlineObject, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe("A ParagraphElement that contains an InlineObject.")
                  .optional(),
                pageBreak: z.object({
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A PageBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this PageBreak, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement representing a page break. A page break makes the subsequent text start at the top of the next page.",
                ).optional(),
                person: z.object({
                  personId: z.string().describe(
                    "Output only. The unique ID of this link.",
                  ).optional(),
                  personProperties: z.object({
                    email: z.string().describe(
                      "The email address linked to this Person. This field is always present.",
                    ).optional(),
                    name: z.string().describe(
                      "The name of the person if it's displayed in the link text instead of the person's email address.",
                    ).optional(),
                  }).describe("Properties specific to a linked Person.")
                    .optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this person link from the document. A Person might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this person link into the document. A Person might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this Person, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A person or email address mentioned in a document. These mentions behave as a single, immutable element containing the person's name or email address.",
                ).optional(),
                richLink: z.object({
                  richLinkId: z.string().describe(
                    "Output only. The ID of this link.",
                  ).optional(),
                  richLinkProperties: z.object({
                    mimeType: z.string().describe(
                      "The [MIME type](https://developers.google.com/drive/api/v3/mime-types) of the RichLink, if there's one (for example, when it's a file in Drive).",
                    ).optional(),
                    title: z.string().describe(
                      "The title of the RichLink as displayed in the link. This title matches the title of the linked resource at the time of the insertion or last update of the link. This field is always present.",
                    ).optional(),
                    uri: z.string().describe(
                      "The URI to the RichLink. This is always present.",
                    ).optional(),
                  }).describe("Properties specific to a RichLink.").optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this RichLink, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this paragraph element, in UTF-16 code units.",
                ).optional(),
                textRun: z.object({
                  content: z.string().describe(
                    "The text of this run. Any non-text elements in the run are replaced with the Unicode character U+E907.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TextRun may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTextStyleChanges: z.record(
                    z.string(),
                    z.object({
                      textStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        baselineOffset: z.enum([
                          "BASELINE_OFFSET_UNSPECIFIED",
                          "NONE",
                          "SUPERSCRIPT",
                          "SUBSCRIPT",
                        ]).describe(
                          "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                        ).optional(),
                        bold: z.boolean().describe(
                          "Whether or not the text is rendered as bold.",
                        ).optional(),
                        fontSize: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        foregroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        italic: z.boolean().describe(
                          "Whether or not the text is italicized.",
                        ).optional(),
                        link: z.object({
                          bookmark: z.object({
                            id: z.string().describe(
                              "The ID of a bookmark in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this bookmark.",
                            ).optional(),
                          }).describe(
                            "A reference to a bookmark in this document.",
                          ).optional(),
                          bookmarkId: z.string().describe(
                            "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          heading: z.object({
                            id: z.string().describe(
                              "The ID of a heading in this document.",
                            ).optional(),
                            tabId: z.string().describe(
                              "The ID of the tab containing this heading.",
                            ).optional(),
                          }).describe(
                            "A reference to a heading in this document.",
                          ).optional(),
                          headingId: z.string().describe(
                            "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of a tab in this document.",
                          ).optional(),
                          url: z.string().describe("An external URL.")
                            .optional(),
                        }).describe(
                          "A reference to another portion of a document or an external URL resource.",
                        ).optional(),
                        smallCaps: z.boolean().describe(
                          "Whether or not the text is in small capital letters.",
                        ).optional(),
                        strikethrough: z.boolean().describe(
                          "Whether or not the text is struck through.",
                        ).optional(),
                        underline: z.boolean().describe(
                          "Whether or not the text is underlined.",
                        ).optional(),
                        weightedFontFamily: z.object({
                          fontFamily: z.string().describe(
                            "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                          ).optional(),
                          weight: z.number().int().describe(
                            "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                          ).optional(),
                        }).describe(
                          "Represents a font family and weight of text.",
                        ).optional(),
                      }).describe(
                        "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                      ).optional(),
                      textStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        baselineOffsetSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to baseline_offset.",
                        ).optional(),
                        boldSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to bold.",
                        ).optional(),
                        fontSizeSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to font_size.",
                        ).optional(),
                        foregroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to foreground_color.",
                        ).optional(),
                        italicSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to italic.",
                        ).optional(),
                        linkSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to link.",
                        ).optional(),
                        smallCapsSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to small_caps.",
                        ).optional(),
                        strikethroughSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to strikethrough.",
                        ).optional(),
                        underlineSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to underline.",
                        ).optional(),
                        weightedFontFamilySuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to weighted_font_family.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested text style changes to this run, keyed by suggestion ID.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                }).describe(
                  "A ParagraphElement that represents a run of text that all has the same styling.",
                ).optional(),
              })).describe(
                "The content of the paragraph, broken down into its component parts.",
              ).optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              positionedObjectIds: z.array(z.string()).describe(
                "The IDs of the positioned objects tethered to this paragraph.",
              ).optional(),
              suggestedBulletChanges: z.record(
                z.string(),
                z.object({
                  bullet: z.object({
                    listId: z.string().describe(
                      "The ID of the list this paragraph belongs to.",
                    ).optional(),
                    nestingLevel: z.number().int().describe(
                      "The nesting level of this paragraph in the list.",
                    ).optional(),
                    textStyle: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      baselineOffset: z.enum([
                        "BASELINE_OFFSET_UNSPECIFIED",
                        "NONE",
                        "SUPERSCRIPT",
                        "SUBSCRIPT",
                      ]).describe(
                        "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                      ).optional(),
                      bold: z.boolean().describe(
                        "Whether or not the text is rendered as bold.",
                      ).optional(),
                      fontSize: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      foregroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      italic: z.boolean().describe(
                        "Whether or not the text is italicized.",
                      ).optional(),
                      link: z.object({
                        bookmark: z.object({
                          id: z.string().describe(
                            "The ID of a bookmark in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this bookmark.",
                          ).optional(),
                        }).describe(
                          "A reference to a bookmark in this document.",
                        ).optional(),
                        bookmarkId: z.string().describe(
                          "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        heading: z.object({
                          id: z.string().describe(
                            "The ID of a heading in this document.",
                          ).optional(),
                          tabId: z.string().describe(
                            "The ID of the tab containing this heading.",
                          ).optional(),
                        }).describe(
                          "A reference to a heading in this document.",
                        ).optional(),
                        headingId: z.string().describe(
                          "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of a tab in this document.",
                        ).optional(),
                        url: z.string().describe("An external URL.").optional(),
                      }).describe(
                        "A reference to another portion of a document or an external URL resource.",
                      ).optional(),
                      smallCaps: z.boolean().describe(
                        "Whether or not the text is in small capital letters.",
                      ).optional(),
                      strikethrough: z.boolean().describe(
                        "Whether or not the text is struck through.",
                      ).optional(),
                      underline: z.boolean().describe(
                        "Whether or not the text is underlined.",
                      ).optional(),
                      weightedFontFamily: z.object({
                        fontFamily: z.string().describe(
                          "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                        ).optional(),
                        weight: z.number().int().describe(
                          "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                        ).optional(),
                      }).describe(
                        "Represents a font family and weight of text.",
                      ).optional(),
                    }).describe(
                      "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                    ).optional(),
                  }).describe("Describes the bullet of a paragraph.")
                    .optional(),
                  bulletSuggestionState: z.object({
                    listIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the list_id.",
                    ).optional(),
                    nestingLevelSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to the nesting_level.",
                    ).optional(),
                    textStyleSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to background_color.",
                      ).optional(),
                      baselineOffsetSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to baseline_offset.",
                      ).optional(),
                      boldSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to bold.",
                      ).optional(),
                      fontSizeSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to font_size.",
                      ).optional(),
                      foregroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to foreground_color.",
                      ).optional(),
                      italicSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to italic.",
                      ).optional(),
                      linkSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to link.",
                      ).optional(),
                      smallCapsSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to small_caps.",
                      ).optional(),
                      strikethroughSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to strikethrough.",
                      ).optional(),
                      underlineSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to underline.",
                      ).optional(),
                      weightedFontFamilySuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to weighted_font_family.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Bullet have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe("The suggested changes to this paragraph's bullet.")
                .optional(),
              suggestedParagraphStyleChanges: z.record(
                z.string(),
                z.object({
                  paragraphStyle: z.object({
                    alignment: z.enum([
                      "ALIGNMENT_UNSPECIFIED",
                      "START",
                      "CENTER",
                      "END",
                      "JUSTIFIED",
                    ]).describe("The text alignment for this paragraph.")
                      .optional(),
                    avoidWidowAndOrphan: z.boolean().describe(
                      "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                    ).optional(),
                    borderBetween: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      padding: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe("A border around a paragraph.").optional(),
                    direction: z.enum([
                      "CONTENT_DIRECTION_UNSPECIFIED",
                      "LEFT_TO_RIGHT",
                      "RIGHT_TO_LEFT",
                    ]).describe(
                      "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                    ).optional(),
                    headingId: z.string().describe(
                      "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                    ).optional(),
                    indentEnd: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentFirstLine: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    indentStart: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    keepLinesTogether: z.boolean().describe(
                      "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    keepWithNext: z.boolean().describe(
                      "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                    ).optional(),
                    lineSpacing: z.number().describe(
                      "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                    ).optional(),
                    namedStyleType: z.enum([
                      "NAMED_STYLE_TYPE_UNSPECIFIED",
                      "NORMAL_TEXT",
                      "TITLE",
                      "SUBTITLE",
                      "HEADING_1",
                      "HEADING_2",
                      "HEADING_3",
                      "HEADING_4",
                      "HEADING_5",
                      "HEADING_6",
                    ]).describe(
                      "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                    ).optional(),
                    pageBreakBefore: z.boolean().describe(
                      "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                    ).optional(),
                    shading: z.object({
                      backgroundColor: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                    }).describe("The shading of a paragraph.").optional(),
                    spaceAbove: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spaceBelow: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    spacingMode: z.enum([
                      "SPACING_MODE_UNSPECIFIED",
                      "NEVER_COLLAPSE",
                      "COLLAPSE_LISTS",
                    ]).describe("The spacing mode for the paragraph.")
                      .optional(),
                    tabStops: z.array(z.object({
                      alignment: z.enum([
                        "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                        "START",
                        "CENTER",
                        "END",
                      ]).describe(
                        "The alignment of this tab stop. If unset, the value defaults to START.",
                      ).optional(),
                      offset: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    })).describe(
                      "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                  paragraphStyleSuggestionState: z.object({
                    alignmentSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to alignment.",
                    ).optional(),
                    avoidWidowAndOrphanSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                    ).optional(),
                    borderBetweenSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_between.",
                    ).optional(),
                    borderBottomSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_bottom.",
                    ).optional(),
                    borderLeftSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_left.",
                    ).optional(),
                    borderRightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_right.",
                    ).optional(),
                    borderTopSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to border_top.",
                    ).optional(),
                    directionSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to direction.",
                    ).optional(),
                    headingIdSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to heading_id.",
                    ).optional(),
                    indentEndSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_end.",
                    ).optional(),
                    indentFirstLineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_first_line.",
                    ).optional(),
                    indentStartSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to indent_start.",
                    ).optional(),
                    keepLinesTogetherSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_lines_together.",
                    ).optional(),
                    keepWithNextSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to keep_with_next.",
                    ).optional(),
                    lineSpacingSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to line_spacing.",
                    ).optional(),
                    namedStyleTypeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to named_style_type.",
                    ).optional(),
                    pageBreakBeforeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to page_break_before.",
                    ).optional(),
                    shadingSuggestionState: z.object({
                      backgroundColorSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to the Shading.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    spaceAboveSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_above.",
                    ).optional(),
                    spaceBelowSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to space_below.",
                    ).optional(),
                    spacingModeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to spacing_mode.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                }),
              ).describe(
                "The suggested paragraph style changes to this paragraph, keyed by suggestion ID.",
              ).optional(),
              suggestedPositionedObjectIds: z.record(
                z.string(),
                z.object({
                  objectIds: z.array(z.string()).describe("The object IDs.")
                    .optional(),
                }),
              ).describe(
                "The IDs of the positioned objects suggested to be attached to this paragraph, keyed by suggestion ID.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a paragraph. A paragraph is a range of content that's terminated with a newline character.",
            ).optional(),
            sectionBreak: z.object({
              sectionStyle: z.object({
                columnProperties: z.array(z.object({
                  paddingEnd: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "The section's columns properties. If empty, the section contains one column with the default properties in the Docs editor. A section can be updated to have no more than 3 columns. When updating this property, setting a concrete value is required. Unsetting this property will result in a 400 bad request error.",
                ).optional(),
                columnSeparatorStyle: z.enum([
                  "COLUMN_SEPARATOR_STYLE_UNSPECIFIED",
                  "NONE",
                  "BETWEEN_EACH_COLUMN",
                ]).describe(
                  "The style of column separators. This style can be set even when there's one column in the section. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                contentDirection: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The content direction of this section. If unset, the value defaults to LEFT_TO_RIGHT. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                defaultFooterId: z.string().describe(
                  "The ID of the default footer. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                defaultHeaderId: z.string().describe(
                  "The ID of the default header. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's default_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageFooterId: z.string().describe(
                  "The ID of the footer used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the footers on even pages in the section. If it is false, the footers on even pages use the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                evenPageHeaderId: z.string().describe(
                  "The ID of the header used only for even pages. If the value of DocumentStyle's use_even_page_header_footer is true, this value is used for the headers on even pages in the section. If it is false, the headers on even pages use the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's even_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageFooterId: z.string().describe(
                  "The ID of the footer used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the footer on the first page of the section. If it's false, the footer on the first page of the section uses the default_footer_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_footer_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                firstPageHeaderId: z.string().describe(
                  "The ID of the header used only for the first page of the section. If use_first_page_header_footer is true, this value is used for the header on the first page of the section. If it's false, the header on the first page of the section uses the default_header_id. If unset, the value inherits from the previous SectionBreak's SectionStyle. If the value is unset in the first SectionBreak, it inherits from DocumentStyle's first_page_header_id. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
                ).optional(),
                flipPageOrientation: z.boolean().describe(
                  "Optional. Indicates whether to flip the dimensions of DocumentStyle's page_size for this section, which allows changing the page orientation between portrait and landscape. If unset, the value inherits from DocumentStyle's flip_page_orientation. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                marginBottom: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginFooter: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginHeader: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginLeft: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginRight: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                marginTop: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                pageNumberStart: z.number().int().describe(
                  "The page number from which to start counting the number of pages for this section. If unset, page numbering continues from the previous section. If the value is unset in the first SectionBreak, refer to DocumentStyle's page_number_start. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
                sectionType: z.enum([
                  "SECTION_TYPE_UNSPECIFIED",
                  "CONTINUOUS",
                  "NEXT_PAGE",
                ]).describe("Output only. The type of section.").optional(),
                useFirstPageHeaderFooter: z.boolean().describe(
                  "Indicates whether to use the first page header / footer IDs for the first page of the section. If unset, it inherits from DocumentStyle's use_first_page_header_footer for the first section. If the value is unset for subsequent sectors, it should be interpreted as false. If DocumentMode is PAGELESS, this property will not be rendered. When updating this property, setting a concrete value is required. Unsetting this property results in a 400 bad request error.",
                ).optional(),
              }).describe("The styling that applies to a section.").optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe(
              "A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.",
            ).optional(),
            startIndex: z.number().int().describe(
              "The zero-based start index of this structural element, in UTF-16 code units.",
            ).optional(),
            table: z.object({
              columns: z.number().int().describe(
                "Number of columns in the table. It's possible for a table to be non-rectangular, so some rows may have a different number of cells.",
              ).optional(),
              rows: z.number().int().describe("Number of rows in the table.")
                .optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A Table may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
              tableRows: z.array(z.object({
                endIndex: z.number().int().describe(
                  "The zero-based end index of this row, exclusive, in UTF-16 code units.",
                ).optional(),
                startIndex: z.number().int().describe(
                  "The zero-based start index of this row, in UTF-16 code units.",
                ).optional(),
                suggestedDeletionIds: z.array(z.string()).describe(
                  "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                ).optional(),
                suggestedInsertionIds: z.array(z.string()).describe(
                  "The suggested insertion IDs. A TableRow may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                ).optional(),
                suggestedTableRowStyleChanges: z.record(
                  z.string(),
                  z.object({
                    tableRowStyle: z.object({
                      minRowHeight: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                      preventOverflow: z.boolean().describe(
                        "Whether the row cannot overflow across page or column boundaries.",
                      ).optional(),
                      tableHeader: z.boolean().describe(
                        "Whether the row is a table header.",
                      ).optional(),
                    }).describe("Styles that apply to a table row.").optional(),
                    tableRowStyleSuggestionState: z.object({
                      minRowHeightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to min_row_height.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base TableRowStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }),
                ).describe(
                  "The suggested style changes to this row, keyed by suggestion ID.",
                ).optional(),
                tableCells: z.array(z.object({
                  content: z.array(z.string()).describe(
                    "The content of the cell.",
                  ).optional(),
                  endIndex: z.number().int().describe(
                    "The zero-based end index of this cell, exclusive, in UTF-16 code units.",
                  ).optional(),
                  startIndex: z.number().int().describe(
                    "The zero-based start index of this cell, in UTF-16 code units.",
                  ).optional(),
                  suggestedDeletionIds: z.array(z.string()).describe(
                    "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
                  ).optional(),
                  suggestedInsertionIds: z.array(z.string()).describe(
                    "The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.",
                  ).optional(),
                  suggestedTableCellStyleChanges: z.record(
                    z.string(),
                    z.object({
                      tableCellStyle: z.object({
                        backgroundColor: z.object({
                          color: z.object({
                            rgbColor: z.object({
                              blue: z.number().describe(
                                "The blue component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              green: z.number().describe(
                                "The green component of the color, from 0.0 to 1.0.",
                              ).optional(),
                              red: z.number().describe(
                                "The red component of the color, from 0.0 to 1.0.",
                              ).optional(),
                            }).describe("An RGB color.").optional(),
                          }).describe("A solid color.").optional(),
                        }).describe(
                          "A color that can either be fully opaque or fully transparent.",
                        ).optional(),
                        borderBottom: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderLeft: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderRight: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        borderTop: z.object({
                          color: z.object({
                            color: z.object({
                              rgbColor: z.object({
                                blue: z.number().describe(
                                  "The blue component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                green: z.number().describe(
                                  "The green component of the color, from 0.0 to 1.0.",
                                ).optional(),
                                red: z.number().describe(
                                  "The red component of the color, from 0.0 to 1.0.",
                                ).optional(),
                              }).describe("An RGB color.").optional(),
                            }).describe("A solid color.").optional(),
                          }).describe(
                            "A color that can either be fully opaque or fully transparent.",
                          ).optional(),
                          dashStyle: z.enum([
                            "DASH_STYLE_UNSPECIFIED",
                            "SOLID",
                            "DOT",
                            "DASH",
                          ]).describe("The dash style of the border.")
                            .optional(),
                          width: z.object({
                            magnitude: z.number().describe("The magnitude.")
                              .optional(),
                            unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                              "The units for magnitude.",
                            ).optional(),
                          }).describe(
                            "A magnitude in a single direction in the specified units.",
                          ).optional(),
                        }).describe(
                          "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                        ).optional(),
                        columnSpan: z.number().int().describe(
                          "The column span of the cell. This property is read-only.",
                        ).optional(),
                        contentAlignment: z.enum([
                          "CONTENT_ALIGNMENT_UNSPECIFIED",
                          "CONTENT_ALIGNMENT_UNSUPPORTED",
                          "TOP",
                          "MIDDLE",
                          "BOTTOM",
                        ]).describe(
                          "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                        ).optional(),
                        paddingBottom: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingLeft: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingRight: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        paddingTop: z.object({
                          magnitude: z.number().describe("The magnitude.")
                            .optional(),
                          unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                            "The units for magnitude.",
                          ).optional(),
                        }).describe(
                          "A magnitude in a single direction in the specified units.",
                        ).optional(),
                        rowSpan: z.number().int().describe(
                          "The row span of the cell. This property is read-only.",
                        ).optional(),
                      }).describe(
                        "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                      ).optional(),
                      tableCellStyleSuggestionState: z.object({
                        backgroundColorSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to background_color.",
                        ).optional(),
                        borderBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_bottom.",
                        ).optional(),
                        borderLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_left.",
                        ).optional(),
                        borderRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_right.",
                        ).optional(),
                        borderTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to border_top.",
                        ).optional(),
                        columnSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to column_span.",
                        ).optional(),
                        contentAlignmentSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to content_alignment.",
                        ).optional(),
                        paddingBottomSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_bottom.",
                        ).optional(),
                        paddingLeftSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_left.",
                        ).optional(),
                        paddingRightSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_right.",
                        ).optional(),
                        paddingTopSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to padding_top.",
                        ).optional(),
                        rowSpanSuggested: z.boolean().describe(
                          "Indicates if there was a suggested change to row_span.",
                        ).optional(),
                      }).describe(
                        "A mask that indicates which of the fields on the base TableCellStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                      ).optional(),
                    }),
                  ).describe(
                    "The suggested changes to the table cell style, keyed by suggestion ID.",
                  ).optional(),
                  tableCellStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    borderBottom: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderLeft: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderRight: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    borderTop: z.object({
                      color: z.object({
                        color: z.object({
                          rgbColor: z.object({
                            blue: z.number().describe(
                              "The blue component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            green: z.number().describe(
                              "The green component of the color, from 0.0 to 1.0.",
                            ).optional(),
                            red: z.number().describe(
                              "The red component of the color, from 0.0 to 1.0.",
                            ).optional(),
                          }).describe("An RGB color.").optional(),
                        }).describe("A solid color.").optional(),
                      }).describe(
                        "A color that can either be fully opaque or fully transparent.",
                      ).optional(),
                      dashStyle: z.enum([
                        "DASH_STYLE_UNSPECIFIED",
                        "SOLID",
                        "DOT",
                        "DASH",
                      ]).describe("The dash style of the border.").optional(),
                      width: z.object({
                        magnitude: z.number().describe("The magnitude.")
                          .optional(),
                        unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                          "The units for magnitude.",
                        ).optional(),
                      }).describe(
                        "A magnitude in a single direction in the specified units.",
                      ).optional(),
                    }).describe(
                      "A border around a table cell. Table cell borders cannot be transparent. To hide a table cell border, make its width 0.",
                    ).optional(),
                    columnSpan: z.number().int().describe(
                      "The column span of the cell. This property is read-only.",
                    ).optional(),
                    contentAlignment: z.enum([
                      "CONTENT_ALIGNMENT_UNSPECIFIED",
                      "CONTENT_ALIGNMENT_UNSUPPORTED",
                      "TOP",
                      "MIDDLE",
                      "BOTTOM",
                    ]).describe(
                      "The alignment of the content in the table cell. The default alignment matches the alignment for newly created table cells in the Docs editor.",
                    ).optional(),
                    paddingBottom: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingLeft: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingRight: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    paddingTop: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    rowSpan: z.number().int().describe(
                      "The row span of the cell. This property is read-only.",
                    ).optional(),
                  }).describe(
                    "The style of a TableCell. Inherited table cell styles are represented as unset fields in this message. A table cell style can inherit from the table's style.",
                  ).optional(),
                })).describe(
                  "The contents and style of each cell in this row. It's possible for a table to be non-rectangular, so some rows may have a different number of cells than other rows in the same table.",
                ).optional(),
                tableRowStyle: z.object({
                  minRowHeight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  preventOverflow: z.boolean().describe(
                    "Whether the row cannot overflow across page or column boundaries.",
                  ).optional(),
                  tableHeader: z.boolean().describe(
                    "Whether the row is a table header.",
                  ).optional(),
                }).describe("Styles that apply to a table row.").optional(),
              })).describe("The contents and style of each row.").optional(),
              tableStyle: z.object({
                tableColumnProperties: z.array(z.object({
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  widthType: z.enum([
                    "WIDTH_TYPE_UNSPECIFIED",
                    "EVENLY_DISTRIBUTED",
                    "FIXED_WIDTH",
                  ]).describe("The width type of the column.").optional(),
                })).describe(
                  "The properties of each column. Note that in Docs, tables contain rows and rows contain cells, similar to HTML. So the properties for a row can be found on the row's table_row_style.",
                ).optional(),
              }).describe("Styles that apply to a table.").optional(),
            }).describe("A StructuralElement representing a table.").optional(),
            tableOfContents: z.object({
              content: z.array(z.string()).describe(
                "The content of the table of contents.",
              ).optional(),
              suggestedDeletionIds: z.array(z.string()).describe(
                "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
              ).optional(),
              suggestedInsertionIds: z.array(z.string()).describe(
                "The suggested insertion IDs. A TableOfContents may have multiple insertion IDs if it is a nested suggested change. If empty, then this is not a suggested insertion.",
              ).optional(),
            }).describe("A StructuralElement representing a table of contents.")
              .optional(),
          })).describe(
            "The contents of the header. The indexes for a header's content begin at zero.",
          ).optional(),
          headerId: z.string().describe("The ID of the header.").optional(),
        }),
      ).describe("The headers in the document tab, keyed by header ID.")
        .optional(),
      inlineObjects: z.record(
        z.string(),
        z.object({
          inlineObjectProperties: z.object({
            embeddedObject: z.object({
              description: z.string().describe(
                "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
              embeddedDrawingProperties: z.object({}).describe(
                "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
              ).optional(),
              embeddedObjectBorder: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                propertyState: z.enum(["RENDERED", "NOT_RENDERED"]).describe(
                  "The property state of the border property.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around an EmbeddedObject.").optional(),
              imageProperties: z.object({
                angle: z.number().describe(
                  "The clockwise rotation angle of the image, in radians.",
                ).optional(),
                brightness: z.number().describe(
                  "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                contentUri: z.string().describe(
                  "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                ).optional(),
                contrast: z.number().describe(
                  "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                cropProperties: z.object({
                  angle: z.number().describe(
                    "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                  ).optional(),
                  offsetBottom: z.number().describe(
                    "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                  offsetLeft: z.number().describe(
                    "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetRight: z.number().describe(
                    "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetTop: z.number().describe(
                    "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                }).describe(
                  "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                ).optional(),
                sourceUri: z.string().describe(
                  "The source URI is the URI used to insert the image. The source URI can be empty.",
                ).optional(),
                transparency: z.number().describe(
                  "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                ).optional(),
              }).describe("The properties of an image.").optional(),
              linkedContentReference: z.object({
                sheetsChartReference: z.object({
                  chartId: z.number().int().describe(
                    "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                  ).optional(),
                  spreadsheetId: z.string().describe(
                    "The ID of the Google Sheets spreadsheet that contains the source chart.",
                  ).optional(),
                }).describe(
                  "A reference to a linked chart embedded from Google Sheets.",
                ).optional(),
              }).describe("A reference to the external linked source content.")
                .optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              size: z.object({
                height: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A width and height.").optional(),
              title: z.string().describe(
                "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
            }).describe("An embedded object in the document.").optional(),
          }).describe("Properties of an InlineObject.").optional(),
          objectId: z.string().describe(
            "The ID of this inline object. Can be used to update an object’s properties.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInlineObjectPropertiesChanges: z.record(
            z.string(),
            z.object({
              inlineObjectProperties: z.object({
                embeddedObject: z.object({
                  description: z.string().describe(
                    "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                  embeddedDrawingProperties: z.object({}).describe(
                    "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
                  ).optional(),
                  embeddedObjectBorder: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    propertyState: z.enum(["RENDERED", "NOT_RENDERED"])
                      .describe("The property state of the border property.")
                      .optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around an EmbeddedObject.").optional(),
                  imageProperties: z.object({
                    angle: z.number().describe(
                      "The clockwise rotation angle of the image, in radians.",
                    ).optional(),
                    brightness: z.number().describe(
                      "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    contentUri: z.string().describe(
                      "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                    ).optional(),
                    contrast: z.number().describe(
                      "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    cropProperties: z.object({
                      angle: z.number().describe(
                        "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                      ).optional(),
                      offsetBottom: z.number().describe(
                        "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                      offsetLeft: z.number().describe(
                        "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetRight: z.number().describe(
                        "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetTop: z.number().describe(
                        "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                    }).describe(
                      "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                    ).optional(),
                    sourceUri: z.string().describe(
                      "The source URI is the URI used to insert the image. The source URI can be empty.",
                    ).optional(),
                    transparency: z.number().describe(
                      "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                    ).optional(),
                  }).describe("The properties of an image.").optional(),
                  linkedContentReference: z.object({
                    sheetsChartReference: z.object({
                      chartId: z.number().int().describe(
                        "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                      ).optional(),
                      spreadsheetId: z.string().describe(
                        "The ID of the Google Sheets spreadsheet that contains the source chart.",
                      ).optional(),
                    }).describe(
                      "A reference to a linked chart embedded from Google Sheets.",
                    ).optional(),
                  }).describe(
                    "A reference to the external linked source content.",
                  ).optional(),
                  marginBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  size: z.object({
                    height: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A width and height.").optional(),
                  title: z.string().describe(
                    "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                }).describe("An embedded object in the document.").optional(),
              }).describe("Properties of an InlineObject.").optional(),
              inlineObjectPropertiesSuggestionState: z.object({
                embeddedObjectSuggestionState: z.object({
                  descriptionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to description.",
                  ).optional(),
                  embeddedDrawingPropertiesSuggestionState: z.object({})
                    .describe(
                      "A mask that indicates which of the fields on the base EmbeddedDrawingProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  embeddedObjectBorderSuggestionState: z.object({
                    colorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to color.",
                    ).optional(),
                    dashStyleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to dash_style.",
                    ).optional(),
                    propertyStateSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to property_state.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base EmbeddedObjectBorder have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  imagePropertiesSuggestionState: z.object({
                    angleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to angle.",
                    ).optional(),
                    brightnessSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to brightness.",
                    ).optional(),
                    contentUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to content_uri.",
                    ).optional(),
                    contrastSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to contrast.",
                    ).optional(),
                    cropPropertiesSuggestionState: z.object({
                      angleSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to angle.",
                      ).optional(),
                      offsetBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_bottom.",
                      ).optional(),
                      offsetLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_left.",
                      ).optional(),
                      offsetRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_right.",
                      ).optional(),
                      offsetTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_top.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base CropProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    sourceUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to source_uri.",
                    ).optional(),
                    transparencySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to transparency.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ImageProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  linkedContentReferenceSuggestionState: z.object({
                    sheetsChartReferenceSuggestionState: z.object({
                      chartIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to chart_id.",
                      ).optional(),
                      spreadsheetIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to spreadsheet_id.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base SheetsChartReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base LinkedContentReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  marginBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_bottom.",
                  ).optional(),
                  marginLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_left.",
                  ).optional(),
                  marginRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_right.",
                  ).optional(),
                  marginTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_top.",
                  ).optional(),
                  sizeSuggestionState: z.object({
                    heightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to height.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
                  ).optional(),
                  titleSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to title.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base EmbeddedObject have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base InlineObjectProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the inline object properties, keyed by suggestion ID.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
        }),
      ).describe("The inline objects in the document tab, keyed by object ID.")
        .optional(),
      lists: z.record(
        z.string(),
        z.object({
          listProperties: z.object({
            nestingLevels: z.array(z.object({
              bulletAlignment: z.enum([
                "BULLET_ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
              ]).describe(
                "The alignment of the bullet within the space allotted for rendering the bullet.",
              ).optional(),
              glyphFormat: z.string().describe(
                "The format string used by bullets at this level of nesting. The glyph format contains one or more placeholders, and these placeholders are replaced with the appropriate values depending on the glyph_type or glyph_symbol. The placeholders follow the pattern `%[nesting_level]`. Furthermore, placeholders can have prefixes and suffixes. Thus, the glyph format follows the pattern `%[nesting_level]`. Note that the prefix and suffix are optional and can be arbitrary strings. For example, the glyph format `%0.` indicates that the rendered glyph will replace the placeholder with the corresponding glyph for nesting level 0 followed by a period as the suffix. So a list with a glyph type of UPPER_ALPHA and glyph format `%0.` at nesting level 0 will result in a list with rendered glyphs `A.` `B.` `C.` The glyph format can contain placeholders for the current nesting level as well as placeholders for parent nesting levels. For example, a list can have a glyph format of `%0.` at nesting level 0 and a glyph format of `%0.%1.` at nesting level 1. Assuming both nesting levels have DECIMAL glyph types, this would result in a list with rendered glyphs `1.` `2.` ` 2.1.` ` 2.2.` `3.` For nesting levels that are ordered, the string that replaces a placeholder in the glyph format for a particular paragraph depends on the paragraph's order within the list.",
              ).optional(),
              glyphSymbol: z.string().describe(
                "A custom glyph symbol used by bullets when paragraphs at this level of nesting is unordered. The glyph symbol replaces placeholders within the glyph_format. For example, if the glyph_symbol is the solid circle corresponding to Unicode U+25cf code point and the glyph_format is `%0`, the rendered glyph would be the solid circle.",
              ).optional(),
              glyphType: z.enum([
                "GLYPH_TYPE_UNSPECIFIED",
                "NONE",
                "DECIMAL",
                "ZERO_DECIMAL",
                "UPPER_ALPHA",
                "ALPHA",
                "UPPER_ROMAN",
                "ROMAN",
              ]).describe(
                "The type of glyph used by bullets when paragraphs at this level of nesting is ordered. The glyph type determines the type of glyph used to replace placeholders within the glyph_format when paragraphs at this level of nesting are ordered. For example, if the nesting level is 0, the glyph_format is `%0.` and the glyph type is DECIMAL, then the rendered glyph would replace the placeholder `%0` in the glyph format with a number corresponding to the list item's order within the list.",
              ).optional(),
              indentFirstLine: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              indentStart: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              startNumber: z.number().int().describe(
                "The number of the first list item at this nesting level. A value of 0 is treated as a value of 1 for lettered lists and Roman numeral lists. For values of both 0 and 1, lettered and Roman numeral lists will begin at `a` and `i` respectively. This value is ignored for nesting levels with unordered glyphs.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            })).describe(
              "Describes the properties of the bullets at the associated level. A list has at most 9 levels of nesting with nesting level 0 corresponding to the top-most level and nesting level 8 corresponding to the most nested level. The nesting levels are returned in ascending order with the least nested returned first.",
            ).optional(),
          }).describe(
            "The properties of a list that describe the look and feel of bullets belonging to paragraphs associated with a list.",
          ).optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this list.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedListPropertiesChanges: z.record(
            z.string(),
            z.object({
              listProperties: z.object({
                nestingLevels: z.array(z.object({
                  bulletAlignment: z.enum([
                    "BULLET_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of the bullet within the space allotted for rendering the bullet.",
                  ).optional(),
                  glyphFormat: z.string().describe(
                    "The format string used by bullets at this level of nesting. The glyph format contains one or more placeholders, and these placeholders are replaced with the appropriate values depending on the glyph_type or glyph_symbol. The placeholders follow the pattern `%[nesting_level]`. Furthermore, placeholders can have prefixes and suffixes. Thus, the glyph format follows the pattern `%[nesting_level]`. Note that the prefix and suffix are optional and can be arbitrary strings. For example, the glyph format `%0.` indicates that the rendered glyph will replace the placeholder with the corresponding glyph for nesting level 0 followed by a period as the suffix. So a list with a glyph type of UPPER_ALPHA and glyph format `%0.` at nesting level 0 will result in a list with rendered glyphs `A.` `B.` `C.` The glyph format can contain placeholders for the current nesting level as well as placeholders for parent nesting levels. For example, a list can have a glyph format of `%0.` at nesting level 0 and a glyph format of `%0.%1.` at nesting level 1. Assuming both nesting levels have DECIMAL glyph types, this would result in a list with rendered glyphs `1.` `2.` ` 2.1.` ` 2.2.` `3.` For nesting levels that are ordered, the string that replaces a placeholder in the glyph format for a particular paragraph depends on the paragraph's order within the list.",
                  ).optional(),
                  glyphSymbol: z.string().describe(
                    "A custom glyph symbol used by bullets when paragraphs at this level of nesting is unordered. The glyph symbol replaces placeholders within the glyph_format. For example, if the glyph_symbol is the solid circle corresponding to Unicode U+25cf code point and the glyph_format is `%0`, the rendered glyph would be the solid circle.",
                  ).optional(),
                  glyphType: z.enum([
                    "GLYPH_TYPE_UNSPECIFIED",
                    "NONE",
                    "DECIMAL",
                    "ZERO_DECIMAL",
                    "UPPER_ALPHA",
                    "ALPHA",
                    "UPPER_ROMAN",
                    "ROMAN",
                  ]).describe(
                    "The type of glyph used by bullets when paragraphs at this level of nesting is ordered. The glyph type determines the type of glyph used to replace placeholders within the glyph_format when paragraphs at this level of nesting are ordered. For example, if the nesting level is 0, the glyph_format is `%0.` and the glyph type is DECIMAL, then the rendered glyph would replace the placeholder `%0` in the glyph format with a number corresponding to the list item's order within the list.",
                  ).optional(),
                  indentFirstLine: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  indentStart: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  startNumber: z.number().int().describe(
                    "The number of the first list item at this nesting level. A value of 0 is treated as a value of 1 for lettered lists and Roman numeral lists. For values of both 0 and 1, lettered and Roman numeral lists will begin at `a` and `i` respectively. This value is ignored for nesting levels with unordered glyphs.",
                  ).optional(),
                  textStyle: z.object({
                    backgroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    baselineOffset: z.enum([
                      "BASELINE_OFFSET_UNSPECIFIED",
                      "NONE",
                      "SUPERSCRIPT",
                      "SUBSCRIPT",
                    ]).describe(
                      "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                    ).optional(),
                    bold: z.boolean().describe(
                      "Whether or not the text is rendered as bold.",
                    ).optional(),
                    fontSize: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    foregroundColor: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    italic: z.boolean().describe(
                      "Whether or not the text is italicized.",
                    ).optional(),
                    link: z.object({
                      bookmark: z.object({
                        id: z.string().describe(
                          "The ID of a bookmark in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this bookmark.",
                        ).optional(),
                      }).describe("A reference to a bookmark in this document.")
                        .optional(),
                      bookmarkId: z.string().describe(
                        "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      heading: z.object({
                        id: z.string().describe(
                          "The ID of a heading in this document.",
                        ).optional(),
                        tabId: z.string().describe(
                          "The ID of the tab containing this heading.",
                        ).optional(),
                      }).describe("A reference to a heading in this document.")
                        .optional(),
                      headingId: z.string().describe(
                        "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                      ).optional(),
                      tabId: z.string().describe(
                        "The ID of a tab in this document.",
                      ).optional(),
                      url: z.string().describe("An external URL.").optional(),
                    }).describe(
                      "A reference to another portion of a document or an external URL resource.",
                    ).optional(),
                    smallCaps: z.boolean().describe(
                      "Whether or not the text is in small capital letters.",
                    ).optional(),
                    strikethrough: z.boolean().describe(
                      "Whether or not the text is struck through.",
                    ).optional(),
                    underline: z.boolean().describe(
                      "Whether or not the text is underlined.",
                    ).optional(),
                    weightedFontFamily: z.object({
                      fontFamily: z.string().describe(
                        "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                      ).optional(),
                      weight: z.number().int().describe(
                        "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                      ).optional(),
                    }).describe("Represents a font family and weight of text.")
                      .optional(),
                  }).describe(
                    "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
                  ).optional(),
                })).describe(
                  "Describes the properties of the bullets at the associated level. A list has at most 9 levels of nesting with nesting level 0 corresponding to the top-most level and nesting level 8 corresponding to the most nested level. The nesting levels are returned in ascending order with the least nested returned first.",
                ).optional(),
              }).describe(
                "The properties of a list that describe the look and feel of bullets belonging to paragraphs associated with a list.",
              ).optional(),
              listPropertiesSuggestionState: z.object({
                nestingLevelsSuggestionStates: z.array(z.object({
                  bulletAlignmentSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to bullet_alignment.",
                  ).optional(),
                  glyphFormatSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_format.",
                  ).optional(),
                  glyphSymbolSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_symbol.",
                  ).optional(),
                  glyphTypeSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to glyph_type.",
                  ).optional(),
                  indentFirstLineSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_first_line.",
                  ).optional(),
                  indentStartSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to indent_start.",
                  ).optional(),
                  startNumberSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to start_number.",
                  ).optional(),
                  textStyleSuggestionState: z.object({
                    backgroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to background_color.",
                    ).optional(),
                    baselineOffsetSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to baseline_offset.",
                    ).optional(),
                    boldSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to bold.",
                    ).optional(),
                    fontSizeSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to font_size.",
                    ).optional(),
                    foregroundColorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to foreground_color.",
                    ).optional(),
                    italicSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to italic.",
                    ).optional(),
                    linkSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to link.",
                    ).optional(),
                    smallCapsSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to small_caps.",
                    ).optional(),
                    strikethroughSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to strikethrough.",
                    ).optional(),
                    underlineSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to underline.",
                    ).optional(),
                    weightedFontFamilySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to weighted_font_family.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                })).describe(
                  "A mask that indicates which of the fields on the corresponding NestingLevel in nesting_levels have been changed in this suggestion. The nesting level suggestion states are returned in ascending order of the nesting level with the least nested returned first.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base ListProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the list properties, keyed by suggestion ID.",
          ).optional(),
        }),
      ).describe("The lists in the document tab, keyed by list ID.").optional(),
      namedRanges: z.record(
        z.string(),
        z.object({
          name: z.string().describe("The name that all the named ranges share.")
            .optional(),
          namedRanges: z.array(z.object({
            name: z.string().describe("The name of the named range.")
              .optional(),
            namedRangeId: z.string().describe("The ID of the named range.")
              .optional(),
            ranges: z.array(z.object({
              endIndex: z.number().int().describe(
                "The zero-based end index of this range, exclusive, in UTF-16 code units. In all current uses, an end index must be provided. This field is an Int32Value in order to accommodate future use cases with open-ended ranges.",
              ).optional(),
              segmentId: z.string().describe(
                "The ID of the header, footer, or footnote that this range is contained in. An empty segment ID signifies the document's body.",
              ).optional(),
              startIndex: z.number().int().describe(
                "The zero-based start index of this range, in UTF-16 code units. In all current uses, a start index must be provided. This field is an Int32Value in order to accommodate future use cases with open-ended ranges.",
              ).optional(),
              tabId: z.string().describe(
                "The tab that contains this range. When omitted, the request applies to the first tab. In a document containing a single tab: - If provided, must match the singular tab's ID. - If omitted, the request applies to the singular tab. In a document containing multiple tabs: - If provided, the request applies to the specified tab. - If omitted, the request applies to the first tab in the document.",
              ).optional(),
            })).describe("The ranges that belong to this named range.")
              .optional(),
          })).describe("The NamedRanges that share the same name.").optional(),
        }),
      ).describe("The named ranges in the document tab, keyed by name.")
        .optional(),
      namedStyles: z.object({
        styles: z.array(z.object({
          namedStyleType: z.enum([
            "NAMED_STYLE_TYPE_UNSPECIFIED",
            "NORMAL_TEXT",
            "TITLE",
            "SUBTITLE",
            "HEADING_1",
            "HEADING_2",
            "HEADING_3",
            "HEADING_4",
            "HEADING_5",
            "HEADING_6",
          ]).describe("The type of this named style.").optional(),
          paragraphStyle: z.object({
            alignment: z.enum([
              "ALIGNMENT_UNSPECIFIED",
              "START",
              "CENTER",
              "END",
              "JUSTIFIED",
            ]).describe("The text alignment for this paragraph.").optional(),
            avoidWidowAndOrphan: z.boolean().describe(
              "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
            ).optional(),
            borderBetween: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderBottom: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderLeft: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderRight: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            borderTop: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
              dashStyle: z.enum([
                "DASH_STYLE_UNSPECIFIED",
                "SOLID",
                "DOT",
                "DASH",
              ]).describe("The dash style of the border.").optional(),
              padding: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A border around a paragraph.").optional(),
            direction: z.enum([
              "CONTENT_DIRECTION_UNSPECIFIED",
              "LEFT_TO_RIGHT",
              "RIGHT_TO_LEFT",
            ]).describe(
              "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
            ).optional(),
            headingId: z.string().describe(
              "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
            ).optional(),
            indentEnd: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            indentFirstLine: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            indentStart: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            keepLinesTogether: z.boolean().describe(
              "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
            ).optional(),
            keepWithNext: z.boolean().describe(
              "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
            ).optional(),
            lineSpacing: z.number().describe(
              "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
            ).optional(),
            namedStyleType: z.enum([
              "NAMED_STYLE_TYPE_UNSPECIFIED",
              "NORMAL_TEXT",
              "TITLE",
              "SUBTITLE",
              "HEADING_1",
              "HEADING_2",
              "HEADING_3",
              "HEADING_4",
              "HEADING_5",
              "HEADING_6",
            ]).describe(
              "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
            ).optional(),
            pageBreakBefore: z.boolean().describe(
              "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
            ).optional(),
            shading: z.object({
              backgroundColor: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
            }).describe("The shading of a paragraph.").optional(),
            spaceAbove: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            spaceBelow: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            spacingMode: z.enum([
              "SPACING_MODE_UNSPECIFIED",
              "NEVER_COLLAPSE",
              "COLLAPSE_LISTS",
            ]).describe("The spacing mode for the paragraph.").optional(),
            tabStops: z.array(z.object({
              alignment: z.enum([
                "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                "START",
                "CENTER",
                "END",
              ]).describe(
                "The alignment of this tab stop. If unset, the value defaults to START.",
              ).optional(),
              offset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            })).describe(
              "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
            ).optional(),
          }).describe(
            "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
          textStyle: z.object({
            backgroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            baselineOffset: z.enum([
              "BASELINE_OFFSET_UNSPECIFIED",
              "NONE",
              "SUPERSCRIPT",
              "SUBSCRIPT",
            ]).describe(
              "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
            ).optional(),
            bold: z.boolean().describe(
              "Whether or not the text is rendered as bold.",
            ).optional(),
            fontSize: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            foregroundColor: z.object({
              color: z.object({
                rgbColor: z.object({
                  blue: z.number().describe(
                    "The blue component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  green: z.number().describe(
                    "The green component of the color, from 0.0 to 1.0.",
                  ).optional(),
                  red: z.number().describe(
                    "The red component of the color, from 0.0 to 1.0.",
                  ).optional(),
                }).describe("An RGB color.").optional(),
              }).describe("A solid color.").optional(),
            }).describe(
              "A color that can either be fully opaque or fully transparent.",
            ).optional(),
            italic: z.boolean().describe(
              "Whether or not the text is italicized.",
            ).optional(),
            link: z.object({
              bookmark: z.object({
                id: z.string().describe(
                  "The ID of a bookmark in this document.",
                ).optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this bookmark.",
                ).optional(),
              }).describe("A reference to a bookmark in this document.")
                .optional(),
              bookmarkId: z.string().describe(
                "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              heading: z.object({
                id: z.string().describe("The ID of a heading in this document.")
                  .optional(),
                tabId: z.string().describe(
                  "The ID of the tab containing this heading.",
                ).optional(),
              }).describe("A reference to a heading in this document.")
                .optional(),
              headingId: z.string().describe(
                "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
              ).optional(),
              tabId: z.string().describe("The ID of a tab in this document.")
                .optional(),
              url: z.string().describe("An external URL.").optional(),
            }).describe(
              "A reference to another portion of a document or an external URL resource.",
            ).optional(),
            smallCaps: z.boolean().describe(
              "Whether or not the text is in small capital letters.",
            ).optional(),
            strikethrough: z.boolean().describe(
              "Whether or not the text is struck through.",
            ).optional(),
            underline: z.boolean().describe(
              "Whether or not the text is underlined.",
            ).optional(),
            weightedFontFamily: z.object({
              fontFamily: z.string().describe(
                "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
              ).optional(),
              weight: z.number().int().describe(
                "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
              ).optional(),
            }).describe("Represents a font family and weight of text.")
              .optional(),
          }).describe(
            "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
          ).optional(),
        })).describe(
          "The named styles. There's an entry for each of the possible named style types.",
        ).optional(),
      }).describe(
        "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
      ).optional(),
      positionedObjects: z.record(
        z.string(),
        z.object({
          objectId: z.string().describe("The ID of this positioned object.")
            .optional(),
          positionedObjectProperties: z.object({
            embeddedObject: z.object({
              description: z.string().describe(
                "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
              embeddedDrawingProperties: z.object({}).describe(
                "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
              ).optional(),
              embeddedObjectBorder: z.object({
                color: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                dashStyle: z.enum([
                  "DASH_STYLE_UNSPECIFIED",
                  "SOLID",
                  "DOT",
                  "DASH",
                ]).describe("The dash style of the border.").optional(),
                propertyState: z.enum(["RENDERED", "NOT_RENDERED"]).describe(
                  "The property state of the border property.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A border around an EmbeddedObject.").optional(),
              imageProperties: z.object({
                angle: z.number().describe(
                  "The clockwise rotation angle of the image, in radians.",
                ).optional(),
                brightness: z.number().describe(
                  "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                contentUri: z.string().describe(
                  "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                ).optional(),
                contrast: z.number().describe(
                  "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                ).optional(),
                cropProperties: z.object({
                  angle: z.number().describe(
                    "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                  ).optional(),
                  offsetBottom: z.number().describe(
                    "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                  offsetLeft: z.number().describe(
                    "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetRight: z.number().describe(
                    "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                  ).optional(),
                  offsetTop: z.number().describe(
                    "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                  ).optional(),
                }).describe(
                  "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                ).optional(),
                sourceUri: z.string().describe(
                  "The source URI is the URI used to insert the image. The source URI can be empty.",
                ).optional(),
                transparency: z.number().describe(
                  "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                ).optional(),
              }).describe("The properties of an image.").optional(),
              linkedContentReference: z.object({
                sheetsChartReference: z.object({
                  chartId: z.number().int().describe(
                    "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                  ).optional(),
                  spreadsheetId: z.string().describe(
                    "The ID of the Google Sheets spreadsheet that contains the source chart.",
                  ).optional(),
                }).describe(
                  "A reference to a linked chart embedded from Google Sheets.",
                ).optional(),
              }).describe("A reference to the external linked source content.")
                .optional(),
              marginBottom: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginLeft: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginRight: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              marginTop: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              size: z.object({
                height: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                width: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
              }).describe("A width and height.").optional(),
              title: z.string().describe(
                "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
              ).optional(),
            }).describe("An embedded object in the document.").optional(),
            positioning: z.object({
              layout: z.enum([
                "POSITIONED_OBJECT_LAYOUT_UNSPECIFIED",
                "WRAP_TEXT",
                "BREAK_LEFT",
                "BREAK_RIGHT",
                "BREAK_LEFT_RIGHT",
                "IN_FRONT_OF_TEXT",
                "BEHIND_TEXT",
              ]).describe("The layout of this positioned object.").optional(),
              leftOffset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              topOffset: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe(
              "The positioning of a PositionedObject. The positioned object is positioned relative to the beginning of the Paragraph it's tethered to.",
            ).optional(),
          }).describe("Properties of a PositionedObject.").optional(),
          suggestedDeletionIds: z.array(z.string()).describe(
            "The suggested deletion IDs. If empty, then there are no suggested deletions of this content.",
          ).optional(),
          suggestedInsertionId: z.string().describe(
            "The suggested insertion ID. If empty, then this is not a suggested insertion.",
          ).optional(),
          suggestedPositionedObjectPropertiesChanges: z.record(
            z.string(),
            z.object({
              positionedObjectProperties: z.object({
                embeddedObject: z.object({
                  description: z.string().describe(
                    "The description of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                  embeddedDrawingProperties: z.object({}).describe(
                    "The properties of an embedded drawing and used to differentiate the object type. An embedded drawing is one that's created and edited within a document. Note that extensive details are not supported.",
                  ).optional(),
                  embeddedObjectBorder: z.object({
                    color: z.object({
                      color: z.object({
                        rgbColor: z.object({
                          blue: z.number().describe(
                            "The blue component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          green: z.number().describe(
                            "The green component of the color, from 0.0 to 1.0.",
                          ).optional(),
                          red: z.number().describe(
                            "The red component of the color, from 0.0 to 1.0.",
                          ).optional(),
                        }).describe("An RGB color.").optional(),
                      }).describe("A solid color.").optional(),
                    }).describe(
                      "A color that can either be fully opaque or fully transparent.",
                    ).optional(),
                    dashStyle: z.enum([
                      "DASH_STYLE_UNSPECIFIED",
                      "SOLID",
                      "DOT",
                      "DASH",
                    ]).describe("The dash style of the border.").optional(),
                    propertyState: z.enum(["RENDERED", "NOT_RENDERED"])
                      .describe("The property state of the border property.")
                      .optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A border around an EmbeddedObject.").optional(),
                  imageProperties: z.object({
                    angle: z.number().describe(
                      "The clockwise rotation angle of the image, in radians.",
                    ).optional(),
                    brightness: z.number().describe(
                      "The brightness effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    contentUri: z.string().describe(
                      "A URI to the image with a default lifetime of 30 minutes. This URI is tagged with the account of the requester. Anyone with the URI effectively accesses the image as the original requester. Access to the image may be lost if the document's sharing settings change.",
                    ).optional(),
                    contrast: z.number().describe(
                      "The contrast effect of the image. The value should be in the interval [-1.0, 1.0], where 0 means no effect.",
                    ).optional(),
                    cropProperties: z.object({
                      angle: z.number().describe(
                        "The clockwise rotation angle of the crop rectangle around its center, in radians. Rotation is applied after the offsets.",
                      ).optional(),
                      offsetBottom: z.number().describe(
                        "The offset specifies how far inwards the bottom edge of the crop rectangle is from the bottom edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                      offsetLeft: z.number().describe(
                        "The offset specifies how far inwards the left edge of the crop rectangle is from the left edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetRight: z.number().describe(
                        "The offset specifies how far inwards the right edge of the crop rectangle is from the right edge of the original content as a fraction of the original content's width.",
                      ).optional(),
                      offsetTop: z.number().describe(
                        "The offset specifies how far inwards the top edge of the crop rectangle is from the top edge of the original content as a fraction of the original content's height.",
                      ).optional(),
                    }).describe(
                      "The crop properties of an image. The crop rectangle is represented using fractional offsets from the original content's 4 edges. - If the offset is in the interval (0, 1), the corresponding edge of crop rectangle is positioned inside of the image's original bounding rectangle. - If the offset is negative or greater than 1, the corresponding edge of crop rectangle is positioned outside of the image's original bounding rectangle. - If all offsets and rotation angles are 0, the image is not cropped.",
                    ).optional(),
                    sourceUri: z.string().describe(
                      "The source URI is the URI used to insert the image. The source URI can be empty.",
                    ).optional(),
                    transparency: z.number().describe(
                      "The transparency effect of the image. The value should be in the interval [0.0, 1.0], where 0 means no effect and 1 means transparent.",
                    ).optional(),
                  }).describe("The properties of an image.").optional(),
                  linkedContentReference: z.object({
                    sheetsChartReference: z.object({
                      chartId: z.number().int().describe(
                        "The ID of the specific chart in the Google Sheets spreadsheet that's embedded.",
                      ).optional(),
                      spreadsheetId: z.string().describe(
                        "The ID of the Google Sheets spreadsheet that contains the source chart.",
                      ).optional(),
                    }).describe(
                      "A reference to a linked chart embedded from Google Sheets.",
                    ).optional(),
                  }).describe(
                    "A reference to the external linked source content.",
                  ).optional(),
                  marginBottom: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginLeft: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginRight: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  marginTop: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  size: z.object({
                    height: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                    width: z.object({
                      magnitude: z.number().describe("The magnitude.")
                        .optional(),
                      unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                        "The units for magnitude.",
                      ).optional(),
                    }).describe(
                      "A magnitude in a single direction in the specified units.",
                    ).optional(),
                  }).describe("A width and height.").optional(),
                  title: z.string().describe(
                    "The title of the embedded object. The `title` and `description` are both combined to display alt text.",
                  ).optional(),
                }).describe("An embedded object in the document.").optional(),
                positioning: z.object({
                  layout: z.enum([
                    "POSITIONED_OBJECT_LAYOUT_UNSPECIFIED",
                    "WRAP_TEXT",
                    "BREAK_LEFT",
                    "BREAK_RIGHT",
                    "BREAK_LEFT_RIGHT",
                    "IN_FRONT_OF_TEXT",
                    "BEHIND_TEXT",
                  ]).describe("The layout of this positioned object.")
                    .optional(),
                  leftOffset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  topOffset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe(
                  "The positioning of a PositionedObject. The positioned object is positioned relative to the beginning of the Paragraph it's tethered to.",
                ).optional(),
              }).describe("Properties of a PositionedObject.").optional(),
              positionedObjectPropertiesSuggestionState: z.object({
                embeddedObjectSuggestionState: z.object({
                  descriptionSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to description.",
                  ).optional(),
                  embeddedDrawingPropertiesSuggestionState: z.object({})
                    .describe(
                      "A mask that indicates which of the fields on the base EmbeddedDrawingProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  embeddedObjectBorderSuggestionState: z.object({
                    colorSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to color.",
                    ).optional(),
                    dashStyleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to dash_style.",
                    ).optional(),
                    propertyStateSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to property_state.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base EmbeddedObjectBorder have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  imagePropertiesSuggestionState: z.object({
                    angleSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to angle.",
                    ).optional(),
                    brightnessSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to brightness.",
                    ).optional(),
                    contentUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to content_uri.",
                    ).optional(),
                    contrastSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to contrast.",
                    ).optional(),
                    cropPropertiesSuggestionState: z.object({
                      angleSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to angle.",
                      ).optional(),
                      offsetBottomSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_bottom.",
                      ).optional(),
                      offsetLeftSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_left.",
                      ).optional(),
                      offsetRightSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_right.",
                      ).optional(),
                      offsetTopSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to offset_top.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base CropProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                    sourceUriSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to source_uri.",
                    ).optional(),
                    transparencySuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to transparency.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base ImageProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  linkedContentReferenceSuggestionState: z.object({
                    sheetsChartReferenceSuggestionState: z.object({
                      chartIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to chart_id.",
                      ).optional(),
                      spreadsheetIdSuggested: z.boolean().describe(
                        "Indicates if there was a suggested change to spreadsheet_id.",
                      ).optional(),
                    }).describe(
                      "A mask that indicates which of the fields on the base SheetsChartReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base LinkedContentReference have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                  ).optional(),
                  marginBottomSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_bottom.",
                  ).optional(),
                  marginLeftSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_left.",
                  ).optional(),
                  marginRightSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_right.",
                  ).optional(),
                  marginTopSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to margin_top.",
                  ).optional(),
                  sizeSuggestionState: z.object({
                    heightSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to height.",
                    ).optional(),
                    widthSuggested: z.boolean().describe(
                      "Indicates if there was a suggested change to width.",
                    ).optional(),
                  }).describe(
                    "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
                  ).optional(),
                  titleSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to title.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base EmbeddedObject have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
                positioningSuggestionState: z.object({
                  layoutSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to layout.",
                  ).optional(),
                  leftOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to left_offset.",
                  ).optional(),
                  topOffsetSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to top_offset.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base PositionedObjectPositioning have been changed in this suggestion. For any field set to true, there's a new suggested value.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base PositionedObjectProperties have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            }),
          ).describe(
            "The suggested changes to the positioned object properties, keyed by suggestion ID.",
          ).optional(),
        }),
      ).describe(
        "The positioned objects in the document tab, keyed by object ID.",
      ).optional(),
      suggestedDocumentStyleChanges: z.record(
        z.string(),
        z.object({
          documentStyle: z.object({
            background: z.object({
              color: z.object({
                color: z.object({
                  rgbColor: z.object({
                    blue: z.number().describe(
                      "The blue component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    green: z.number().describe(
                      "The green component of the color, from 0.0 to 1.0.",
                    ).optional(),
                    red: z.number().describe(
                      "The red component of the color, from 0.0 to 1.0.",
                    ).optional(),
                  }).describe("An RGB color.").optional(),
                }).describe("A solid color.").optional(),
              }).describe(
                "A color that can either be fully opaque or fully transparent.",
              ).optional(),
            }).describe("Represents the background of a document.").optional(),
            defaultFooterId: z.string().describe(
              "The ID of the default footer. If not set, there's no default footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            defaultHeaderId: z.string().describe(
              "The ID of the default header. If not set, there's no default header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            documentFormat: z.object({
              documentMode: z.enum([
                "DOCUMENT_MODE_UNSPECIFIED",
                "PAGES",
                "PAGELESS",
              ]).describe("Whether the document has pages or is pageless.")
                .optional(),
            }).describe("Represents document-level format settings.")
              .optional(),
            evenPageFooterId: z.string().describe(
              "The ID of the footer used only for even pages. The value of use_even_page_header_footer determines whether to use the default_footer_id or this value for the footer on even pages. If not set, there's no even page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            evenPageHeaderId: z.string().describe(
              "The ID of the header used only for even pages. The value of use_even_page_header_footer determines whether to use the default_header_id or this value for the header on even pages. If not set, there's no even page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            firstPageFooterId: z.string().describe(
              "The ID of the footer used only for the first page. If not set then a unique footer for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_footer_id or this value for the footer on the first page. If not set, there's no first page footer. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            firstPageHeaderId: z.string().describe(
              "The ID of the header used only for the first page. If not set then a unique header for the first page does not exist. The value of use_first_page_header_footer determines whether to use the default_header_id or this value for the header on the first page. If not set, there's no first page header. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            flipPageOrientation: z.boolean().describe(
              "Optional. Indicates whether to flip the dimensions of the page_size, which allows changing the page orientation between portrait and landscape. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            marginBottom: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginFooter: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginHeader: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginLeft: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginRight: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            marginTop: z.object({
              magnitude: z.number().describe("The magnitude.").optional(),
              unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                "The units for magnitude.",
              ).optional(),
            }).describe(
              "A magnitude in a single direction in the specified units.",
            ).optional(),
            pageNumberStart: z.number().int().describe(
              "The page number from which to start counting the number of pages. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            pageSize: z.object({
              height: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
              width: z.object({
                magnitude: z.number().describe("The magnitude.").optional(),
                unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                  "The units for magnitude.",
                ).optional(),
              }).describe(
                "A magnitude in a single direction in the specified units.",
              ).optional(),
            }).describe("A width and height.").optional(),
            useCustomHeaderFooterMargins: z.boolean().describe(
              "Indicates whether DocumentStyle margin_header, SectionStyle margin_header and DocumentStyle margin_footer, SectionStyle margin_footer are respected. When false, the default values in the Docs editor for header and footer margin is used. If DocumentMode is PAGELESS, this property will not be rendered. This property is read-only.",
            ).optional(),
            useEvenPageHeaderFooter: z.boolean().describe(
              "Indicates whether to use the even page header / footer IDs for the even pages. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
            useFirstPageHeaderFooter: z.boolean().describe(
              "Indicates whether to use the first page header / footer IDs for the first page. If DocumentMode is PAGELESS, this property will not be rendered.",
            ).optional(),
          }).describe("The style of the document.").optional(),
          documentStyleSuggestionState: z.object({
            backgroundSuggestionState: z.object({
              backgroundColorSuggested: z.boolean().describe(
                "Indicates whether the current background color has been modified in this suggestion.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Background have been changed in this suggestion. For any field set to true, the Backgound has a new suggested value.",
            ).optional(),
            defaultFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to default_footer_id.",
            ).optional(),
            defaultHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to default_header_id.",
            ).optional(),
            evenPageFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to even_page_footer_id.",
            ).optional(),
            evenPageHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to even_page_header_id.",
            ).optional(),
            firstPageFooterIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to first_page_footer_id.",
            ).optional(),
            firstPageHeaderIdSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to first_page_header_id.",
            ).optional(),
            flipPageOrientationSuggested: z.boolean().describe(
              "Optional. Indicates if there was a suggested change to flip_page_orientation.",
            ).optional(),
            marginBottomSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_bottom.",
            ).optional(),
            marginFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_footer.",
            ).optional(),
            marginHeaderSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_header.",
            ).optional(),
            marginLeftSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_left.",
            ).optional(),
            marginRightSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_right.",
            ).optional(),
            marginTopSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to margin_top.",
            ).optional(),
            pageNumberStartSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to page_number_start.",
            ).optional(),
            pageSizeSuggestionState: z.object({
              heightSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to height.",
              ).optional(),
              widthSuggested: z.boolean().describe(
                "Indicates if there was a suggested change to width.",
              ).optional(),
            }).describe(
              "A mask that indicates which of the fields on the base Size have been changed in this suggestion. For any field set to true, the Size has a new suggested value.",
            ).optional(),
            useCustomHeaderFooterMarginsSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_custom_header_footer_margins.",
            ).optional(),
            useEvenPageHeaderFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_even_page_header_footer.",
            ).optional(),
            useFirstPageHeaderFooterSuggested: z.boolean().describe(
              "Indicates if there was a suggested change to use_first_page_header_footer.",
            ).optional(),
          }).describe(
            "A mask that indicates which of the fields on the base DocumentStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
          ).optional(),
        }),
      ).describe(
        "The suggested changes to the style of the document tab, keyed by suggestion ID.",
      ).optional(),
      suggestedNamedStylesChanges: z.record(
        z.string(),
        z.object({
          namedStyles: z.object({
            styles: z.array(z.object({
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe("The type of this named style.").optional(),
              paragraphStyle: z.object({
                alignment: z.enum([
                  "ALIGNMENT_UNSPECIFIED",
                  "START",
                  "CENTER",
                  "END",
                  "JUSTIFIED",
                ]).describe("The text alignment for this paragraph.")
                  .optional(),
                avoidWidowAndOrphan: z.boolean().describe(
                  "Whether to avoid widows and orphans for the paragraph. If unset, the value is inherited from the parent.",
                ).optional(),
                borderBetween: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderBottom: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderLeft: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderRight: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                borderTop: z.object({
                  color: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                  dashStyle: z.enum([
                    "DASH_STYLE_UNSPECIFIED",
                    "SOLID",
                    "DOT",
                    "DASH",
                  ]).describe("The dash style of the border.").optional(),
                  padding: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                  width: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                }).describe("A border around a paragraph.").optional(),
                direction: z.enum([
                  "CONTENT_DIRECTION_UNSPECIFIED",
                  "LEFT_TO_RIGHT",
                  "RIGHT_TO_LEFT",
                ]).describe(
                  "The text direction of this paragraph. If unset, the value defaults to LEFT_TO_RIGHT since paragraph direction is not inherited.",
                ).optional(),
                headingId: z.string().describe(
                  "The heading ID of the paragraph. If empty, then this paragraph is not a heading. This property is read-only.",
                ).optional(),
                indentEnd: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentFirstLine: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                indentStart: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                keepLinesTogether: z.boolean().describe(
                  "Whether all lines of the paragraph should be laid out on the same page or column if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                keepWithNext: z.boolean().describe(
                  "Whether at least a part of this paragraph should be laid out on the same page or column as the next paragraph if possible. If unset, the value is inherited from the parent.",
                ).optional(),
                lineSpacing: z.number().describe(
                  "The amount of space between lines, as a percentage of normal, where normal is represented as 100.0. If unset, the value is inherited from the parent.",
                ).optional(),
                namedStyleType: z.enum([
                  "NAMED_STYLE_TYPE_UNSPECIFIED",
                  "NORMAL_TEXT",
                  "TITLE",
                  "SUBTITLE",
                  "HEADING_1",
                  "HEADING_2",
                  "HEADING_3",
                  "HEADING_4",
                  "HEADING_5",
                  "HEADING_6",
                ]).describe(
                  "The named style type of the paragraph. Since updating the named style type affects other properties within ParagraphStyle, the named style type is applied before the other properties are updated.",
                ).optional(),
                pageBreakBefore: z.boolean().describe(
                  "Whether the current paragraph should always start at the beginning of a page. If unset, the value is inherited from the parent. Attempting to update page_break_before for paragraphs in unsupported regions, including Table, Header, Footer and Footnote, can result in an invalid document state that returns a 400 bad request error.",
                ).optional(),
                shading: z.object({
                  backgroundColor: z.object({
                    color: z.object({
                      rgbColor: z.object({
                        blue: z.number().describe(
                          "The blue component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        green: z.number().describe(
                          "The green component of the color, from 0.0 to 1.0.",
                        ).optional(),
                        red: z.number().describe(
                          "The red component of the color, from 0.0 to 1.0.",
                        ).optional(),
                      }).describe("An RGB color.").optional(),
                    }).describe("A solid color.").optional(),
                  }).describe(
                    "A color that can either be fully opaque or fully transparent.",
                  ).optional(),
                }).describe("The shading of a paragraph.").optional(),
                spaceAbove: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spaceBelow: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                spacingMode: z.enum([
                  "SPACING_MODE_UNSPECIFIED",
                  "NEVER_COLLAPSE",
                  "COLLAPSE_LISTS",
                ]).describe("The spacing mode for the paragraph.").optional(),
                tabStops: z.array(z.object({
                  alignment: z.enum([
                    "TAB_STOP_ALIGNMENT_UNSPECIFIED",
                    "START",
                    "CENTER",
                    "END",
                  ]).describe(
                    "The alignment of this tab stop. If unset, the value defaults to START.",
                  ).optional(),
                  offset: z.object({
                    magnitude: z.number().describe("The magnitude.").optional(),
                    unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                      "The units for magnitude.",
                    ).optional(),
                  }).describe(
                    "A magnitude in a single direction in the specified units.",
                  ).optional(),
                })).describe(
                  "A list of the tab stops for this paragraph. The list of tab stops is not inherited. This property is read-only.",
                ).optional(),
              }).describe(
                "Styles that apply to a whole paragraph. Inherited paragraph styles are represented as unset fields in this message. A paragraph style's parent depends on where the paragraph style is defined: * The ParagraphStyle on a Paragraph inherits from the paragraph's corresponding named style type. * The ParagraphStyle on a named style inherits from the normal text named style. * The ParagraphStyle of the normal text named style inherits from the default paragraph style in the Docs editor. * The ParagraphStyle on a Paragraph element that's contained in a table may inherit its paragraph style from the table style. If the paragraph style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
              textStyle: z.object({
                backgroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                baselineOffset: z.enum([
                  "BASELINE_OFFSET_UNSPECIFIED",
                  "NONE",
                  "SUPERSCRIPT",
                  "SUBSCRIPT",
                ]).describe(
                  "The text's vertical offset from its normal position. Text with `SUPERSCRIPT` or `SUBSCRIPT` baseline offsets is automatically rendered in a smaller font size, computed based on the `font_size` field. Changes in this field don't affect the `font_size`.",
                ).optional(),
                bold: z.boolean().describe(
                  "Whether or not the text is rendered as bold.",
                ).optional(),
                fontSize: z.object({
                  magnitude: z.number().describe("The magnitude.").optional(),
                  unit: z.enum(["UNIT_UNSPECIFIED", "PT"]).describe(
                    "The units for magnitude.",
                  ).optional(),
                }).describe(
                  "A magnitude in a single direction in the specified units.",
                ).optional(),
                foregroundColor: z.object({
                  color: z.object({
                    rgbColor: z.object({
                      blue: z.number().describe(
                        "The blue component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      green: z.number().describe(
                        "The green component of the color, from 0.0 to 1.0.",
                      ).optional(),
                      red: z.number().describe(
                        "The red component of the color, from 0.0 to 1.0.",
                      ).optional(),
                    }).describe("An RGB color.").optional(),
                  }).describe("A solid color.").optional(),
                }).describe(
                  "A color that can either be fully opaque or fully transparent.",
                ).optional(),
                italic: z.boolean().describe(
                  "Whether or not the text is italicized.",
                ).optional(),
                link: z.object({
                  bookmark: z.object({
                    id: z.string().describe(
                      "The ID of a bookmark in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this bookmark.",
                    ).optional(),
                  }).describe("A reference to a bookmark in this document.")
                    .optional(),
                  bookmarkId: z.string().describe(
                    "The ID of a bookmark in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.bookmark for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a bookmark within the singular tab. Otherwise, Link.bookmark is returned. If this field is used in a write request, the bookmark is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  heading: z.object({
                    id: z.string().describe(
                      "The ID of a heading in this document.",
                    ).optional(),
                    tabId: z.string().describe(
                      "The ID of the tab containing this heading.",
                    ).optional(),
                  }).describe("A reference to a heading in this document.")
                    .optional(),
                  headingId: z.string().describe(
                    "The ID of a heading in this document. Legacy field: Instead, set includeTabsContent to `true` and use Link.heading for read and write operations. This field is only returned when includeTabsContent is set to `false` in documents containing a single tab and links to a heading within the singular tab. Otherwise, Link.heading is returned. If this field is used in a write request, the heading is considered to be from the tab ID specified in the request. If a tab ID is not specified in the request, it is considered to be from the first tab in the document.",
                  ).optional(),
                  tabId: z.string().describe(
                    "The ID of a tab in this document.",
                  ).optional(),
                  url: z.string().describe("An external URL.").optional(),
                }).describe(
                  "A reference to another portion of a document or an external URL resource.",
                ).optional(),
                smallCaps: z.boolean().describe(
                  "Whether or not the text is in small capital letters.",
                ).optional(),
                strikethrough: z.boolean().describe(
                  "Whether or not the text is struck through.",
                ).optional(),
                underline: z.boolean().describe(
                  "Whether or not the text is underlined.",
                ).optional(),
                weightedFontFamily: z.object({
                  fontFamily: z.string().describe(
                    "The font family of the text. The font family can be any font from the Font menu in Docs or from [Google Fonts] (https://fonts.google.com/). If the font name is unrecognized, the text is rendered in `Arial`.",
                  ).optional(),
                  weight: z.number().int().describe(
                    "The weight of the font. This field can have any value that's a multiple of `100` between `100` and `900`, inclusive. This range corresponds to the numerical values described in the CSS 2.1 Specification, [section 15.6](https://www.w3.org/TR/CSS21/fonts.html#font-boldness), with non-numerical values disallowed. The default value is `400` (\"normal\"). The font weight makes up just one component of the rendered font weight. A combination of the `weight` and the text style's resolved `bold` value determine the rendered weight, after accounting for inheritance: * If the text is bold and the weight is less than `400`, the rendered weight is 400. * If the text is bold and the weight is greater than or equal to `400` but is less than `700`, the rendered weight is `700`. * If the weight is greater than or equal to `700`, the rendered weight is equal to the weight. * If the text is not bold, the rendered weight is equal to the weight.",
                  ).optional(),
                }).describe("Represents a font family and weight of text.")
                  .optional(),
              }).describe(
                "Represents the styling that can be applied to text. Inherited text styles are represented as unset fields in this message. A text style's parent depends on where the text style is defined: * The TextStyle of text in a Paragraph inherits from the paragraph's corresponding named style type. * The TextStyle on a named style inherits from the normal text named style. * The TextStyle of the normal text named style inherits from the default text style in the Docs editor. * The TextStyle on a Paragraph element that's contained in a table may inherit its text style from the table style. If the text style does not inherit from a parent, unsetting fields will revert the style to a value matching the defaults in the Docs editor.",
              ).optional(),
            })).describe(
              "The named styles. There's an entry for each of the possible named style types.",
            ).optional(),
          }).describe(
            "The named styles. Paragraphs in the document can inherit their TextStyle and ParagraphStyle from these named styles.",
          ).optional(),
          namedStylesSuggestionState: z.object({
            stylesSuggestionStates: z.array(z.object({
              namedStyleType: z.enum([
                "NAMED_STYLE_TYPE_UNSPECIFIED",
                "NORMAL_TEXT",
                "TITLE",
                "SUBTITLE",
                "HEADING_1",
                "HEADING_2",
                "HEADING_3",
                "HEADING_4",
                "HEADING_5",
                "HEADING_6",
              ]).describe(
                "The named style type that this suggestion state corresponds to. This field is provided as a convenience for matching the NamedStyleSuggestionState with its corresponding NamedStyle.",
              ).optional(),
              paragraphStyleSuggestionState: z.object({
                alignmentSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to alignment.",
                ).optional(),
                avoidWidowAndOrphanSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to avoid_widow_and_orphan.",
                ).optional(),
                borderBetweenSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_between.",
                ).optional(),
                borderBottomSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_bottom.",
                ).optional(),
                borderLeftSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_left.",
                ).optional(),
                borderRightSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_right.",
                ).optional(),
                borderTopSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to border_top.",
                ).optional(),
                directionSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to direction.",
                ).optional(),
                headingIdSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to heading_id.",
                ).optional(),
                indentEndSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_end.",
                ).optional(),
                indentFirstLineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_first_line.",
                ).optional(),
                indentStartSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to indent_start.",
                ).optional(),
                keepLinesTogetherSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to keep_lines_together.",
                ).optional(),
                keepWithNextSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to keep_with_next.",
                ).optional(),
                lineSpacingSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to line_spacing.",
                ).optional(),
                namedStyleTypeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to named_style_type.",
                ).optional(),
                pageBreakBeforeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to page_break_before.",
                ).optional(),
                shadingSuggestionState: z.object({
                  backgroundColorSuggested: z.boolean().describe(
                    "Indicates if there was a suggested change to the Shading.",
                  ).optional(),
                }).describe(
                  "A mask that indicates which of the fields on the base Shading have been changed in this suggested change. For any field set to true, there's a new suggested value.",
                ).optional(),
                spaceAboveSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to space_above.",
                ).optional(),
                spaceBelowSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to space_below.",
                ).optional(),
                spacingModeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to spacing_mode.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base ParagraphStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
              textStyleSuggestionState: z.object({
                backgroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to background_color.",
                ).optional(),
                baselineOffsetSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to baseline_offset.",
                ).optional(),
                boldSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to bold.",
                ).optional(),
                fontSizeSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to font_size.",
                ).optional(),
                foregroundColorSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to foreground_color.",
                ).optional(),
                italicSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to italic.",
                ).optional(),
                linkSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to link.",
                ).optional(),
                smallCapsSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to small_caps.",
                ).optional(),
                strikethroughSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to strikethrough.",
                ).optional(),
                underlineSuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to underline.",
                ).optional(),
                weightedFontFamilySuggested: z.boolean().describe(
                  "Indicates if there was a suggested change to weighted_font_family.",
                ).optional(),
              }).describe(
                "A mask that indicates which of the fields on the base TextStyle have been changed in this suggestion. For any field set to true, there's a new suggested value.",
              ).optional(),
            })).describe(
              "A mask that indicates which of the fields on the corresponding NamedStyle in styles have been changed in this suggestion. The order of these named style suggestion states matches the order of the corresponding named style within the named styles suggestion.",
            ).optional(),
          }).describe("The suggestion state of a NamedStyles message.")
            .optional(),
        }),
      ).describe(
        "The suggested changes to the named styles of the document tab, keyed by suggestion ID.",
      ).optional(),
    }).describe("A tab with document contents.").optional(),
    tabProperties: z.object({
      iconEmoji: z.string().describe(
        "Optional. The emoji icon displayed with the tab. A valid emoji icon is represented by a non-empty Unicode string. Any set of characters that don't represent a single emoji is invalid. If an emoji is invalid, a 400 bad request error is returned. If this value is unset or empty, the tab will display the default tab icon.",
      ).optional(),
      index: z.number().int().describe(
        "The zero-based index of the tab within the parent.",
      ).optional(),
      nestingLevel: z.number().int().describe(
        "Output only. The depth of the tab within the document. Root-level tabs start at 0.",
      ).optional(),
      parentTabId: z.string().describe(
        "Optional. The ID of the parent tab. Empty when the current tab is a root-level tab, which means it doesn't have any parents.",
      ).optional(),
      tabId: z.string().describe("The immutable ID of the tab.").optional(),
      title: z.string().describe("The user-visible name of the tab.")
        .optional(),
    }).describe("Properties of a tab.").optional(),
  })).describe(
    "Tabs that are part of a document. Tabs can contain child tabs, a tab nested within another tab. Child tabs are represented by the Tab.childTabs field.",
  ).optional(),
  title: z.string().describe("The title of the document.").optional(),
});

export const model = {
  type: "@swamp/gcp/docs/documents",
  version: "2026.04.03.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.02.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "A Google Docs document.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a documents",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const body: Record<string, unknown> = {};
        if (g["body"] !== undefined) body["body"] = g["body"];
        if (g["documentStyle"] !== undefined) {
          body["documentStyle"] = g["documentStyle"];
        }
        if (g["namedStyles"] !== undefined) {
          body["namedStyles"] = g["namedStyles"];
        }
        if (g["tabs"] !== undefined) body["tabs"] = g["tabs"];
        if (g["title"] !== undefined) body["title"] = g["title"];
        if (g["name"] !== undefined) params["documentId"] = String(g["name"]);
        const result = await createResource(
          BASE_URL,
          INSERT_CONFIG,
          params,
          body,
          GET_CONFIG,
        ) as StateData;
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a documents",
      arguments: z.object({
        identifier: z.string().describe("The name of the documents"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["documentId"] = args.identifier;
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName = (g.name?.toString() ?? args.identifier).replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync documents state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const identifier = existing.name?.toString() ?? g["name"]?.toString();
          if (!identifier) {
            throw new Error(
              "No identifier found in existing state or globalArgs",
            );
          }
          params["documentId"] = identifier;
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
    batch_update: {
      description: "batch update",
      arguments: z.object({
        requests: z.any().optional(),
        writeControl: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          (g.name?.toString() ?? "current").replace(/[\/\\]/g, "_").replace(
            /\.\./,
            "_",
          ),
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        params["documentId"] = existing["name"]?.toString() ??
          g["name"]?.toString() ?? "";
        const body: Record<string, unknown> = {};
        if (args["requests"] !== undefined) body["requests"] = args["requests"];
        if (args["writeControl"] !== undefined) {
          body["writeControl"] = args["writeControl"];
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "docs.documents.batchUpdate",
            "path": "v1/documents/{documentId}:batchUpdate",
            "httpMethod": "POST",
            "parameterOrder": ["documentId"],
            "parameters": {
              "documentId": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
  },
};
