// Auto-generated extension model for @swamp/gcp/firebaserules/rulesets
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  deleteResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
} from "./_lib/gcp.ts";

const BASE_URL = "https://firebaserules.googleapis.com/";

const GET_CONFIG = {
  "id": "firebaserules.projects.rulesets.get",
  "path": "v1/{+name}",
  "httpMethod": "GET",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const INSERT_CONFIG = {
  "id": "firebaserules.projects.rulesets.create",
  "path": "v1/{+name}/rulesets",
  "httpMethod": "POST",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const DELETE_CONFIG = {
  "id": "firebaserules.projects.rulesets.delete",
  "path": "v1/{+name}",
  "httpMethod": "DELETE",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  attachmentPoint: z.string().describe(
    "Immutable. Intended resource to which this Ruleset should be released. May be left blank to signify the resource associated with the default release. Expected format: firestore.googleapis.com/projects//databases/",
  ).optional(),
  metadata: z.object({
    services: z.array(z.string()).describe(
      'Services that this ruleset has declarations for (e.g., "cloud.firestore"). There may be 0+ of these.',
    ).optional(),
  }).describe("Metadata for a Ruleset.").optional(),
  name: z.string().describe(
    "Output only. Name of the `Ruleset`. The ruleset_id is auto generated by the service. Format: `projects/{project_id}/rulesets/{ruleset_id}`",
  ).optional(),
  source: z.object({
    files: z.array(z.object({
      content: z.string().describe("Required. Textual Content.").optional(),
      fingerprint: z.string().describe(
        "Fingerprint (e.g. github sha) associated with the `File`.",
      ).optional(),
      name: z.string().describe("Required. File name.").optional(),
    })).describe("Required. `File` set constituting the `Source` bundle.")
      .optional(),
  }).describe(
    "`Source` is one or more `File` messages comprising a logical set of rules.",
  ).optional(),
});

const StateSchema = z.object({
  attachmentPoint: z.string().optional(),
  createTime: z.string().optional(),
  metadata: z.object({
    services: z.array(z.string()),
  }).optional(),
  name: z.string(),
  source: z.object({
    files: z.array(z.object({
      content: z.string(),
      fingerprint: z.string(),
      name: z.string(),
    })),
  }).optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  attachmentPoint: z.string().describe(
    "Immutable. Intended resource to which this Ruleset should be released. May be left blank to signify the resource associated with the default release. Expected format: firestore.googleapis.com/projects//databases/",
  ).optional(),
  metadata: z.object({
    services: z.array(z.string()).describe(
      'Services that this ruleset has declarations for (e.g., "cloud.firestore"). There may be 0+ of these.',
    ).optional(),
  }).describe("Metadata for a Ruleset.").optional(),
  name: z.string().describe(
    "Output only. Name of the `Ruleset`. The ruleset_id is auto generated by the service. Format: `projects/{project_id}/rulesets/{ruleset_id}`",
  ).optional(),
  source: z.object({
    files: z.array(z.object({
      content: z.string().describe("Required. Textual Content.").optional(),
      fingerprint: z.string().describe(
        "Fingerprint (e.g. github sha) associated with the `File`.",
      ).optional(),
      name: z.string().describe("Required. File name.").optional(),
    })).describe("Required. `File` set constituting the `Source` bundle.")
      .optional(),
  }).describe(
    "`Source` is one or more `File` messages comprising a logical set of rules.",
  ).optional(),
});

export const model = {
  type: "@swamp/gcp/firebaserules/rulesets",
  version: "2026.04.03.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.02.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description:
        "`Ruleset` is an immutable copy of `Source` with a globally unique identifier ...",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a rulesets",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["name"] !== undefined) params["name"] = String(g["name"]);
        const body: Record<string, unknown> = {};
        if (g["attachmentPoint"] !== undefined) {
          body["attachmentPoint"] = g["attachmentPoint"];
        }
        if (g["metadata"] !== undefined) body["metadata"] = g["metadata"];
        if (g["source"] !== undefined) body["source"] = g["source"];
        const result = await createResource(
          BASE_URL,
          INSERT_CONFIG,
          params,
          body,
          GET_CONFIG,
        ) as StateData;
        const instanceName = ((result.name ?? g.name)?.toString() ?? "current")
          .replace(/[\/\\]/g, "_").replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a rulesets",
      arguments: z.object({
        identifier: z.string().describe("The name of the rulesets"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["name"] = args.identifier;
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName =
          ((result.name ?? g.name)?.toString() ?? args.identifier).replace(
            /[\/\\]/g,
            "_",
          ).replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete the rulesets",
      arguments: z.object({
        identifier: z.string().describe("The name of the rulesets"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        params["name"] = args.identifier;
        const { existed } = await deleteResource(
          BASE_URL,
          DELETE_CONFIG,
          params,
        );
        const instanceName = (g.name?.toString() ?? args.identifier).replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const handle = await context.writeResource("state", instanceName, {
          identifier: args.identifier,
          existed,
          status: existed ? "deleted" : "not_found",
          deletedAt: new Date().toISOString(),
        });
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync rulesets state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const identifier = existing.name?.toString() ?? g["name"]?.toString();
          if (!identifier) {
            throw new Error(
              "No identifier found in existing state or globalArgs",
            );
          }
          params["name"] = identifier;
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
  },
};
