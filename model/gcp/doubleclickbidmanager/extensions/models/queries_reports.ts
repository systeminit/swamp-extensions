// Auto-generated extension model for @swamp/gcp/doubleclickbidmanager/queries-reports
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  getProjectId,
  isResourceNotFoundError,
  readResource,
} from "./_lib/gcp.ts";

const BASE_URL = "https://doubleclickbidmanager.googleapis.com/v2/";

const GET_CONFIG = {
  "id": "doubleclickbidmanager.queries.reports.get",
  "path": "queries/{queryId}/reports/{reportId}",
  "httpMethod": "GET",
  "parameterOrder": [
    "queryId",
    "reportId",
  ],
  "parameters": {
    "queryId": {
      "location": "path",
      "required": true,
    },
    "reportId": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  name: z.string().describe(
    "Instance name for this resource (used as the unique identifier in the factory pattern)",
  ),
});

const StateSchema = z.object({
  key: z.object({
    queryId: z.string(),
    reportId: z.string(),
  }).optional(),
  metadata: z.object({
    googleCloudStoragePath: z.string(),
    reportDataEndDate: z.object({
      day: z.number(),
      month: z.number(),
      year: z.number(),
    }),
    reportDataStartDate: z.object({
      day: z.number(),
      month: z.number(),
      year: z.number(),
    }),
    status: z.object({
      finishTime: z.string(),
      format: z.string(),
      state: z.string(),
    }),
  }).optional(),
  params: z.object({
    filters: z.array(z.object({
      type: z.string(),
      value: z.string(),
    })),
    groupBys: z.array(z.string()),
    metrics: z.array(z.string()),
    options: z.object({
      includeOnlyTargetedUserLists: z.boolean(),
    }),
    type: z.string(),
  }).optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  name: z.string().optional(),
});

export const model = {
  type: "@swamp/gcp/doubleclickbidmanager/queries-reports",
  version: "2026.03.27.1",
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "A single report generated by its parent report.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    get: {
      description: "Get a reports",
      arguments: z.object({
        identifier: z.string().describe("The name of the reports"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        if (g["queryId"] !== undefined) {
          params["queryId"] = String(g["queryId"]);
        }
        params["reportId"] = args.identifier;
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName = g.name?.toString() ?? args.identifier;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync reports state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          if (g["queryId"] !== undefined) {
            params["queryId"] = String(g["queryId"]);
          } else if (existing["queryId"]) {
            params["queryId"] = String(existing["queryId"]);
          }
          const identifier = existing.name?.toString() ?? g["name"]?.toString();
          if (!identifier) {
            throw new Error(
              "No identifier found in existing state or globalArgs",
            );
          }
          params["reportId"] = identifier;
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
  },
};
