// Auto-generated extension model for @swamp/gcp/baremetalsolution/instances
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/gcp.ts";

/** Construct the fully-qualified resource name from parent and short name. */
function buildResourceName(parent: string, shortName: string): string {
  return `${parent}/instances/${shortName}`;
}

const BASE_URL = "https://baremetalsolution.googleapis.com/";

const GET_CONFIG = {
  "id": "baremetalsolution.projects.locations.instances.get",
  "path": "v2/{+name}",
  "httpMethod": "GET",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const PATCH_CONFIG = {
  "id": "baremetalsolution.projects.locations.instances.patch",
  "path": "v2/{+name}",
  "httpMethod": "PATCH",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
    "updateMask": {
      "location": "query",
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  createTime: z.string().describe("Output only. Create a time stamp.")
    .optional(),
  firmwareVersion: z.string().describe(
    "Output only. The firmware version for the instance.",
  ).optional(),
  hyperthreadingEnabled: z.boolean().describe(
    "True if you enable hyperthreading for the server, otherwise false. The default value is false.",
  ).optional(),
  id: z.string().describe(
    "Output only. An identifier for the `Instance`, generated by the backend.",
  ).optional(),
  interactiveSerialConsoleEnabled: z.boolean().describe(
    "Output only. True if the interactive serial console feature is enabled for the instance, false otherwise. The default value is false.",
  ).optional(),
  kmsKeyVersion: z.string().describe(
    "Optional. Name of the KMS crypto key version used to encrypt the initial passwords. The key has to have ASYMMETRIC_DECRYPT purpose. Format is `projects/{project}/locations/{location}/keyRings/{keyring}/cryptoKeys/{key}/cryptoKeyVersions/{version}`.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  logicalInterfaces: z.array(z.object({
    interfaceIndex: z.number().int().describe(
      "The index of the logical interface mapping to the index of the hardware bond or nic on the chosen network template. This field is deprecated.",
    ).optional(),
    logicalNetworkInterfaces: z.array(z.object({
      defaultGateway: z.boolean().describe(
        "Whether this interface is the default gateway for the instance. Only one interface can be the default gateway for the instance.",
      ).optional(),
      id: z.string().describe(
        "An identifier for the `Network`, generated by the backend.",
      ).optional(),
      ipAddress: z.string().describe("IP address in the network").optional(),
      network: z.string().describe("Name of the network").optional(),
      networkType: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
        "Type of network.",
      ).optional(),
    })).describe(
      "List of logical network interfaces within a logical interface.",
    ).optional(),
    name: z.string().describe(
      "Interface name. This is of syntax or and forms part of the network template name.",
    ).optional(),
  })).describe(
    "List of logical interfaces for the instance. The number of logical interfaces will be the same as number of hardware bond/nic on the chosen network template. For the non-multivlan configurations (for eg, existing servers) that use existing default network template (bondaa-bondaa), both the Instance.networks field and the Instance.logical_interfaces fields will be filled to ensure backward compatibility. For the others, only Instance.logical_interfaces will be filled.",
  ).optional(),
  loginInfo: z.string().describe(
    "Output only. Text field about info for logging in.",
  ).optional(),
  luns: z.array(z.object({
    bootLun: z.boolean().describe("Display if this LUN is a boot LUN.")
      .optional(),
    expireTime: z.string().describe(
      "Output only. Time after which LUN will be fully deleted. It is filled only for LUNs in COOL_OFF state.",
    ).optional(),
    id: z.string().describe(
      "An identifier for the LUN, generated by the backend.",
    ).optional(),
    instances: z.array(z.string()).describe(
      "Output only. Instances this Lun is attached to.",
    ).optional(),
    multiprotocolType: z.enum(["MULTIPROTOCOL_TYPE_UNSPECIFIED", "LINUX"])
      .describe(
        "The LUN multiprotocol type ensures the characteristics of the LUN are optimized for each operating system.",
      ).optional(),
    name: z.string().describe("Output only. The name of the LUN.").optional(),
    shareable: z.boolean().describe(
      "Display if this LUN can be shared between multiple physical servers.",
    ).optional(),
    sizeGb: z.string().describe("The size of this LUN, in GiB.").optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "CREATING",
      "UPDATING",
      "READY",
      "DELETING",
      "COOL_OFF",
    ]).describe("The state of this storage volume.").optional(),
    storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
      "The storage type for this LUN.",
    ).optional(),
    storageVolume: z.string().describe(
      "Display the storage volume for this LUN.",
    ).optional(),
    wwid: z.string().describe("The WWID for this LUN.").optional(),
  })).describe("Immutable. List of LUNs associated with this server.")
    .optional(),
  machineType: z.string().describe(
    "Immutable. The server type. [Available server types](https://cloud.google.com/bare-metal/docs/bms-planning#server_configurations)",
  ).optional(),
  name: z.string().describe(
    "Immutable. The resource name of this `Instance`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/instances/{instance}`",
  ).optional(),
  networkTemplate: z.string().describe(
    'Instance network template name. For eg, bondaa-bondaa, bondab-nic, etc. Generally, the template name follows the syntax of "bond" or "nic".',
  ).optional(),
  networks: z.array(z.object({
    cidr: z.string().describe("The cidr of the Network.").optional(),
    gatewayIp: z.string().describe("Output only. Gateway ip address.")
      .optional(),
    id: z.string().describe(
      "An identifier for the `Network`, generated by the backend.",
    ).optional(),
    ipAddress: z.string().describe("IP address configured.").optional(),
    jumboFramesEnabled: z.boolean().describe(
      "Whether network uses standard frames or jumbo ones.",
    ).optional(),
    labels: z.record(z.string(), z.string()).describe(
      "Labels as key value pairs.",
    ).optional(),
    macAddress: z.array(z.string()).describe("List of physical interfaces.")
      .optional(),
    mountPoints: z.array(z.object({
      defaultGateway: z.boolean().describe(
        "Network should be a default gateway.",
      ).optional(),
      instance: z.string().describe("Instance to attach network to.")
        .optional(),
      ipAddress: z.string().describe("Ip address of the server.").optional(),
      logicalInterface: z.string().describe("Logical interface to detach from.")
        .optional(),
    })).describe("Input only. List of mount points to attach the network to.")
      .optional(),
    name: z.string().describe(
      "Output only. The resource name of this `Network`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/networks/{network}`",
    ).optional(),
    pod: z.string().describe(
      "Immutable. Pod name. Pod is an independent part of infrastructure. Network can only be connected to the assets (instances, nfsshares) allocated in the same pod.",
    ).optional(),
    reservations: z.array(z.object({
      endAddress: z.string().describe(
        "The last address of this reservation block, inclusive. I.e., for cases when reservations are only single addresses, end_address and start_address will be the same. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
      ).optional(),
      note: z.string().describe(
        "A note about this reservation, intended for human consumption.",
      ).optional(),
      startAddress: z.string().describe(
        "The first address of this reservation block. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
      ).optional(),
    })).describe(
      "List of IP address reservations in this network. When updating this field, an error will be generated if a reservation conflicts with an IP address already allocated to a physical server.",
    ).optional(),
    servicesCidr: z.string().describe(
      "IP range for reserved for services (e.g. NFS).",
    ).optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "PROVISIONING",
      "PROVISIONED",
      "DEPROVISIONING",
      "UPDATING",
    ]).describe("The Network state.").optional(),
    type: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
      "The type of this network.",
    ).optional(),
    vlanId: z.string().describe("The vlan id of the Network.").optional(),
    vrf: z.object({
      name: z.string().describe("The name of the VRF.").optional(),
      qosPolicy: z.object({
        bandwidthGbps: z.number().describe(
          "The bandwidth permitted by the QOS policy, in gbps.",
        ).optional(),
      }).describe("QOS policy parameters.").optional(),
      state: z.enum(["STATE_UNSPECIFIED", "PROVISIONING", "PROVISIONED"])
        .describe("The possible state of VRF.").optional(),
      vlanAttachments: z.array(z.object({
        id: z.string().describe(
          "Immutable. The identifier of the attachment within vrf.",
        ).optional(),
        interconnectAttachment: z.string().describe(
          "Optional. The name of the vlan attachment within vrf. This is of the form projects/{project_number}/regions/{region}/interconnectAttachments/{interconnect_attachment}",
        ).optional(),
        pairingKey: z.string().describe("Input only. Pairing key.").optional(),
        peerIp: z.string().describe("The peer IP of the attachment.")
          .optional(),
        peerVlanId: z.string().describe("The peer vlan ID of the attachment.")
          .optional(),
        qosPolicy: z.object({
          bandwidthGbps: z.number().describe(
            "The bandwidth permitted by the QOS policy, in gbps.",
          ).optional(),
        }).describe("QOS policy parameters.").optional(),
        routerIp: z.string().describe("The router IP of the attachment.")
          .optional(),
      })).describe("The list of VLAN attachments for the VRF.").optional(),
    }).describe("A network VRF.").optional(),
    vrfAttachment: z.string().describe(
      "Optional. The name of a pre-existing Vrf that the network should be attached to. Format is `vrfs/{vrf}`.",
    ).optional(),
  })).describe("Output only. List of networks associated with this server.")
    .optional(),
  osImage: z.string().describe(
    "The OS image currently installed on the server.",
  ).optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Instance can only be connected to the assets (networks, volumes) allocated in the same pod.",
  ).optional(),
  sshKeys: z.array(z.string()).describe(
    "Optional. List of SSH Keys used during instance provisioning.",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "PROVISIONING",
    "RUNNING",
    "DELETED",
    "UPDATING",
    "STARTING",
    "STOPPING",
    "SHUTDOWN",
  ]).describe("Output only. The state of the server.").optional(),
  updateTime: z.string().describe("Output only. Update a time stamp.")
    .optional(),
  volumes: z.array(z.object({
    attached: z.boolean().describe(
      "Output only. Is the Volume attached at at least one instance. This field is a lightweight counterpart of `instances` field. It is filled in List responses as well.",
    ).optional(),
    autoGrownSizeGib: z.string().describe(
      "The size, in GiB, that this storage volume has expanded as a result of an auto grow policy. In the absence of auto-grow, the value is 0.",
    ).optional(),
    bootVolume: z.boolean().describe(
      "Output only. Whether this volume is a boot volume. A boot volume is one which contains a boot LUN.",
    ).optional(),
    currentSizeGib: z.string().describe(
      "The current size of this storage volume, in GiB, including space reserved for snapshots. This size might be different than the requested size if the storage volume has been configured with auto grow or auto shrink.",
    ).optional(),
    emergencySizeGib: z.string().describe(
      "Additional emergency size that was requested for this Volume, in GiB. current_size_gib includes this value.",
    ).optional(),
    expireTime: z.string().describe(
      "Output only. Time after which volume will be fully deleted. It is filled only for volumes in COOLOFF state.",
    ).optional(),
    id: z.string().describe(
      "An identifier for the `Volume`, generated by the backend.",
    ).optional(),
    instances: z.array(z.string()).describe(
      "Output only. Instances this Volume is attached to. This field is set only in Get requests.",
    ).optional(),
    labels: z.record(z.string(), z.string()).describe(
      "Labels as key value pairs.",
    ).optional(),
    maxSizeGib: z.string().describe(
      "Maximum size volume can be expanded to in case of evergency, in GiB.",
    ).optional(),
    name: z.string().describe(
      "Output only. The resource name of this `Volume`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/volumes/{volume}`",
    ).optional(),
    notes: z.string().describe(
      "Input only. User-specified notes for new Volume. Used to provision Volumes that require manual intervention.",
    ).optional(),
    originallyRequestedSizeGib: z.string().describe(
      "Originally requested size, in GiB.",
    ).optional(),
    performanceTier: z.enum([
      "VOLUME_PERFORMANCE_TIER_UNSPECIFIED",
      "VOLUME_PERFORMANCE_TIER_SHARED",
      "VOLUME_PERFORMANCE_TIER_ASSIGNED",
      "VOLUME_PERFORMANCE_TIER_HT",
      "VOLUME_PERFORMANCE_TIER_QOS2_PERFORMANCE",
    ]).describe("Immutable. Performance tier of the Volume. Default is SHARED.")
      .optional(),
    pod: z.string().describe(
      "Immutable. Pod name. Pod is an independent part of infrastructure. Volume can only be connected to the instances allocated in the same pod.",
    ).optional(),
    protocol: z.enum(["PROTOCOL_UNSPECIFIED", "FIBRE_CHANNEL", "NFS"]).describe(
      "Output only. Storage protocol for the Volume.",
    ).optional(),
    remainingSpaceGib: z.string().describe(
      "The space remaining in the storage volume for new LUNs, in GiB, excluding space reserved for snapshots.",
    ).optional(),
    requestedSizeGib: z.string().describe(
      "The requested size of this storage volume, in GiB.",
    ).optional(),
    snapshotAutoDeleteBehavior: z.enum([
      "SNAPSHOT_AUTO_DELETE_BEHAVIOR_UNSPECIFIED",
      "DISABLED",
      "OLDEST_FIRST",
      "NEWEST_FIRST",
    ]).describe("The behavior to use when snapshot reserved space is full.")
      .optional(),
    snapshotEnabled: z.boolean().describe("Whether snapshots are enabled.")
      .optional(),
    snapshotReservationDetail: z.object({
      reservedSpaceGib: z.string().describe(
        "The space on this storage volume reserved for snapshots, shown in GiB.",
      ).optional(),
      reservedSpacePercent: z.number().int().describe(
        "Percent of the total Volume size reserved for snapshot copies. Enabling snapshots requires reserving 20% or more of the storage volume space for snapshots. Maximum reserved space for snapshots is 40%. Setting this field will effectively set snapshot_enabled to true.",
      ).optional(),
      reservedSpaceRemainingGib: z.string().describe(
        "The amount, in GiB, of available space in this storage volume's reserved snapshot space.",
      ).optional(),
      reservedSpaceUsedPercent: z.number().int().describe(
        "The percent of snapshot space on this storage volume actually being used by the snapshot copies. This value might be higher than 100% if the snapshot copies have overflowed into the data portion of the storage volume.",
      ).optional(),
    }).describe(
      "Details about snapshot space reservation and usage on the storage volume.",
    ).optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "CREATING",
      "READY",
      "DELETING",
      "UPDATING",
      "COOL_OFF",
    ]).describe("The state of this storage volume.").optional(),
    storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
      "The storage type for this volume.",
    ).optional(),
    workloadProfile: z.enum(["WORKLOAD_PROFILE_UNSPECIFIED", "GENERIC", "HANA"])
      .describe("The workload profile for the volume.").optional(),
  })).describe(
    "Input only. List of Volumes to attach to this Instance on creation. This field won't be populated in Get/List responses.",
  ).optional(),
  workloadProfile: z.enum([
    "WORKLOAD_PROFILE_UNSPECIFIED",
    "WORKLOAD_PROFILE_GENERIC",
    "WORKLOAD_PROFILE_HANA",
  ]).describe("The workload profile for the instance.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

const StateSchema = z.object({
  createTime: z.string().optional(),
  firmwareVersion: z.string().optional(),
  hyperthreadingEnabled: z.boolean().optional(),
  id: z.string().optional(),
  interactiveSerialConsoleEnabled: z.boolean().optional(),
  kmsKeyVersion: z.string().optional(),
  labels: z.record(z.string(), z.unknown()).optional(),
  logicalInterfaces: z.array(z.object({
    interfaceIndex: z.number(),
    logicalNetworkInterfaces: z.array(z.object({
      defaultGateway: z.boolean(),
      id: z.string(),
      ipAddress: z.string(),
      network: z.string(),
      networkType: z.string(),
    })),
    name: z.string(),
  })).optional(),
  loginInfo: z.string().optional(),
  luns: z.array(z.object({
    bootLun: z.boolean(),
    expireTime: z.string(),
    id: z.string(),
    instances: z.array(z.string()),
    multiprotocolType: z.string(),
    name: z.string(),
    shareable: z.boolean(),
    sizeGb: z.string(),
    state: z.string(),
    storageType: z.string(),
    storageVolume: z.string(),
    wwid: z.string(),
  })).optional(),
  machineType: z.string().optional(),
  name: z.string(),
  networkTemplate: z.string().optional(),
  networks: z.array(z.object({
    cidr: z.string(),
    gatewayIp: z.string(),
    id: z.string(),
    ipAddress: z.string(),
    jumboFramesEnabled: z.boolean(),
    labels: z.record(z.string(), z.unknown()),
    macAddress: z.array(z.string()),
    mountPoints: z.array(z.object({
      defaultGateway: z.boolean(),
      instance: z.string(),
      ipAddress: z.string(),
      logicalInterface: z.string(),
    })),
    name: z.string(),
    pod: z.string(),
    reservations: z.array(z.object({
      endAddress: z.string(),
      note: z.string(),
      startAddress: z.string(),
    })),
    servicesCidr: z.string(),
    state: z.string(),
    type: z.string(),
    vlanId: z.string(),
    vrf: z.object({
      name: z.string(),
      qosPolicy: z.object({
        bandwidthGbps: z.number(),
      }),
      state: z.string(),
      vlanAttachments: z.array(z.object({
        id: z.string(),
        interconnectAttachment: z.string(),
        pairingKey: z.string(),
        peerIp: z.string(),
        peerVlanId: z.string(),
        qosPolicy: z.object({
          bandwidthGbps: z.number(),
        }),
        routerIp: z.string(),
      })),
    }),
    vrfAttachment: z.string(),
  })).optional(),
  osImage: z.string().optional(),
  pod: z.string().optional(),
  sshKeys: z.array(z.string()).optional(),
  state: z.string().optional(),
  updateTime: z.string().optional(),
  volumes: z.array(z.object({
    attached: z.boolean(),
    autoGrownSizeGib: z.string(),
    bootVolume: z.boolean(),
    currentSizeGib: z.string(),
    emergencySizeGib: z.string(),
    expireTime: z.string(),
    id: z.string(),
    instances: z.array(z.string()),
    labels: z.record(z.string(), z.unknown()),
    maxSizeGib: z.string(),
    name: z.string(),
    notes: z.string(),
    originallyRequestedSizeGib: z.string(),
    performanceTier: z.string(),
    pod: z.string(),
    protocol: z.string(),
    remainingSpaceGib: z.string(),
    requestedSizeGib: z.string(),
    snapshotAutoDeleteBehavior: z.string(),
    snapshotEnabled: z.boolean(),
    snapshotReservationDetail: z.object({
      reservedSpaceGib: z.string(),
      reservedSpacePercent: z.number(),
      reservedSpaceRemainingGib: z.string(),
      reservedSpaceUsedPercent: z.number(),
    }),
    state: z.string(),
    storageType: z.string(),
    workloadProfile: z.string(),
  })).optional(),
  workloadProfile: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  createTime: z.string().describe("Output only. Create a time stamp.")
    .optional(),
  firmwareVersion: z.string().describe(
    "Output only. The firmware version for the instance.",
  ).optional(),
  hyperthreadingEnabled: z.boolean().describe(
    "True if you enable hyperthreading for the server, otherwise false. The default value is false.",
  ).optional(),
  id: z.string().describe(
    "Output only. An identifier for the `Instance`, generated by the backend.",
  ).optional(),
  interactiveSerialConsoleEnabled: z.boolean().describe(
    "Output only. True if the interactive serial console feature is enabled for the instance, false otherwise. The default value is false.",
  ).optional(),
  kmsKeyVersion: z.string().describe(
    "Optional. Name of the KMS crypto key version used to encrypt the initial passwords. The key has to have ASYMMETRIC_DECRYPT purpose. Format is `projects/{project}/locations/{location}/keyRings/{keyring}/cryptoKeys/{key}/cryptoKeyVersions/{version}`.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  logicalInterfaces: z.array(z.object({
    interfaceIndex: z.number().int().describe(
      "The index of the logical interface mapping to the index of the hardware bond or nic on the chosen network template. This field is deprecated.",
    ).optional(),
    logicalNetworkInterfaces: z.array(z.object({
      defaultGateway: z.boolean().describe(
        "Whether this interface is the default gateway for the instance. Only one interface can be the default gateway for the instance.",
      ).optional(),
      id: z.string().describe(
        "An identifier for the `Network`, generated by the backend.",
      ).optional(),
      ipAddress: z.string().describe("IP address in the network").optional(),
      network: z.string().describe("Name of the network").optional(),
      networkType: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
        "Type of network.",
      ).optional(),
    })).describe(
      "List of logical network interfaces within a logical interface.",
    ).optional(),
    name: z.string().describe(
      "Interface name. This is of syntax or and forms part of the network template name.",
    ).optional(),
  })).describe(
    "List of logical interfaces for the instance. The number of logical interfaces will be the same as number of hardware bond/nic on the chosen network template. For the non-multivlan configurations (for eg, existing servers) that use existing default network template (bondaa-bondaa), both the Instance.networks field and the Instance.logical_interfaces fields will be filled to ensure backward compatibility. For the others, only Instance.logical_interfaces will be filled.",
  ).optional(),
  loginInfo: z.string().describe(
    "Output only. Text field about info for logging in.",
  ).optional(),
  luns: z.array(z.object({
    bootLun: z.boolean().describe("Display if this LUN is a boot LUN.")
      .optional(),
    expireTime: z.string().describe(
      "Output only. Time after which LUN will be fully deleted. It is filled only for LUNs in COOL_OFF state.",
    ).optional(),
    id: z.string().describe(
      "An identifier for the LUN, generated by the backend.",
    ).optional(),
    instances: z.array(z.string()).describe(
      "Output only. Instances this Lun is attached to.",
    ).optional(),
    multiprotocolType: z.enum(["MULTIPROTOCOL_TYPE_UNSPECIFIED", "LINUX"])
      .describe(
        "The LUN multiprotocol type ensures the characteristics of the LUN are optimized for each operating system.",
      ).optional(),
    name: z.string().describe("Output only. The name of the LUN.").optional(),
    shareable: z.boolean().describe(
      "Display if this LUN can be shared between multiple physical servers.",
    ).optional(),
    sizeGb: z.string().describe("The size of this LUN, in GiB.").optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "CREATING",
      "UPDATING",
      "READY",
      "DELETING",
      "COOL_OFF",
    ]).describe("The state of this storage volume.").optional(),
    storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
      "The storage type for this LUN.",
    ).optional(),
    storageVolume: z.string().describe(
      "Display the storage volume for this LUN.",
    ).optional(),
    wwid: z.string().describe("The WWID for this LUN.").optional(),
  })).describe("Immutable. List of LUNs associated with this server.")
    .optional(),
  machineType: z.string().describe(
    "Immutable. The server type. [Available server types](https://cloud.google.com/bare-metal/docs/bms-planning#server_configurations)",
  ).optional(),
  name: z.string().describe(
    "Immutable. The resource name of this `Instance`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/instances/{instance}`",
  ).optional(),
  networkTemplate: z.string().describe(
    'Instance network template name. For eg, bondaa-bondaa, bondab-nic, etc. Generally, the template name follows the syntax of "bond" or "nic".',
  ).optional(),
  networks: z.array(z.object({
    cidr: z.string().describe("The cidr of the Network.").optional(),
    gatewayIp: z.string().describe("Output only. Gateway ip address.")
      .optional(),
    id: z.string().describe(
      "An identifier for the `Network`, generated by the backend.",
    ).optional(),
    ipAddress: z.string().describe("IP address configured.").optional(),
    jumboFramesEnabled: z.boolean().describe(
      "Whether network uses standard frames or jumbo ones.",
    ).optional(),
    labels: z.record(z.string(), z.string()).describe(
      "Labels as key value pairs.",
    ).optional(),
    macAddress: z.array(z.string()).describe("List of physical interfaces.")
      .optional(),
    mountPoints: z.array(z.object({
      defaultGateway: z.boolean().describe(
        "Network should be a default gateway.",
      ).optional(),
      instance: z.string().describe("Instance to attach network to.")
        .optional(),
      ipAddress: z.string().describe("Ip address of the server.").optional(),
      logicalInterface: z.string().describe("Logical interface to detach from.")
        .optional(),
    })).describe("Input only. List of mount points to attach the network to.")
      .optional(),
    name: z.string().describe(
      "Output only. The resource name of this `Network`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/networks/{network}`",
    ).optional(),
    pod: z.string().describe(
      "Immutable. Pod name. Pod is an independent part of infrastructure. Network can only be connected to the assets (instances, nfsshares) allocated in the same pod.",
    ).optional(),
    reservations: z.array(z.object({
      endAddress: z.string().describe(
        "The last address of this reservation block, inclusive. I.e., for cases when reservations are only single addresses, end_address and start_address will be the same. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
      ).optional(),
      note: z.string().describe(
        "A note about this reservation, intended for human consumption.",
      ).optional(),
      startAddress: z.string().describe(
        "The first address of this reservation block. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
      ).optional(),
    })).describe(
      "List of IP address reservations in this network. When updating this field, an error will be generated if a reservation conflicts with an IP address already allocated to a physical server.",
    ).optional(),
    servicesCidr: z.string().describe(
      "IP range for reserved for services (e.g. NFS).",
    ).optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "PROVISIONING",
      "PROVISIONED",
      "DEPROVISIONING",
      "UPDATING",
    ]).describe("The Network state.").optional(),
    type: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
      "The type of this network.",
    ).optional(),
    vlanId: z.string().describe("The vlan id of the Network.").optional(),
    vrf: z.object({
      name: z.string().describe("The name of the VRF.").optional(),
      qosPolicy: z.object({
        bandwidthGbps: z.number().describe(
          "The bandwidth permitted by the QOS policy, in gbps.",
        ).optional(),
      }).describe("QOS policy parameters.").optional(),
      state: z.enum(["STATE_UNSPECIFIED", "PROVISIONING", "PROVISIONED"])
        .describe("The possible state of VRF.").optional(),
      vlanAttachments: z.array(z.object({
        id: z.string().describe(
          "Immutable. The identifier of the attachment within vrf.",
        ).optional(),
        interconnectAttachment: z.string().describe(
          "Optional. The name of the vlan attachment within vrf. This is of the form projects/{project_number}/regions/{region}/interconnectAttachments/{interconnect_attachment}",
        ).optional(),
        pairingKey: z.string().describe("Input only. Pairing key.").optional(),
        peerIp: z.string().describe("The peer IP of the attachment.")
          .optional(),
        peerVlanId: z.string().describe("The peer vlan ID of the attachment.")
          .optional(),
        qosPolicy: z.object({
          bandwidthGbps: z.number().describe(
            "The bandwidth permitted by the QOS policy, in gbps.",
          ).optional(),
        }).describe("QOS policy parameters.").optional(),
        routerIp: z.string().describe("The router IP of the attachment.")
          .optional(),
      })).describe("The list of VLAN attachments for the VRF.").optional(),
    }).describe("A network VRF.").optional(),
    vrfAttachment: z.string().describe(
      "Optional. The name of a pre-existing Vrf that the network should be attached to. Format is `vrfs/{vrf}`.",
    ).optional(),
  })).describe("Output only. List of networks associated with this server.")
    .optional(),
  osImage: z.string().describe(
    "The OS image currently installed on the server.",
  ).optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Instance can only be connected to the assets (networks, volumes) allocated in the same pod.",
  ).optional(),
  sshKeys: z.array(z.string()).describe(
    "Optional. List of SSH Keys used during instance provisioning.",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "PROVISIONING",
    "RUNNING",
    "DELETED",
    "UPDATING",
    "STARTING",
    "STOPPING",
    "SHUTDOWN",
  ]).describe("Output only. The state of the server.").optional(),
  updateTime: z.string().describe("Output only. Update a time stamp.")
    .optional(),
  volumes: z.array(z.object({
    attached: z.boolean().describe(
      "Output only. Is the Volume attached at at least one instance. This field is a lightweight counterpart of `instances` field. It is filled in List responses as well.",
    ).optional(),
    autoGrownSizeGib: z.string().describe(
      "The size, in GiB, that this storage volume has expanded as a result of an auto grow policy. In the absence of auto-grow, the value is 0.",
    ).optional(),
    bootVolume: z.boolean().describe(
      "Output only. Whether this volume is a boot volume. A boot volume is one which contains a boot LUN.",
    ).optional(),
    currentSizeGib: z.string().describe(
      "The current size of this storage volume, in GiB, including space reserved for snapshots. This size might be different than the requested size if the storage volume has been configured with auto grow or auto shrink.",
    ).optional(),
    emergencySizeGib: z.string().describe(
      "Additional emergency size that was requested for this Volume, in GiB. current_size_gib includes this value.",
    ).optional(),
    expireTime: z.string().describe(
      "Output only. Time after which volume will be fully deleted. It is filled only for volumes in COOLOFF state.",
    ).optional(),
    id: z.string().describe(
      "An identifier for the `Volume`, generated by the backend.",
    ).optional(),
    instances: z.array(z.string()).describe(
      "Output only. Instances this Volume is attached to. This field is set only in Get requests.",
    ).optional(),
    labels: z.record(z.string(), z.string()).describe(
      "Labels as key value pairs.",
    ).optional(),
    maxSizeGib: z.string().describe(
      "Maximum size volume can be expanded to in case of evergency, in GiB.",
    ).optional(),
    name: z.string().describe(
      "Output only. The resource name of this `Volume`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/volumes/{volume}`",
    ).optional(),
    notes: z.string().describe(
      "Input only. User-specified notes for new Volume. Used to provision Volumes that require manual intervention.",
    ).optional(),
    originallyRequestedSizeGib: z.string().describe(
      "Originally requested size, in GiB.",
    ).optional(),
    performanceTier: z.enum([
      "VOLUME_PERFORMANCE_TIER_UNSPECIFIED",
      "VOLUME_PERFORMANCE_TIER_SHARED",
      "VOLUME_PERFORMANCE_TIER_ASSIGNED",
      "VOLUME_PERFORMANCE_TIER_HT",
      "VOLUME_PERFORMANCE_TIER_QOS2_PERFORMANCE",
    ]).describe("Immutable. Performance tier of the Volume. Default is SHARED.")
      .optional(),
    pod: z.string().describe(
      "Immutable. Pod name. Pod is an independent part of infrastructure. Volume can only be connected to the instances allocated in the same pod.",
    ).optional(),
    protocol: z.enum(["PROTOCOL_UNSPECIFIED", "FIBRE_CHANNEL", "NFS"]).describe(
      "Output only. Storage protocol for the Volume.",
    ).optional(),
    remainingSpaceGib: z.string().describe(
      "The space remaining in the storage volume for new LUNs, in GiB, excluding space reserved for snapshots.",
    ).optional(),
    requestedSizeGib: z.string().describe(
      "The requested size of this storage volume, in GiB.",
    ).optional(),
    snapshotAutoDeleteBehavior: z.enum([
      "SNAPSHOT_AUTO_DELETE_BEHAVIOR_UNSPECIFIED",
      "DISABLED",
      "OLDEST_FIRST",
      "NEWEST_FIRST",
    ]).describe("The behavior to use when snapshot reserved space is full.")
      .optional(),
    snapshotEnabled: z.boolean().describe("Whether snapshots are enabled.")
      .optional(),
    snapshotReservationDetail: z.object({
      reservedSpaceGib: z.string().describe(
        "The space on this storage volume reserved for snapshots, shown in GiB.",
      ).optional(),
      reservedSpacePercent: z.number().int().describe(
        "Percent of the total Volume size reserved for snapshot copies. Enabling snapshots requires reserving 20% or more of the storage volume space for snapshots. Maximum reserved space for snapshots is 40%. Setting this field will effectively set snapshot_enabled to true.",
      ).optional(),
      reservedSpaceRemainingGib: z.string().describe(
        "The amount, in GiB, of available space in this storage volume's reserved snapshot space.",
      ).optional(),
      reservedSpaceUsedPercent: z.number().int().describe(
        "The percent of snapshot space on this storage volume actually being used by the snapshot copies. This value might be higher than 100% if the snapshot copies have overflowed into the data portion of the storage volume.",
      ).optional(),
    }).describe(
      "Details about snapshot space reservation and usage on the storage volume.",
    ).optional(),
    state: z.enum([
      "STATE_UNSPECIFIED",
      "CREATING",
      "READY",
      "DELETING",
      "UPDATING",
      "COOL_OFF",
    ]).describe("The state of this storage volume.").optional(),
    storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
      "The storage type for this volume.",
    ).optional(),
    workloadProfile: z.enum(["WORKLOAD_PROFILE_UNSPECIFIED", "GENERIC", "HANA"])
      .describe("The workload profile for the volume.").optional(),
  })).describe(
    "Input only. List of Volumes to attach to this Instance on creation. This field won't be populated in Get/List responses.",
  ).optional(),
  workloadProfile: z.enum([
    "WORKLOAD_PROFILE_UNSPECIFIED",
    "WORKLOAD_PROFILE_GENERIC",
    "WORKLOAD_PROFILE_HANA",
  ]).describe("The workload profile for the instance.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

export const model = {
  type: "@swamp/gcp/baremetalsolution/instances",
  version: "2026.04.02.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.02.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "A server.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    get: {
      description: "Get a instances",
      arguments: z.object({
        identifier: z.string().describe("The name of the instances"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          args.identifier,
        );
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName = (result.name ?? g.name)?.toString() ??
          args.identifier;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update instances attributes",
      arguments: z.object({
        waitForReady: z.boolean().describe(
          "Wait for the resource to reach a ready state after update (default: true)",
        ).optional(),
      }),
      execute: async (args: { waitForReady?: boolean }, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const params: Record<string, string> = { project: projectId };
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          existing["name"]?.toString() ?? g["name"]?.toString() ?? "",
        );
        const body: Record<string, unknown> = {};
        if (g["createTime"] !== undefined) body["createTime"] = g["createTime"];
        if (g["firmwareVersion"] !== undefined) {
          body["firmwareVersion"] = g["firmwareVersion"];
        }
        if (g["hyperthreadingEnabled"] !== undefined) {
          body["hyperthreadingEnabled"] = g["hyperthreadingEnabled"];
        }
        if (g["id"] !== undefined) body["id"] = g["id"];
        if (g["interactiveSerialConsoleEnabled"] !== undefined) {
          body["interactiveSerialConsoleEnabled"] =
            g["interactiveSerialConsoleEnabled"];
        }
        if (g["kmsKeyVersion"] !== undefined) {
          body["kmsKeyVersion"] = g["kmsKeyVersion"];
        }
        if (g["labels"] !== undefined) body["labels"] = g["labels"];
        if (g["logicalInterfaces"] !== undefined) {
          body["logicalInterfaces"] = g["logicalInterfaces"];
        }
        if (g["loginInfo"] !== undefined) body["loginInfo"] = g["loginInfo"];
        if (g["luns"] !== undefined) body["luns"] = g["luns"];
        if (g["machineType"] !== undefined) {
          body["machineType"] = g["machineType"];
        }
        if (g["networkTemplate"] !== undefined) {
          body["networkTemplate"] = g["networkTemplate"];
        }
        if (g["networks"] !== undefined) body["networks"] = g["networks"];
        if (g["osImage"] !== undefined) body["osImage"] = g["osImage"];
        if (g["pod"] !== undefined) body["pod"] = g["pod"];
        if (g["sshKeys"] !== undefined) body["sshKeys"] = g["sshKeys"];
        if (g["state"] !== undefined) body["state"] = g["state"];
        if (g["updateTime"] !== undefined) body["updateTime"] = g["updateTime"];
        if (g["volumes"] !== undefined) body["volumes"] = g["volumes"];
        if (g["workloadProfile"] !== undefined) {
          body["workloadProfile"] = g["workloadProfile"];
        }
        for (const key of Object.keys(existing)) {
          if (
            key === "fingerprint" || key === "labelFingerprint" ||
            key === "etag" || key.endsWith("Fingerprint")
          ) {
            body[key] = existing[key];
          }
        }
        const result = await updateResource(
          BASE_URL,
          PATCH_CONFIG,
          params,
          body,
          GET_CONFIG,
          (args.waitForReady ?? true)
            ? {
              "statusField": "state",
              "readyValues": ["RUNNING"],
              "failedValues": [],
            }
            : undefined,
        ) as StateData;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync instances state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const shortName = existing.name?.toString() ?? g["name"]?.toString();
          if (!shortName) throw new Error("No identifier found");
          params["name"] = buildResourceName(
            String(g["parent"] ?? ""),
            shortName,
          );
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
    detach_lun: {
      description: "detach lun",
      arguments: z.object({
        lun: z.any().optional(),
        skipReboot: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          g.name?.toString() ?? "current",
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        params["instance"] = existing["name"]?.toString() ??
          g["name"]?.toString() ?? "";
        const body: Record<string, unknown> = {};
        if (args["lun"] !== undefined) body["lun"] = args["lun"];
        if (args["skipReboot"] !== undefined) {
          body["skipReboot"] = args["skipReboot"];
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.detachLun",
            "path": "v2/{+instance}:detachLun",
            "httpMethod": "POST",
            "parameterOrder": ["instance"],
            "parameters": {
              "instance": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
    disable_hyperthreading: {
      description: "disable hyperthreading",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "baremetalsolution.projects.locations.instances.disableHyperthreading",
            "path": "v2/{+name}:disableHyperthreading",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    disable_interactive_serial_console: {
      description: "disable interactive serial console",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "baremetalsolution.projects.locations.instances.disableInteractiveSerialConsole",
            "path": "v2/{+name}:disableInteractiveSerialConsole",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    enable_hyperthreading: {
      description: "enable hyperthreading",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "baremetalsolution.projects.locations.instances.enableHyperthreading",
            "path": "v2/{+name}:enableHyperthreading",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    enable_interactive_serial_console: {
      description: "enable interactive serial console",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "baremetalsolution.projects.locations.instances.enableInteractiveSerialConsole",
            "path": "v2/{+name}:enableInteractiveSerialConsole",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    load_auth_info: {
      description: "load auth info",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.loadAuthInfo",
            "path": "v2/{+name}:loadAuthInfo",
            "httpMethod": "GET",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    reimage: {
      description: "reimage",
      arguments: z.object({
        kmsKeyVersion: z.any().optional(),
        osImage: z.any().optional(),
        sshKeys: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const body: Record<string, unknown> = {};
        if (args["kmsKeyVersion"] !== undefined) {
          body["kmsKeyVersion"] = args["kmsKeyVersion"];
        }
        if (args["osImage"] !== undefined) body["osImage"] = args["osImage"];
        if (args["sshKeys"] !== undefined) body["sshKeys"] = args["sshKeys"];
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.reimage",
            "path": "v2/{+name}:reimage",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          body,
        );
        return { result };
      },
    },
    rename: {
      description: "rename",
      arguments: z.object({
        newInstanceId: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const body: Record<string, unknown> = {};
        if (args["newInstanceId"] !== undefined) {
          body["newInstanceId"] = args["newInstanceId"];
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.rename",
            "path": "v2/{+name}:rename",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          body,
        );
        return { result };
      },
    },
    reset: {
      description: "reset",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.reset",
            "path": "v2/{+name}:reset",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    start: {
      description: "start",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.start",
            "path": "v2/{+name}:start",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    stop: {
      description: "stop",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.instances.stop",
            "path": "v2/{+name}:stop",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
  },
};
