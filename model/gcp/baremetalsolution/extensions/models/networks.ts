// Auto-generated extension model for @swamp/gcp/baremetalsolution/networks
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/gcp.ts";

/** Construct the fully-qualified resource name from parent and short name. */
function buildResourceName(parent: string, shortName: string): string {
  return `${parent}/networks/${shortName}`;
}

const BASE_URL = "https://baremetalsolution.googleapis.com/";

const GET_CONFIG = {
  "id": "baremetalsolution.projects.locations.networks.get",
  "path": "v2/{+name}",
  "httpMethod": "GET",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const PATCH_CONFIG = {
  "id": "baremetalsolution.projects.locations.networks.patch",
  "path": "v2/{+name}",
  "httpMethod": "PATCH",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
    "updateMask": {
      "location": "query",
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  cidr: z.string().describe("The cidr of the Network.").optional(),
  gatewayIp: z.string().describe("Output only. Gateway ip address.").optional(),
  id: z.string().describe(
    "An identifier for the `Network`, generated by the backend.",
  ).optional(),
  ipAddress: z.string().describe("IP address configured.").optional(),
  jumboFramesEnabled: z.boolean().describe(
    "Whether network uses standard frames or jumbo ones.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  macAddress: z.array(z.string()).describe("List of physical interfaces.")
    .optional(),
  mountPoints: z.array(z.object({
    defaultGateway: z.boolean().describe("Network should be a default gateway.")
      .optional(),
    instance: z.string().describe("Instance to attach network to.").optional(),
    ipAddress: z.string().describe("Ip address of the server.").optional(),
    logicalInterface: z.string().describe("Logical interface to detach from.")
      .optional(),
  })).describe("Input only. List of mount points to attach the network to.")
    .optional(),
  name: z.string().describe(
    "Output only. The resource name of this `Network`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/networks/{network}`",
  ).optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Network can only be connected to the assets (instances, nfsshares) allocated in the same pod.",
  ).optional(),
  reservations: z.array(z.object({
    endAddress: z.string().describe(
      "The last address of this reservation block, inclusive. I.e., for cases when reservations are only single addresses, end_address and start_address will be the same. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
    ).optional(),
    note: z.string().describe(
      "A note about this reservation, intended for human consumption.",
    ).optional(),
    startAddress: z.string().describe(
      "The first address of this reservation block. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
    ).optional(),
  })).describe(
    "List of IP address reservations in this network. When updating this field, an error will be generated if a reservation conflicts with an IP address already allocated to a physical server.",
  ).optional(),
  servicesCidr: z.string().describe(
    "IP range for reserved for services (e.g. NFS).",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "PROVISIONING",
    "PROVISIONED",
    "DEPROVISIONING",
    "UPDATING",
  ]).describe("The Network state.").optional(),
  type: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
    "The type of this network.",
  ).optional(),
  vlanId: z.string().describe("The vlan id of the Network.").optional(),
  vrf: z.object({
    name: z.string().describe("The name of the VRF.").optional(),
    qosPolicy: z.object({
      bandwidthGbps: z.number().describe(
        "The bandwidth permitted by the QOS policy, in gbps.",
      ).optional(),
    }).describe("QOS policy parameters.").optional(),
    state: z.enum(["STATE_UNSPECIFIED", "PROVISIONING", "PROVISIONED"])
      .describe("The possible state of VRF.").optional(),
    vlanAttachments: z.array(z.object({
      id: z.string().describe(
        "Immutable. The identifier of the attachment within vrf.",
      ).optional(),
      interconnectAttachment: z.string().describe(
        "Optional. The name of the vlan attachment within vrf. This is of the form projects/{project_number}/regions/{region}/interconnectAttachments/{interconnect_attachment}",
      ).optional(),
      pairingKey: z.string().describe("Input only. Pairing key.").optional(),
      peerIp: z.string().describe("The peer IP of the attachment.").optional(),
      peerVlanId: z.string().describe("The peer vlan ID of the attachment.")
        .optional(),
      qosPolicy: z.object({
        bandwidthGbps: z.number().describe(
          "The bandwidth permitted by the QOS policy, in gbps.",
        ).optional(),
      }).describe("QOS policy parameters.").optional(),
      routerIp: z.string().describe("The router IP of the attachment.")
        .optional(),
    })).describe("The list of VLAN attachments for the VRF.").optional(),
  }).describe("A network VRF.").optional(),
  vrfAttachment: z.string().describe(
    "Optional. The name of a pre-existing Vrf that the network should be attached to. Format is `vrfs/{vrf}`.",
  ).optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

const StateSchema = z.object({
  cidr: z.string().optional(),
  gatewayIp: z.string().optional(),
  id: z.string().optional(),
  ipAddress: z.string().optional(),
  jumboFramesEnabled: z.boolean().optional(),
  labels: z.record(z.string(), z.unknown()).optional(),
  macAddress: z.array(z.string()).optional(),
  mountPoints: z.array(z.object({
    defaultGateway: z.boolean(),
    instance: z.string(),
    ipAddress: z.string(),
    logicalInterface: z.string(),
  })).optional(),
  name: z.string(),
  pod: z.string().optional(),
  reservations: z.array(z.object({
    endAddress: z.string(),
    note: z.string(),
    startAddress: z.string(),
  })).optional(),
  servicesCidr: z.string().optional(),
  state: z.string().optional(),
  type: z.string().optional(),
  vlanId: z.string().optional(),
  vrf: z.object({
    name: z.string(),
    qosPolicy: z.object({
      bandwidthGbps: z.number(),
    }),
    state: z.string(),
    vlanAttachments: z.array(z.object({
      id: z.string(),
      interconnectAttachment: z.string(),
      pairingKey: z.string(),
      peerIp: z.string(),
      peerVlanId: z.string(),
      qosPolicy: z.object({
        bandwidthGbps: z.number(),
      }),
      routerIp: z.string(),
    })),
  }).optional(),
  vrfAttachment: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  cidr: z.string().describe("The cidr of the Network.").optional(),
  gatewayIp: z.string().describe("Output only. Gateway ip address.").optional(),
  id: z.string().describe(
    "An identifier for the `Network`, generated by the backend.",
  ).optional(),
  ipAddress: z.string().describe("IP address configured.").optional(),
  jumboFramesEnabled: z.boolean().describe(
    "Whether network uses standard frames or jumbo ones.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  macAddress: z.array(z.string()).describe("List of physical interfaces.")
    .optional(),
  mountPoints: z.array(z.object({
    defaultGateway: z.boolean().describe("Network should be a default gateway.")
      .optional(),
    instance: z.string().describe("Instance to attach network to.").optional(),
    ipAddress: z.string().describe("Ip address of the server.").optional(),
    logicalInterface: z.string().describe("Logical interface to detach from.")
      .optional(),
  })).describe("Input only. List of mount points to attach the network to.")
    .optional(),
  name: z.string().describe(
    "Output only. The resource name of this `Network`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/networks/{network}`",
  ).optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Network can only be connected to the assets (instances, nfsshares) allocated in the same pod.",
  ).optional(),
  reservations: z.array(z.object({
    endAddress: z.string().describe(
      "The last address of this reservation block, inclusive. I.e., for cases when reservations are only single addresses, end_address and start_address will be the same. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
    ).optional(),
    note: z.string().describe(
      "A note about this reservation, intended for human consumption.",
    ).optional(),
    startAddress: z.string().describe(
      "The first address of this reservation block. Must be specified as a single IPv4 address, e.g. 10.1.2.2.",
    ).optional(),
  })).describe(
    "List of IP address reservations in this network. When updating this field, an error will be generated if a reservation conflicts with an IP address already allocated to a physical server.",
  ).optional(),
  servicesCidr: z.string().describe(
    "IP range for reserved for services (e.g. NFS).",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "PROVISIONING",
    "PROVISIONED",
    "DEPROVISIONING",
    "UPDATING",
  ]).describe("The Network state.").optional(),
  type: z.enum(["TYPE_UNSPECIFIED", "CLIENT", "PRIVATE"]).describe(
    "The type of this network.",
  ).optional(),
  vlanId: z.string().describe("The vlan id of the Network.").optional(),
  vrf: z.object({
    name: z.string().describe("The name of the VRF.").optional(),
    qosPolicy: z.object({
      bandwidthGbps: z.number().describe(
        "The bandwidth permitted by the QOS policy, in gbps.",
      ).optional(),
    }).describe("QOS policy parameters.").optional(),
    state: z.enum(["STATE_UNSPECIFIED", "PROVISIONING", "PROVISIONED"])
      .describe("The possible state of VRF.").optional(),
    vlanAttachments: z.array(z.object({
      id: z.string().describe(
        "Immutable. The identifier of the attachment within vrf.",
      ).optional(),
      interconnectAttachment: z.string().describe(
        "Optional. The name of the vlan attachment within vrf. This is of the form projects/{project_number}/regions/{region}/interconnectAttachments/{interconnect_attachment}",
      ).optional(),
      pairingKey: z.string().describe("Input only. Pairing key.").optional(),
      peerIp: z.string().describe("The peer IP of the attachment.").optional(),
      peerVlanId: z.string().describe("The peer vlan ID of the attachment.")
        .optional(),
      qosPolicy: z.object({
        bandwidthGbps: z.number().describe(
          "The bandwidth permitted by the QOS policy, in gbps.",
        ).optional(),
      }).describe("QOS policy parameters.").optional(),
      routerIp: z.string().describe("The router IP of the attachment.")
        .optional(),
    })).describe("The list of VLAN attachments for the VRF.").optional(),
  }).describe("A network VRF.").optional(),
  vrfAttachment: z.string().describe(
    "Optional. The name of a pre-existing Vrf that the network should be attached to. Format is `vrfs/{vrf}`.",
  ).optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

export const model = {
  type: "@swamp/gcp/baremetalsolution/networks",
  version: "2026.04.01.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.01.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "A Network.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    get: {
      description: "Get a networks",
      arguments: z.object({
        identifier: z.string().describe("The name of the networks"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          args.identifier,
        );
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName = (result.name ?? g.name)?.toString() ??
          args.identifier;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update networks attributes",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const params: Record<string, string> = { project: projectId };
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          existing["name"]?.toString() ?? g["name"]?.toString() ?? "",
        );
        const body: Record<string, unknown> = {};
        if (g["cidr"] !== undefined) body["cidr"] = g["cidr"];
        if (g["gatewayIp"] !== undefined) body["gatewayIp"] = g["gatewayIp"];
        if (g["id"] !== undefined) body["id"] = g["id"];
        if (g["ipAddress"] !== undefined) body["ipAddress"] = g["ipAddress"];
        if (g["jumboFramesEnabled"] !== undefined) {
          body["jumboFramesEnabled"] = g["jumboFramesEnabled"];
        }
        if (g["labels"] !== undefined) body["labels"] = g["labels"];
        if (g["macAddress"] !== undefined) body["macAddress"] = g["macAddress"];
        if (g["mountPoints"] !== undefined) {
          body["mountPoints"] = g["mountPoints"];
        }
        if (g["pod"] !== undefined) body["pod"] = g["pod"];
        if (g["reservations"] !== undefined) {
          body["reservations"] = g["reservations"];
        }
        if (g["servicesCidr"] !== undefined) {
          body["servicesCidr"] = g["servicesCidr"];
        }
        if (g["state"] !== undefined) body["state"] = g["state"];
        if (g["type"] !== undefined) body["type"] = g["type"];
        if (g["vlanId"] !== undefined) body["vlanId"] = g["vlanId"];
        if (g["vrf"] !== undefined) body["vrf"] = g["vrf"];
        if (g["vrfAttachment"] !== undefined) {
          body["vrfAttachment"] = g["vrfAttachment"];
        }
        for (const key of Object.keys(existing)) {
          if (
            key === "fingerprint" || key === "labelFingerprint" ||
            key === "etag" || key.endsWith("Fingerprint")
          ) {
            body[key] = existing[key];
          }
        }
        const result = await updateResource(
          BASE_URL,
          PATCH_CONFIG,
          params,
          body,
          GET_CONFIG,
        ) as StateData;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync networks state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const shortName = existing.name?.toString() ?? g["name"]?.toString();
          if (!shortName) throw new Error("No identifier found");
          params["name"] = buildResourceName(
            String(g["parent"] ?? ""),
            shortName,
          );
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
    list_network_usage: {
      description: "list network usage",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["location"] !== undefined) {
          params["location"] = String(g["location"]);
        }
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "baremetalsolution.projects.locations.networks.listNetworkUsage",
            "path": "v2/{+location}/networks:listNetworkUsage",
            "httpMethod": "GET",
            "parameterOrder": ["location"],
            "parameters": {
              "location": { "location": "path", "required": true },
            },
          },
          params,
          {},
        );
        return { result };
      },
    },
    rename: {
      description: "rename",
      arguments: z.object({
        newNetworkId: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const body: Record<string, unknown> = {};
        if (args["newNetworkId"] !== undefined) {
          body["newNetworkId"] = args["newNetworkId"];
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.networks.rename",
            "path": "v2/{+name}:rename",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          body,
        );
        return { result };
      },
    },
  },
};
