// Auto-generated extension model for @swamp/gcp/baremetalsolution/volumes
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/gcp.ts";

/** Construct the fully-qualified resource name from parent and short name. */
function buildResourceName(parent: string, shortName: string): string {
  return `${parent}/volumes/${shortName}`;
}

const BASE_URL = "https://baremetalsolution.googleapis.com/";

const GET_CONFIG = {
  "id": "baremetalsolution.projects.locations.volumes.get",
  "path": "v2/{+name}",
  "httpMethod": "GET",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const PATCH_CONFIG = {
  "id": "baremetalsolution.projects.locations.volumes.patch",
  "path": "v2/{+name}",
  "httpMethod": "PATCH",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
    "updateMask": {
      "location": "query",
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  attached: z.boolean().describe(
    "Output only. Is the Volume attached at at least one instance. This field is a lightweight counterpart of `instances` field. It is filled in List responses as well.",
  ).optional(),
  autoGrownSizeGib: z.string().describe(
    "The size, in GiB, that this storage volume has expanded as a result of an auto grow policy. In the absence of auto-grow, the value is 0.",
  ).optional(),
  bootVolume: z.boolean().describe(
    "Output only. Whether this volume is a boot volume. A boot volume is one which contains a boot LUN.",
  ).optional(),
  currentSizeGib: z.string().describe(
    "The current size of this storage volume, in GiB, including space reserved for snapshots. This size might be different than the requested size if the storage volume has been configured with auto grow or auto shrink.",
  ).optional(),
  emergencySizeGib: z.string().describe(
    "Additional emergency size that was requested for this Volume, in GiB. current_size_gib includes this value.",
  ).optional(),
  expireTime: z.string().describe(
    "Output only. Time after which volume will be fully deleted. It is filled only for volumes in COOLOFF state.",
  ).optional(),
  id: z.string().describe(
    "An identifier for the `Volume`, generated by the backend.",
  ).optional(),
  instances: z.array(z.string()).describe(
    "Output only. Instances this Volume is attached to. This field is set only in Get requests.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  maxSizeGib: z.string().describe(
    "Maximum size volume can be expanded to in case of evergency, in GiB.",
  ).optional(),
  name: z.string().describe(
    "Output only. The resource name of this `Volume`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/volumes/{volume}`",
  ).optional(),
  notes: z.string().describe(
    "Input only. User-specified notes for new Volume. Used to provision Volumes that require manual intervention.",
  ).optional(),
  originallyRequestedSizeGib: z.string().describe(
    "Originally requested size, in GiB.",
  ).optional(),
  performanceTier: z.enum([
    "VOLUME_PERFORMANCE_TIER_UNSPECIFIED",
    "VOLUME_PERFORMANCE_TIER_SHARED",
    "VOLUME_PERFORMANCE_TIER_ASSIGNED",
    "VOLUME_PERFORMANCE_TIER_HT",
    "VOLUME_PERFORMANCE_TIER_QOS2_PERFORMANCE",
  ]).describe("Immutable. Performance tier of the Volume. Default is SHARED.")
    .optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Volume can only be connected to the instances allocated in the same pod.",
  ).optional(),
  protocol: z.enum(["PROTOCOL_UNSPECIFIED", "FIBRE_CHANNEL", "NFS"]).describe(
    "Output only. Storage protocol for the Volume.",
  ).optional(),
  remainingSpaceGib: z.string().describe(
    "The space remaining in the storage volume for new LUNs, in GiB, excluding space reserved for snapshots.",
  ).optional(),
  requestedSizeGib: z.string().describe(
    "The requested size of this storage volume, in GiB.",
  ).optional(),
  snapshotAutoDeleteBehavior: z.enum([
    "SNAPSHOT_AUTO_DELETE_BEHAVIOR_UNSPECIFIED",
    "DISABLED",
    "OLDEST_FIRST",
    "NEWEST_FIRST",
  ]).describe("The behavior to use when snapshot reserved space is full.")
    .optional(),
  snapshotEnabled: z.boolean().describe("Whether snapshots are enabled.")
    .optional(),
  snapshotReservationDetail: z.object({
    reservedSpaceGib: z.string().describe(
      "The space on this storage volume reserved for snapshots, shown in GiB.",
    ).optional(),
    reservedSpacePercent: z.number().int().describe(
      "Percent of the total Volume size reserved for snapshot copies. Enabling snapshots requires reserving 20% or more of the storage volume space for snapshots. Maximum reserved space for snapshots is 40%. Setting this field will effectively set snapshot_enabled to true.",
    ).optional(),
    reservedSpaceRemainingGib: z.string().describe(
      "The amount, in GiB, of available space in this storage volume's reserved snapshot space.",
    ).optional(),
    reservedSpaceUsedPercent: z.number().int().describe(
      "The percent of snapshot space on this storage volume actually being used by the snapshot copies. This value might be higher than 100% if the snapshot copies have overflowed into the data portion of the storage volume.",
    ).optional(),
  }).describe(
    "Details about snapshot space reservation and usage on the storage volume.",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "CREATING",
    "READY",
    "DELETING",
    "UPDATING",
    "COOL_OFF",
  ]).describe("The state of this storage volume.").optional(),
  storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
    "The storage type for this volume.",
  ).optional(),
  workloadProfile: z.enum(["WORKLOAD_PROFILE_UNSPECIFIED", "GENERIC", "HANA"])
    .describe("The workload profile for the volume.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

const StateSchema = z.object({
  attached: z.boolean().optional(),
  autoGrownSizeGib: z.string().optional(),
  bootVolume: z.boolean().optional(),
  currentSizeGib: z.string().optional(),
  emergencySizeGib: z.string().optional(),
  expireTime: z.string().optional(),
  id: z.string().optional(),
  instances: z.array(z.string()).optional(),
  labels: z.record(z.string(), z.unknown()).optional(),
  maxSizeGib: z.string().optional(),
  name: z.string(),
  notes: z.string().optional(),
  originallyRequestedSizeGib: z.string().optional(),
  performanceTier: z.string().optional(),
  pod: z.string().optional(),
  protocol: z.string().optional(),
  remainingSpaceGib: z.string().optional(),
  requestedSizeGib: z.string().optional(),
  snapshotAutoDeleteBehavior: z.string().optional(),
  snapshotEnabled: z.boolean().optional(),
  snapshotReservationDetail: z.object({
    reservedSpaceGib: z.string(),
    reservedSpacePercent: z.number(),
    reservedSpaceRemainingGib: z.string(),
    reservedSpaceUsedPercent: z.number(),
  }).optional(),
  state: z.string().optional(),
  storageType: z.string().optional(),
  workloadProfile: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  attached: z.boolean().describe(
    "Output only. Is the Volume attached at at least one instance. This field is a lightweight counterpart of `instances` field. It is filled in List responses as well.",
  ).optional(),
  autoGrownSizeGib: z.string().describe(
    "The size, in GiB, that this storage volume has expanded as a result of an auto grow policy. In the absence of auto-grow, the value is 0.",
  ).optional(),
  bootVolume: z.boolean().describe(
    "Output only. Whether this volume is a boot volume. A boot volume is one which contains a boot LUN.",
  ).optional(),
  currentSizeGib: z.string().describe(
    "The current size of this storage volume, in GiB, including space reserved for snapshots. This size might be different than the requested size if the storage volume has been configured with auto grow or auto shrink.",
  ).optional(),
  emergencySizeGib: z.string().describe(
    "Additional emergency size that was requested for this Volume, in GiB. current_size_gib includes this value.",
  ).optional(),
  expireTime: z.string().describe(
    "Output only. Time after which volume will be fully deleted. It is filled only for volumes in COOLOFF state.",
  ).optional(),
  id: z.string().describe(
    "An identifier for the `Volume`, generated by the backend.",
  ).optional(),
  instances: z.array(z.string()).describe(
    "Output only. Instances this Volume is attached to. This field is set only in Get requests.",
  ).optional(),
  labels: z.record(z.string(), z.string()).describe(
    "Labels as key value pairs.",
  ).optional(),
  maxSizeGib: z.string().describe(
    "Maximum size volume can be expanded to in case of evergency, in GiB.",
  ).optional(),
  name: z.string().describe(
    "Output only. The resource name of this `Volume`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/volumes/{volume}`",
  ).optional(),
  notes: z.string().describe(
    "Input only. User-specified notes for new Volume. Used to provision Volumes that require manual intervention.",
  ).optional(),
  originallyRequestedSizeGib: z.string().describe(
    "Originally requested size, in GiB.",
  ).optional(),
  performanceTier: z.enum([
    "VOLUME_PERFORMANCE_TIER_UNSPECIFIED",
    "VOLUME_PERFORMANCE_TIER_SHARED",
    "VOLUME_PERFORMANCE_TIER_ASSIGNED",
    "VOLUME_PERFORMANCE_TIER_HT",
    "VOLUME_PERFORMANCE_TIER_QOS2_PERFORMANCE",
  ]).describe("Immutable. Performance tier of the Volume. Default is SHARED.")
    .optional(),
  pod: z.string().describe(
    "Immutable. Pod name. Pod is an independent part of infrastructure. Volume can only be connected to the instances allocated in the same pod.",
  ).optional(),
  protocol: z.enum(["PROTOCOL_UNSPECIFIED", "FIBRE_CHANNEL", "NFS"]).describe(
    "Output only. Storage protocol for the Volume.",
  ).optional(),
  remainingSpaceGib: z.string().describe(
    "The space remaining in the storage volume for new LUNs, in GiB, excluding space reserved for snapshots.",
  ).optional(),
  requestedSizeGib: z.string().describe(
    "The requested size of this storage volume, in GiB.",
  ).optional(),
  snapshotAutoDeleteBehavior: z.enum([
    "SNAPSHOT_AUTO_DELETE_BEHAVIOR_UNSPECIFIED",
    "DISABLED",
    "OLDEST_FIRST",
    "NEWEST_FIRST",
  ]).describe("The behavior to use when snapshot reserved space is full.")
    .optional(),
  snapshotEnabled: z.boolean().describe("Whether snapshots are enabled.")
    .optional(),
  snapshotReservationDetail: z.object({
    reservedSpaceGib: z.string().describe(
      "The space on this storage volume reserved for snapshots, shown in GiB.",
    ).optional(),
    reservedSpacePercent: z.number().int().describe(
      "Percent of the total Volume size reserved for snapshot copies. Enabling snapshots requires reserving 20% or more of the storage volume space for snapshots. Maximum reserved space for snapshots is 40%. Setting this field will effectively set snapshot_enabled to true.",
    ).optional(),
    reservedSpaceRemainingGib: z.string().describe(
      "The amount, in GiB, of available space in this storage volume's reserved snapshot space.",
    ).optional(),
    reservedSpaceUsedPercent: z.number().int().describe(
      "The percent of snapshot space on this storage volume actually being used by the snapshot copies. This value might be higher than 100% if the snapshot copies have overflowed into the data portion of the storage volume.",
    ).optional(),
  }).describe(
    "Details about snapshot space reservation and usage on the storage volume.",
  ).optional(),
  state: z.enum([
    "STATE_UNSPECIFIED",
    "CREATING",
    "READY",
    "DELETING",
    "UPDATING",
    "COOL_OFF",
  ]).describe("The state of this storage volume.").optional(),
  storageType: z.enum(["STORAGE_TYPE_UNSPECIFIED", "SSD", "HDD"]).describe(
    "The storage type for this volume.",
  ).optional(),
  workloadProfile: z.enum(["WORKLOAD_PROFILE_UNSPECIFIED", "GENERIC", "HANA"])
    .describe("The workload profile for the volume.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

export const model = {
  type: "@swamp/gcp/baremetalsolution/volumes",
  version: "2026.03.27.1",
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "A storage volume.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    get: {
      description: "Get a volumes",
      arguments: z.object({
        identifier: z.string().describe("The name of the volumes"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          args.identifier,
        );
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName = (result.name ?? g.name)?.toString() ??
          args.identifier;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update volumes attributes",
      arguments: z.object({
        waitForReady: z.boolean().describe(
          "Wait for the resource to reach a ready state after update (default: true)",
        ).optional(),
      }),
      execute: async (args: { waitForReady?: boolean }, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const params: Record<string, string> = { project: projectId };
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          existing["name"]?.toString() ?? g["name"]?.toString() ?? "",
        );
        const body: Record<string, unknown> = {};
        if (g["attached"] !== undefined) body["attached"] = g["attached"];
        if (g["autoGrownSizeGib"] !== undefined) {
          body["autoGrownSizeGib"] = g["autoGrownSizeGib"];
        }
        if (g["bootVolume"] !== undefined) body["bootVolume"] = g["bootVolume"];
        if (g["currentSizeGib"] !== undefined) {
          body["currentSizeGib"] = g["currentSizeGib"];
        }
        if (g["emergencySizeGib"] !== undefined) {
          body["emergencySizeGib"] = g["emergencySizeGib"];
        }
        if (g["expireTime"] !== undefined) body["expireTime"] = g["expireTime"];
        if (g["id"] !== undefined) body["id"] = g["id"];
        if (g["instances"] !== undefined) body["instances"] = g["instances"];
        if (g["labels"] !== undefined) body["labels"] = g["labels"];
        if (g["maxSizeGib"] !== undefined) body["maxSizeGib"] = g["maxSizeGib"];
        if (g["notes"] !== undefined) body["notes"] = g["notes"];
        if (g["originallyRequestedSizeGib"] !== undefined) {
          body["originallyRequestedSizeGib"] = g["originallyRequestedSizeGib"];
        }
        if (g["performanceTier"] !== undefined) {
          body["performanceTier"] = g["performanceTier"];
        }
        if (g["pod"] !== undefined) body["pod"] = g["pod"];
        if (g["protocol"] !== undefined) body["protocol"] = g["protocol"];
        if (g["remainingSpaceGib"] !== undefined) {
          body["remainingSpaceGib"] = g["remainingSpaceGib"];
        }
        if (g["requestedSizeGib"] !== undefined) {
          body["requestedSizeGib"] = g["requestedSizeGib"];
        }
        if (g["snapshotAutoDeleteBehavior"] !== undefined) {
          body["snapshotAutoDeleteBehavior"] = g["snapshotAutoDeleteBehavior"];
        }
        if (g["snapshotEnabled"] !== undefined) {
          body["snapshotEnabled"] = g["snapshotEnabled"];
        }
        if (g["snapshotReservationDetail"] !== undefined) {
          body["snapshotReservationDetail"] = g["snapshotReservationDetail"];
        }
        if (g["state"] !== undefined) body["state"] = g["state"];
        if (g["storageType"] !== undefined) {
          body["storageType"] = g["storageType"];
        }
        if (g["workloadProfile"] !== undefined) {
          body["workloadProfile"] = g["workloadProfile"];
        }
        for (const key of Object.keys(existing)) {
          if (
            key === "fingerprint" || key === "labelFingerprint" ||
            key === "etag" || key.endsWith("Fingerprint")
          ) {
            body[key] = existing[key];
          }
        }
        const result = await updateResource(
          BASE_URL,
          PATCH_CONFIG,
          params,
          body,
          GET_CONFIG,
          (args.waitForReady ?? true)
            ? {
              "statusField": "state",
              "readyValues": ["READY"],
              "failedValues": [],
            }
            : undefined,
        ) as StateData;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync volumes state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const shortName = existing.name?.toString() ?? g["name"]?.toString();
          if (!shortName) throw new Error("No identifier found");
          params["name"] = buildResourceName(
            String(g["parent"] ?? ""),
            shortName,
          );
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
    evict: {
      description: "evict",
      arguments: z.object({}),
      execute: async (_args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.volumes.evict",
            "path": "v2/{+name}:evict",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          {},
        );
        return { result };
      },
    },
    rename: {
      description: "rename",
      arguments: z.object({
        newVolumeId: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const body: Record<string, unknown> = {};
        if (args["newVolumeId"] !== undefined) {
          body["newVolumeId"] = args["newVolumeId"];
        }
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.volumes.rename",
            "path": "v2/{+name}:rename",
            "httpMethod": "POST",
            "parameterOrder": ["name"],
            "parameters": { "name": { "location": "path", "required": true } },
          },
          params,
          body,
        );
        return { result };
      },
    },
    resize: {
      description: "resize",
      arguments: z.object({
        sizeGib: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          g.name?.toString() ?? "current",
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        params["volume"] = existing["name"]?.toString() ??
          g["name"]?.toString() ?? "";
        const body: Record<string, unknown> = {};
        if (args["sizeGib"] !== undefined) body["sizeGib"] = args["sizeGib"];
        const result = await createResource(
          BASE_URL,
          {
            "id": "baremetalsolution.projects.locations.volumes.resize",
            "path": "v2/{+volume}:resize",
            "httpMethod": "POST",
            "parameterOrder": ["volume"],
            "parameters": {
              "volume": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
  },
};
