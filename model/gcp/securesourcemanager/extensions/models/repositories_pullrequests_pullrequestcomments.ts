// Auto-generated extension model for @swamp/gcp/securesourcemanager/repositories-pullrequests-pullrequestcomments
// Do not edit manually. Re-generate with: deno task generate:gcp

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  deleteResource,
  getProjectId,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/gcp.ts";

/** Construct the fully-qualified resource name from parent and short name. */
function buildResourceName(parent: string, shortName: string): string {
  return `${parent}/pullRequestComments/${shortName}`;
}

const BASE_URL = "https://securesourcemanager.googleapis.com/";

const GET_CONFIG = {
  "id":
    "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.get",
  "path": "v1/{+name}",
  "httpMethod": "GET",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const INSERT_CONFIG = {
  "id":
    "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.create",
  "path": "v1/{+parent}/pullRequestComments",
  "httpMethod": "POST",
  "parameterOrder": [
    "parent",
  ],
  "parameters": {
    "parent": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const PATCH_CONFIG = {
  "id":
    "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.patch",
  "path": "v1/{+name}",
  "httpMethod": "PATCH",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
    "updateMask": {
      "location": "query",
    },
  },
} as const;

const DELETE_CONFIG = {
  "id":
    "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.delete",
  "path": "v1/{+name}",
  "httpMethod": "DELETE",
  "parameterOrder": [
    "name",
  ],
  "parameters": {
    "name": {
      "location": "path",
      "required": true,
    },
  },
} as const;

const GlobalArgsSchema = z.object({
  code: z.object({
    body: z.string().describe("Required. The comment body.").optional(),
    effectiveCommitSha: z.string().describe(
      "Output only. The effective commit sha this code comment is pointing to.",
    ).optional(),
    effectiveRootComment: z.string().describe(
      "Output only. The root comment of the conversation, derived from the reply field.",
    ).optional(),
    position: z.object({
      line: z.string().describe(
        "Required. The line number of the comment. Positive value means it's on the new side of the diff, negative value means it's on the old side.",
      ).optional(),
      path: z.string().describe("Required. The path of the file.").optional(),
    }).describe("The position of the code comment.").optional(),
    reply: z.string().describe(
      "Optional. Input only. The PullRequestComment resource name that this comment is replying to.",
    ).optional(),
    resolved: z.boolean().describe(
      "Output only. Boolean indicator if the comment is resolved.",
    ).optional(),
  }).describe("The comment on a code line.").optional(),
  comment: z.object({
    body: z.string().describe("Required. The comment body.").optional(),
  }).describe("The general pull request comment.").optional(),
  name: z.string().describe(
    "Identifier. Unique identifier for the pull request comment. The comment id is generated by the server. Format: `projects/{project}/locations/{location}/repositories/{repository}/pullRequests/{pull_request}/pullRequestComments/{comment_id}`",
  ).optional(),
  review: z.object({
    actionType: z.enum([
      "ACTION_TYPE_UNSPECIFIED",
      "COMMENT",
      "CHANGE_REQUESTED",
      "APPROVED",
    ]).describe("Required. The review action type.").optional(),
    body: z.string().describe("Optional. The comment body.").optional(),
    effectiveCommitSha: z.string().describe(
      "Output only. The effective commit sha this review is pointing to.",
    ).optional(),
  }).describe("The review summary comment.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

const StateSchema = z.object({
  code: z.object({
    body: z.string(),
    effectiveCommitSha: z.string(),
    effectiveRootComment: z.string(),
    position: z.object({
      line: z.string(),
      path: z.string(),
    }),
    reply: z.string(),
    resolved: z.boolean(),
  }).optional(),
  comment: z.object({
    body: z.string(),
  }).optional(),
  createTime: z.string().optional(),
  name: z.string(),
  review: z.object({
    actionType: z.string(),
    body: z.string(),
    effectiveCommitSha: z.string(),
  }).optional(),
  updateTime: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  code: z.object({
    body: z.string().describe("Required. The comment body.").optional(),
    effectiveCommitSha: z.string().describe(
      "Output only. The effective commit sha this code comment is pointing to.",
    ).optional(),
    effectiveRootComment: z.string().describe(
      "Output only. The root comment of the conversation, derived from the reply field.",
    ).optional(),
    position: z.object({
      line: z.string().describe(
        "Required. The line number of the comment. Positive value means it's on the new side of the diff, negative value means it's on the old side.",
      ).optional(),
      path: z.string().describe("Required. The path of the file.").optional(),
    }).describe("The position of the code comment.").optional(),
    reply: z.string().describe(
      "Optional. Input only. The PullRequestComment resource name that this comment is replying to.",
    ).optional(),
    resolved: z.boolean().describe(
      "Output only. Boolean indicator if the comment is resolved.",
    ).optional(),
  }).describe("The comment on a code line.").optional(),
  comment: z.object({
    body: z.string().describe("Required. The comment body.").optional(),
  }).describe("The general pull request comment.").optional(),
  name: z.string().describe(
    "Identifier. Unique identifier for the pull request comment. The comment id is generated by the server. Format: `projects/{project}/locations/{location}/repositories/{repository}/pullRequests/{pull_request}/pullRequestComments/{comment_id}`",
  ).optional(),
  review: z.object({
    actionType: z.enum([
      "ACTION_TYPE_UNSPECIFIED",
      "COMMENT",
      "CHANGE_REQUESTED",
      "APPROVED",
    ]).describe("Required. The review action type.").optional(),
    body: z.string().describe("Optional. The comment body.").optional(),
    effectiveCommitSha: z.string().describe(
      "Output only. The effective commit sha this review is pointing to.",
    ).optional(),
  }).describe("The review summary comment.").optional(),
  location: z.string().describe(
    "The location for this resource (e.g., 'us', 'us-central1', 'europe-west1')",
  ).optional(),
});

export const model = {
  type:
    "@swamp/gcp/securesourcemanager/repositories-pullrequests-pullrequestcomments",
  version: "2026.04.03.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.02.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "PullRequestComment represents a comment on a pull request.",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a pullRequestComments",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined) params["parent"] = String(g["parent"]);
        const body: Record<string, unknown> = {};
        if (g["code"] !== undefined) body["code"] = g["code"];
        if (g["comment"] !== undefined) body["comment"] = g["comment"];
        if (g["name"] !== undefined) body["name"] = g["name"];
        if (g["review"] !== undefined) body["review"] = g["review"];
        if (g["parent"] !== undefined && g["name"] !== undefined) {
          params["name"] = buildResourceName(
            String(g["parent"]),
            String(g["name"]),
          );
        }
        const result = await createResource(
          BASE_URL,
          INSERT_CONFIG,
          params,
          body,
          GET_CONFIG,
        ) as StateData;
        const instanceName = ((result.name ?? g.name)?.toString() ?? "current")
          .replace(/[\/\\]/g, "_").replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a pullRequestComments",
      arguments: z.object({
        identifier: z.string().describe("The name of the pullRequestComments"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        const g = context.globalArgs;
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          args.identifier,
        );
        const result = await readResource(
          BASE_URL,
          GET_CONFIG,
          params,
        ) as StateData;
        const instanceName =
          ((result.name ?? g.name)?.toString() ?? args.identifier).replace(
            /[\/\\]/g,
            "_",
          ).replace(/\.\./, "_");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update pullRequestComments attributes",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const params: Record<string, string> = { project: projectId };
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          existing["name"]?.toString() ?? g["name"]?.toString() ?? "",
        );
        const body: Record<string, unknown> = {};
        if (g["code"] !== undefined) body["code"] = g["code"];
        if (g["comment"] !== undefined) body["comment"] = g["comment"];
        if (g["review"] !== undefined) body["review"] = g["review"];
        for (const key of Object.keys(existing)) {
          if (
            key === "fingerprint" || key === "labelFingerprint" ||
            key === "etag" || key.endsWith("Fingerprint")
          ) {
            body[key] = existing[key];
          }
        }
        const result = await updateResource(
          BASE_URL,
          PATCH_CONFIG,
          params,
          body,
          GET_CONFIG,
        ) as StateData;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete the pullRequestComments",
      arguments: z.object({
        identifier: z.string().describe("The name of the pullRequestComments"),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        params["name"] = buildResourceName(
          String(g["parent"] ?? ""),
          args.identifier,
        );
        const { existed } = await deleteResource(
          BASE_URL,
          DELETE_CONFIG,
          params,
        );
        const instanceName = (g.name?.toString() ?? args.identifier).replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const handle = await context.writeResource("state", instanceName, {
          identifier: args.identifier,
          existed,
          status: existed ? "deleted" : "not_found",
          deletedAt: new Date().toISOString(),
        });
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync pullRequestComments state from GCP",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./, "_");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        try {
          const params: Record<string, string> = { project: projectId };
          const shortName = existing.name?.toString() ?? g["name"]?.toString();
          if (!shortName) throw new Error("No identifier found");
          params["name"] = buildResourceName(
            String(g["parent"] ?? ""),
            shortName,
          );
          const result = await readResource(
            BASE_URL,
            GET_CONFIG,
            params,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
    batch_create: {
      description: "batch create",
      arguments: z.object({
        requests: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined) params["parent"] = String(g["parent"]);
        const body: Record<string, unknown> = {};
        if (args["requests"] !== undefined) body["requests"] = args["requests"];
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.batchCreate",
            "path": "v1/{+parent}/pullRequestComments:batchCreate",
            "httpMethod": "POST",
            "parameterOrder": ["parent"],
            "parameters": {
              "parent": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
    resolve: {
      description: "resolve",
      arguments: z.object({
        autoFill: z.any().optional(),
        names: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined) params["parent"] = String(g["parent"]);
        const body: Record<string, unknown> = {};
        if (args["autoFill"] !== undefined) body["autoFill"] = args["autoFill"];
        if (args["names"] !== undefined) body["names"] = args["names"];
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.resolve",
            "path": "v1/{+parent}/pullRequestComments:resolve",
            "httpMethod": "POST",
            "parameterOrder": ["parent"],
            "parameters": {
              "parent": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
    unresolve: {
      description: "unresolve",
      arguments: z.object({
        autoFill: z.any().optional(),
        names: z.any().optional(),
      }),
      execute: async (args: Record<string, unknown>, context: any) => {
        const g = context.globalArgs;
        const projectId = await getProjectId();
        const params: Record<string, string> = { project: projectId };
        if (g["parent"] !== undefined) params["parent"] = String(g["parent"]);
        const body: Record<string, unknown> = {};
        if (args["autoFill"] !== undefined) body["autoFill"] = args["autoFill"];
        if (args["names"] !== undefined) body["names"] = args["names"];
        const result = await createResource(
          BASE_URL,
          {
            "id":
              "securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.unresolve",
            "path": "v1/{+parent}/pullRequestComments:unresolve",
            "httpMethod": "POST",
            "parameterOrder": ["parent"],
            "parameters": {
              "parent": { "location": "path", "required": true },
            },
          },
          params,
          body,
        );
        return { result };
      },
    },
  },
};
