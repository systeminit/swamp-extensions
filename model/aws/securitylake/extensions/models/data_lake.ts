// Auto-generated extension model for @swamp/aws/securitylake/data-lake
// Do not edit manually. Re-generate with: deno task generate:aws

// deno-lint-ignore-file no-explicit-any

import { z } from "zod";
import {
  createResource,
  deleteResource,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/aws.ts";

export const ExpirationSchema = z.object({
  Days: z.number().int().min(1).describe(
    "Number of days before data expires in the Amazon Security Lake object.",
  ).optional(),
});

export const TransitionsSchema = z.object({
  Days: z.number().int().min(1).describe(
    "Number of days before data transitions to a different S3 Storage Class in the Amazon Security Lake object.",
  ).optional(),
  StorageClass: z.string().describe(
    "The range of storage classes that you can choose from based on the data access, resiliency, and cost requirements of your workloads.",
  ).optional(),
});

export const TagSchema = z.object({
  Key: z.string().describe(
    "The key name of the tag. You can specify a value that is 1 to 128 Unicode characters in length and cannot be prefixed with aws:. You can use any of the following characters: the set of Unicode letters, digits, whitespace, `_`, `.`, `/`, `=`, `+`, and `-`.",
  ),
  Value: z.string().describe(
    "The value for the tag. You can specify a value that is 0 to 256 characters in length.",
  ),
});

const GlobalArgsSchema = z.object({
  name: z.string().describe(
    "Instance name for this resource (used as the unique identifier in the factory pattern)",
  ),
  EncryptionConfiguration: z.object({
    KmsKeyId: z.string().describe(
      "The id of KMS encryption key used by Amazon Security Lake to encrypt the Security Lake object.",
    ).optional(),
  }).describe("Provides encryption details of Amazon Security Lake object.")
    .optional(),
  LifecycleConfiguration: z.object({
    Expiration: ExpirationSchema.describe(
      "Provides data expiration details of Amazon Security Lake object.",
    ).optional(),
    Transitions: z.array(TransitionsSchema).describe(
      "Provides data storage transition details of Amazon Security Lake object.",
    ).optional(),
  }).describe("Provides lifecycle details of Amazon Security Lake object.")
    .optional(),
  ReplicationConfiguration: z.object({
    Regions: z.array(
      z.string().regex(
        new RegExp(
          "^(us(-gov)?|af|ap|ca|eu|me|sa)-(central|north|(north(?:east|west))|south|south(?:east|west)|east|west)-\\d+$",
        ),
      ),
    ).describe(
      "Replication enables automatic, asynchronous copying of objects across Amazon S3 buckets. Amazon S3 buckets that are configured for object replication can be owned by the same AWS account or by different accounts. You can replicate objects to a single destination bucket or to multiple destination buckets. The destination buckets can be in different AWS Regions or within the same Region as the source bucket.",
    ).optional(),
    RoleArn: z.string().regex(new RegExp("^arn:.*$")).describe(
      "Replication settings for the Amazon S3 buckets. This parameter uses the AWS Identity and Access Management (IAM) role you created that is managed by Security Lake, to ensure the replication setting is correct.",
    ).optional(),
  }).describe("Provides replication details of Amazon Security Lake object.")
    .optional(),
  MetaStoreManagerRoleArn: z.string().regex(new RegExp("^arn:.*$")).describe(
    "The Amazon Resource Name (ARN) used to index AWS Glue table partitions that are generated by the ingestion and normalization of AWS log sources and custom sources.",
  ).optional(),
  Tags: z.array(TagSchema).optional(),
});

const StateSchema = z.object({
  EncryptionConfiguration: z.object({
    KmsKeyId: z.string(),
  }).optional(),
  LifecycleConfiguration: z.object({
    Expiration: ExpirationSchema,
    Transitions: z.array(TransitionsSchema),
  }).optional(),
  ReplicationConfiguration: z.object({
    Regions: z.array(z.string()),
    RoleArn: z.string(),
  }).optional(),
  MetaStoreManagerRoleArn: z.string().optional(),
  Tags: z.array(TagSchema).optional(),
  Arn: z.string(),
  S3BucketArn: z.string().optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  name: z.string().optional(),
  EncryptionConfiguration: z.object({
    KmsKeyId: z.string().describe(
      "The id of KMS encryption key used by Amazon Security Lake to encrypt the Security Lake object.",
    ).optional(),
  }).describe("Provides encryption details of Amazon Security Lake object.")
    .optional(),
  LifecycleConfiguration: z.object({
    Expiration: ExpirationSchema.describe(
      "Provides data expiration details of Amazon Security Lake object.",
    ).optional(),
    Transitions: z.array(TransitionsSchema).describe(
      "Provides data storage transition details of Amazon Security Lake object.",
    ).optional(),
  }).describe("Provides lifecycle details of Amazon Security Lake object.")
    .optional(),
  ReplicationConfiguration: z.object({
    Regions: z.array(
      z.string().regex(
        new RegExp(
          "^(us(-gov)?|af|ap|ca|eu|me|sa)-(central|north|(north(?:east|west))|south|south(?:east|west)|east|west)-\\d+$",
        ),
      ),
    ).describe(
      "Replication enables automatic, asynchronous copying of objects across Amazon S3 buckets. Amazon S3 buckets that are configured for object replication can be owned by the same AWS account or by different accounts. You can replicate objects to a single destination bucket or to multiple destination buckets. The destination buckets can be in different AWS Regions or within the same Region as the source bucket.",
    ).optional(),
    RoleArn: z.string().regex(new RegExp("^arn:.*$")).describe(
      "Replication settings for the Amazon S3 buckets. This parameter uses the AWS Identity and Access Management (IAM) role you created that is managed by Security Lake, to ensure the replication setting is correct.",
    ).optional(),
  }).describe("Provides replication details of Amazon Security Lake object.")
    .optional(),
  MetaStoreManagerRoleArn: z.string().regex(new RegExp("^arn:.*$")).describe(
    "The Amazon Resource Name (ARN) used to index AWS Glue table partitions that are generated by the ingestion and normalization of AWS log sources and custom sources.",
  ).optional(),
  Tags: z.array(TagSchema).optional(),
});

export const model = {
  type: "@swamp/aws/securitylake/data-lake",
  version: "2026.04.01.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.01.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "SecurityLake DataLake resource state",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a SecurityLake DataLake",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const desiredState: Record<string, unknown> = {};
        for (const [key, value] of Object.entries(g)) {
          if (key === "name") continue;
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await createResource(
          "AWS::SecurityLake::DataLake",
          desiredState,
        ) as StateData;
        const instanceName = g.name?.toString() ?? "current";
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a SecurityLake DataLake",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the SecurityLake DataLake",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const result = await readResource(
          "AWS::SecurityLake::DataLake",
          args.identifier,
        ) as StateData;
        const instanceName = context.globalArgs.name?.toString() ??
          args.identifier;
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update a SecurityLake DataLake",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const identifier = existing.Arn?.toString();
        if (!identifier) {
          throw new Error("No identifier found in existing state");
        }
        const currentState = await readResource(
          "AWS::SecurityLake::DataLake",
          identifier,
        ) as StateData;
        const desiredState: Record<string, unknown> = { ...currentState };
        for (const [key, value] of Object.entries(g)) {
          if (key === "name") continue;
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await updateResource(
          "AWS::SecurityLake::DataLake",
          identifier,
          currentState,
          desiredState,
        );
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete a SecurityLake DataLake",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the SecurityLake DataLake",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const { existed } = await deleteResource(
          "AWS::SecurityLake::DataLake",
          args.identifier,
        );
        const instanceName = context.globalArgs.name?.toString() ??
          args.identifier;
        const handle = await context.writeResource("state", instanceName, {
          identifier: args.identifier,
          existed,
          status: existed ? "deleted" : "not_found",
          deletedAt: new Date().toISOString(),
        });
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync SecurityLake DataLake state from AWS",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const instanceName = g.name?.toString() ?? "current";
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const identifier = existing.Arn?.toString();
        if (!identifier) {
          throw new Error("No identifier found in existing state");
        }
        try {
          const result = await readResource(
            "AWS::SecurityLake::DataLake",
            identifier,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              identifier,
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
  },
};
