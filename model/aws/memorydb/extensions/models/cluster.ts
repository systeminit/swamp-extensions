// Auto-generated extension model for @swamp/aws/memorydb/cluster
// Do not edit manually. Re-generate with: deno task generate:aws

// deno-lint-ignore-file no-explicit-any

/**
 * Swamp extension model for MemoryDB Cluster (AWS::MemoryDB::Cluster).
 *
 * Wraps the CloudFormation resource type as a swamp model so create,
 * get, update, delete, and sync can be driven through `swamp model`.
 *
 * @module
 */

import { z } from "npm:zod@4.3.6";
import {
  createResource,
  deleteResource,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/aws.ts";

const TagSchema = z.object({
  Key: z.string().min(1).max(128).regex(
    new RegExp("^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\\.\\/=+:\\-@]{1,128}$"),
  ).describe("The key for the tag. May not be null."),
  Value: z.string().min(1).max(256).regex(
    new RegExp("^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\\.\\/=+:\\-@]{1,256}$"),
  ).describe("The tag's value. May be null."),
});

const GlobalArgsSchema = z.object({
  ClusterName: z.string().regex(new RegExp("[a-z][a-z0-9\\-]*")).describe(
    "The name of the cluster. This value must be unique as it also serves as the cluster identifier.",
  ),
  Description: z.string().describe("An optional description of the cluster.")
    .optional(),
  MultiRegionClusterName: z.string().describe(
    "The name of the Global Datastore, it is generated by MemoryDB adding a prefix to MultiRegionClusterNameSuffix.",
  ).optional(),
  NodeType: z.string().describe(
    "The compute and memory capacity of the nodes in the cluster.",
  ),
  NumShards: z.number().int().describe(
    "The number of shards the cluster will contain.",
  ).optional(),
  NumReplicasPerShard: z.number().int().describe(
    "The number of replicas to apply to each shard. The limit is 5.",
  ).optional(),
  SubnetGroupName: z.string().describe(
    "The name of the subnet group to be used for the cluster.",
  ).optional(),
  SecurityGroupIds: z.array(z.string()).describe(
    "One or more Amazon VPC security groups associated with this cluster.",
  ).optional(),
  MaintenanceWindow: z.string().describe(
    "Specifies the weekly time range during which maintenance on the cluster is performed. It is specified as a range in the format ddd:hh24:mi-ddd:hh24:mi (24H Clock UTC). The minimum maintenance window is a 60 minute period.",
  ).optional(),
  ParameterGroupName: z.string().describe(
    "The name of the parameter group associated with the cluster.",
  ).optional(),
  SnapshotRetentionLimit: z.number().int().describe(
    "The number of days for which MemoryDB retains automatic snapshots before deleting them. For example, if you set SnapshotRetentionLimit to 5, a snapshot that was taken today is retained for 5 days before being deleted.",
  ).optional(),
  SnapshotWindow: z.string().describe(
    "The daily time range (in UTC) during which MemoryDB begins taking a daily snapshot of your cluster.",
  ).optional(),
  ACLName: z.string().regex(new RegExp("[a-zA-Z][a-zA-Z0-9\\-]*")).describe(
    "The name of the Access Control List to associate with the cluster.",
  ),
  SnsTopicArn: z.string().describe(
    "The Amazon Resource Name (ARN) of the Amazon Simple Notification Service (SNS) topic to which notifications are sent.",
  ).optional(),
  SnsTopicStatus: z.string().describe(
    "The status of the Amazon SNS notification topic. Notifications are sent only if the status is enabled.",
  ).optional(),
  TLSEnabled: z.boolean().describe(
    "A flag that enables in-transit encryption when set to true. You cannot modify the value of TransitEncryptionEnabled after the cluster is created. To enable in-transit encryption on a cluster you must set TransitEncryptionEnabled to true when you create a cluster.",
  ).optional(),
  DataTiering: z.string().describe(
    "Enables data tiering. Data tiering is only supported for clusters using the r6gd node type. This parameter must be set when using r6gd nodes.",
  ).optional(),
  NetworkType: z.string().describe("Must be either ipv4 | ipv6 | dual_stack.")
    .optional(),
  IpDiscovery: z.string().describe(
    "For clusters wth dual stack NetworkType, IpDiscovery controls the Ip protocol (ipv4 or ipv6) returned by the engine commands such as `cluster info` and `cluster nodes` which are used by clients to connect to the nodes in the cluster.",
  ).optional(),
  KmsKeyId: z.string().describe(
    "The ID of the KMS key used to encrypt the cluster.",
  ).optional(),
  SnapshotArns: z.array(z.string()).describe(
    "A list of Amazon Resource Names (ARN) that uniquely identify the RDB snapshot files stored in Amazon S3. The snapshot files are used to populate the new cluster. The Amazon S3 object name in the ARN cannot contain any commas.",
  ).optional(),
  SnapshotName: z.string().describe(
    "The name of a snapshot from which to restore data into the new cluster. The snapshot status changes to restoring while the new cluster is being created.",
  ).optional(),
  FinalSnapshotName: z.string().describe(
    "The user-supplied name of a final cluster snapshot. This is the unique name that identifies the snapshot. MemoryDB creates the snapshot, and then deletes the cluster immediately afterward.",
  ).optional(),
  Engine: z.string().describe("The engine type used by the cluster.")
    .optional(),
  EngineVersion: z.string().describe(
    "The Redis engine version used by the cluster.",
  ).optional(),
  AutoMinorVersionUpgrade: z.boolean().describe(
    "A flag that enables automatic minor version upgrade when set to true. You cannot modify the value of AutoMinorVersionUpgrade after the cluster is created. To enable AutoMinorVersionUpgrade on a cluster you must set AutoMinorVersionUpgrade to true when you create a cluster.",
  ).optional(),
  Tags: z.array(TagSchema).describe(
    "An array of key-value pairs to apply to this cluster.",
  ).optional(),
});

const StateSchema = z.object({
  ClusterName: z.string(),
  Description: z.string().optional(),
  MultiRegionClusterName: z.string().optional(),
  Status: z.string().optional(),
  NodeType: z.string().optional(),
  NumShards: z.number().optional(),
  NumReplicasPerShard: z.number().optional(),
  SubnetGroupName: z.string().optional(),
  SecurityGroupIds: z.array(z.string()).optional(),
  MaintenanceWindow: z.string().optional(),
  ParameterGroupName: z.string().optional(),
  ParameterGroupStatus: z.string().optional(),
  Port: z.number().optional(),
  SnapshotRetentionLimit: z.number().optional(),
  SnapshotWindow: z.string().optional(),
  ACLName: z.string().optional(),
  SnsTopicArn: z.string().optional(),
  SnsTopicStatus: z.string().optional(),
  TLSEnabled: z.boolean().optional(),
  DataTiering: z.string().optional(),
  NetworkType: z.string().optional(),
  IpDiscovery: z.string().optional(),
  KmsKeyId: z.string().optional(),
  SnapshotArns: z.array(z.string()).optional(),
  SnapshotName: z.string().optional(),
  FinalSnapshotName: z.string().optional(),
  ARN: z.string().optional(),
  Engine: z.string().optional(),
  EngineVersion: z.string().optional(),
  ClusterEndpoint: z.object({
    Address: z.string(),
    Port: z.number(),
  }).optional(),
  AutoMinorVersionUpgrade: z.boolean().optional(),
  Tags: z.array(TagSchema).optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  ClusterName: z.string().regex(new RegExp("[a-z][a-z0-9\\-]*")).describe(
    "The name of the cluster. This value must be unique as it also serves as the cluster identifier.",
  ).optional(),
  Description: z.string().describe("An optional description of the cluster.")
    .optional(),
  MultiRegionClusterName: z.string().describe(
    "The name of the Global Datastore, it is generated by MemoryDB adding a prefix to MultiRegionClusterNameSuffix.",
  ).optional(),
  NodeType: z.string().describe(
    "The compute and memory capacity of the nodes in the cluster.",
  ).optional(),
  NumShards: z.number().int().describe(
    "The number of shards the cluster will contain.",
  ).optional(),
  NumReplicasPerShard: z.number().int().describe(
    "The number of replicas to apply to each shard. The limit is 5.",
  ).optional(),
  SubnetGroupName: z.string().describe(
    "The name of the subnet group to be used for the cluster.",
  ).optional(),
  SecurityGroupIds: z.array(z.string()).describe(
    "One or more Amazon VPC security groups associated with this cluster.",
  ).optional(),
  MaintenanceWindow: z.string().describe(
    "Specifies the weekly time range during which maintenance on the cluster is performed. It is specified as a range in the format ddd:hh24:mi-ddd:hh24:mi (24H Clock UTC). The minimum maintenance window is a 60 minute period.",
  ).optional(),
  ParameterGroupName: z.string().describe(
    "The name of the parameter group associated with the cluster.",
  ).optional(),
  SnapshotRetentionLimit: z.number().int().describe(
    "The number of days for which MemoryDB retains automatic snapshots before deleting them. For example, if you set SnapshotRetentionLimit to 5, a snapshot that was taken today is retained for 5 days before being deleted.",
  ).optional(),
  SnapshotWindow: z.string().describe(
    "The daily time range (in UTC) during which MemoryDB begins taking a daily snapshot of your cluster.",
  ).optional(),
  ACLName: z.string().regex(new RegExp("[a-zA-Z][a-zA-Z0-9\\-]*")).describe(
    "The name of the Access Control List to associate with the cluster.",
  ).optional(),
  SnsTopicArn: z.string().describe(
    "The Amazon Resource Name (ARN) of the Amazon Simple Notification Service (SNS) topic to which notifications are sent.",
  ).optional(),
  SnsTopicStatus: z.string().describe(
    "The status of the Amazon SNS notification topic. Notifications are sent only if the status is enabled.",
  ).optional(),
  TLSEnabled: z.boolean().describe(
    "A flag that enables in-transit encryption when set to true. You cannot modify the value of TransitEncryptionEnabled after the cluster is created. To enable in-transit encryption on a cluster you must set TransitEncryptionEnabled to true when you create a cluster.",
  ).optional(),
  DataTiering: z.string().describe(
    "Enables data tiering. Data tiering is only supported for clusters using the r6gd node type. This parameter must be set when using r6gd nodes.",
  ).optional(),
  NetworkType: z.string().describe("Must be either ipv4 | ipv6 | dual_stack.")
    .optional(),
  IpDiscovery: z.string().describe(
    "For clusters wth dual stack NetworkType, IpDiscovery controls the Ip protocol (ipv4 or ipv6) returned by the engine commands such as `cluster info` and `cluster nodes` which are used by clients to connect to the nodes in the cluster.",
  ).optional(),
  KmsKeyId: z.string().describe(
    "The ID of the KMS key used to encrypt the cluster.",
  ).optional(),
  SnapshotArns: z.array(z.string()).describe(
    "A list of Amazon Resource Names (ARN) that uniquely identify the RDB snapshot files stored in Amazon S3. The snapshot files are used to populate the new cluster. The Amazon S3 object name in the ARN cannot contain any commas.",
  ).optional(),
  SnapshotName: z.string().describe(
    "The name of a snapshot from which to restore data into the new cluster. The snapshot status changes to restoring while the new cluster is being created.",
  ).optional(),
  FinalSnapshotName: z.string().describe(
    "The user-supplied name of a final cluster snapshot. This is the unique name that identifies the snapshot. MemoryDB creates the snapshot, and then deletes the cluster immediately afterward.",
  ).optional(),
  Engine: z.string().describe("The engine type used by the cluster.")
    .optional(),
  EngineVersion: z.string().describe(
    "The Redis engine version used by the cluster.",
  ).optional(),
  AutoMinorVersionUpgrade: z.boolean().describe(
    "A flag that enables automatic minor version upgrade when set to true. You cannot modify the value of AutoMinorVersionUpgrade after the cluster is created. To enable AutoMinorVersionUpgrade on a cluster you must set AutoMinorVersionUpgrade to true when you create a cluster.",
  ).optional(),
  Tags: z.array(TagSchema).describe(
    "An array of key-value pairs to apply to this cluster.",
  ).optional(),
});

/** Swamp extension model for MemoryDB Cluster. Registered at `@swamp/aws/memorydb/cluster`. */
export const model = {
  type: "@swamp/aws/memorydb/cluster",
  version: "2026.04.23.2",
  upgrades: [
    {
      toVersion: "2026.04.01.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.03.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.23.1",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.04.23.2",
      description: "No schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "MemoryDB Cluster resource state",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a MemoryDB Cluster",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const desiredState: Record<string, unknown> = {};
        for (const [key, value] of Object.entries(g)) {
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await createResource(
          "AWS::MemoryDB::Cluster",
          desiredState,
        ) as StateData;
        const instanceName =
          ((result.ClusterName ?? g.ClusterName)?.toString() ?? "current")
            .replace(/[\/\\]/g, "_").replace(/\.\./g, "_").replace(/\0/g, "");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a MemoryDB Cluster",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the MemoryDB Cluster",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const result = await readResource(
          "AWS::MemoryDB::Cluster",
          args.identifier,
        ) as StateData;
        const instanceName =
          ((result.ClusterName ?? context.globalArgs.ClusterName)?.toString() ??
            args.identifier).replace(/[\/\\]/g, "_").replace(/\.\./g, "_")
            .replace(/\0/g, "");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update a MemoryDB Cluster",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const instanceName = (g.ClusterName?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./g, "_").replace(/\0/g, "");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const identifier = existing.ClusterName?.toString();
        if (!identifier) {
          throw new Error("No identifier found in existing state");
        }
        const currentState = await readResource(
          "AWS::MemoryDB::Cluster",
          identifier,
        ) as StateData;
        const desiredState: Record<string, unknown> = { ...currentState };
        for (const [key, value] of Object.entries(g)) {
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await updateResource(
          "AWS::MemoryDB::Cluster",
          identifier,
          currentState,
          desiredState,
          [
            "ClusterName",
            "TLSEnabled",
            "DataTiering",
            "KmsKeyId",
            "Port",
            "SubnetGroupName",
            "SnapshotArns",
            "MultiRegionClusterName",
            "SnapshotName",
            "NetworkType",
          ],
        );
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete a MemoryDB Cluster",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the MemoryDB Cluster",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const { existed } = await deleteResource(
          "AWS::MemoryDB::Cluster",
          args.identifier,
        );
        const instanceName =
          (context.globalArgs.ClusterName?.toString() ?? args.identifier)
            .replace(/[\/\\]/g, "_").replace(/\.\./g, "_").replace(/\0/g, "");
        const handle = await context.writeResource("state", instanceName, {
          identifier: args.identifier,
          existed,
          status: existed ? "deleted" : "not_found",
          deletedAt: new Date().toISOString(),
        });
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync MemoryDB Cluster state from AWS",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const instanceName = (g.ClusterName?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./g, "_").replace(/\0/g, "");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const identifier = existing.ClusterName?.toString();
        if (!identifier) {
          throw new Error("No identifier found in existing state");
        }
        try {
          const result = await readResource(
            "AWS::MemoryDB::Cluster",
            identifier,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              identifier,
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
  },
};
